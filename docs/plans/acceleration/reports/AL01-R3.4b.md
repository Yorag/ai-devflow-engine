# AL01-R3.4b Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL01-R3.4b` |
| Lane | `AL01` |
| Task | `R3.4b` |
| Coordination Base | `7aeff59` |
| Evidence report | `docs/plans/acceleration/reports/AL01-R3.4b.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |
| Implementation plan | `docs/plans/implementation/r3.4b-runtime-limit-snapshot.md` |

Worker HEAD is intentionally not declared here. The main coordination session reads the branch head during ingest.

## Scope

Implemented R3.4b runtime-limit and provider-call-policy snapshot freeze.

Changed files:

- `backend/app/domain/runtime_limit_snapshot.py`
- `backend/app/domain/provider_call_policy_snapshot.py`
- `backend/app/repositories/runtime.py`
- `backend/app/services/runs.py`
- `backend/tests/services/test_runtime_limit_snapshot.py`
- `backend/tests/services/test_provider_call_policy_snapshot.py`
- `docs/plans/implementation/r3.4b-runtime-limit-snapshot.md`
- `docs/plans/acceleration/reports/AL01-R3.4b.md`

This slice did not modify API routes, migrations, frontend files, runtime execution, graph compilation, coordination store, central checkpoint snapshot, platform-plan final status, or split-plan final status.

## Source Trace

- Platform plan task: `docs/plans/function-one-platform-plan.md` lists `R3.4b | RuntimeLimitSnapshot 与 ProviderCallPolicySnapshot 固化 | Week 4 | [ ] | 后端`.
- Split-plan task: `docs/plans/function-one-platform/03-run-projection-and-feed.md#r34b` requires run-start `RuntimeLimitSnapshot` and `ProviderCallPolicySnapshot` freeze from current persisted `PlatformRuntimeSettings`, stable refs on `PipelineRun`, template retry validation against platform limits, immutable behavior after later settings updates, and stable configuration/storage error codes.
- Backend spec: `docs/specs/function-one-backend-engine-design-v1.md` requires execution-affecting runtime settings to freeze before run startup, requires `compression_threshold_ratio` to come from `RuntimeLimitSnapshot.context_limits`, and requires provider call policy retry/timeout/backoff/circuit-breaker values to freeze per run.
- Existing prerequisite source: `C2.8` already provides persisted `PlatformRuntimeSettings`, versioning, hard limits, and stable configuration error semantics. `R3.4` and `R3.4a` already established template snapshot and runtime snapshot persistence patterns.

## TDD Evidence

Initial RED:

```text
Command: uv run python -m pytest backend/tests/services/test_runtime_limit_snapshot.py -v
Exit code: 1
Key output: 10 failed; expected ModuleNotFoundError for backend.app.domain.runtime_limit_snapshot
```

```text
Command: uv run python -m pytest backend/tests/services/test_provider_call_policy_snapshot.py -v
Exit code: 1
Key output: 7 failed; expected ModuleNotFoundError for backend.app.domain.provider_call_policy_snapshot
```

Initial GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_runtime_limit_snapshot.py backend/tests/services/test_provider_call_policy_snapshot.py -v
Exit code: 0
Key output: 17 passed in 2.72s
```

Spec review fix RED:

```text
Command: uv run python -m pytest backend/tests/services/test_runtime_limit_snapshot.py backend/tests/services/test_provider_call_policy_snapshot.py -v
Exit code: 1
Key output: 12 failed, 20 passed; expected DID NOT RAISE failures reproduced partial settings fallback through schema defaults
```

Spec review fix GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_runtime_limit_snapshot.py backend/tests/services/test_provider_call_policy_snapshot.py -v
Exit code: 0
Key output: 32 passed in 4.44s
```

## Review

Execution path: `superpowers:subagent-driven-development`.

Implementer and reviewer subagents:

- Model: `gpt-5.5`
- Reasoning effort: `xhigh`

Spec compliance review:

- Initial review found one Important issue: both builders accepted partial settings through `PlatformRuntimeSettingsRead.model_validate(settings)`, which could freeze schema defaults instead of persisted C2.8 values.
- Fix changed both builders to require a real `PlatformRuntimeSettingsRead` and to reject missing persisted fields via `model_fields_set` checks on the relevant nested settings groups.
- Follow-up tests now cover raw partial payloads, coerced partial read objects, and preservation of non-null `delivery_channel_snapshot_ref`, `current_stage_run_id`, and `status`.
- Re-review found the code-level compliance issues fixed. No Critical or Important spec findings remain.

Code quality review:

- Review found no Critical or Important issues.
- One Minor issue remains: the two new attach methods do not yet have explicit `run_id` mismatch tests, although the guard exists in code.
- The slice is approved with that minor residual test gap noted.

## Verification

Fresh main-session verification:

```text
Command: uv run python -m pytest backend/tests/services/test_runtime_limit_snapshot.py backend/tests/services/test_provider_call_policy_snapshot.py -v
Exit code: 0
Key output: 32 passed in 5.48s
```

```text
Command: uv run python -m pytest backend/tests/services/test_runtime_limit_snapshot.py backend/tests/services/test_provider_call_policy_snapshot.py backend/tests/services/test_provider_model_binding_snapshots.py backend/tests/services/test_template_snapshot.py backend/tests/services/test_runtime_settings_service.py backend/tests/db/test_runtime_model_boundary.py -q
Exit code: 0
Key output: 69 passed in 7.83s
```

```text
Command: uv run python -m pytest -q
Exit code: 0
Key output: 469 passed in 63.01s
```

After full-suite verification, only this worker evidence report and the implementation plan execution record were updated. No code, tests, configuration, dependency manifests, lock files, platform-plan statuses, split-plan statuses, acceleration execution checkpoint snapshot, or coordination store were modified after the full-suite run.

## Mock-First And Owner Conflict

- Mock-first status: not mock-first; implemented against existing C2.8 runtime settings, existing C1.7 runtime snapshot tables, and established AL01 runtime snapshot patterns.
- Owner conflict: none observed. Files stay within AL01 run/snapshot owner scope.

## Remaining Risks

- First-run startup transaction wiring is intentionally deferred to the later run-start slice that composes `PipelineRun`, template snapshot, provider/model binding snapshots, runtime-limit snapshot, provider-call-policy snapshot, graph definition, first message event, and initial `StageRun`.
- One Minor test gap remains from code-quality review: the new attach methods do not have explicit `run_id` mismatch tests, though the guard exists in code.

## Commit Readiness

The local worker result is `reported`. After a user-approved checkpoint commit containing the implementation plan, code, tests, and this evidence report, the main coordination session can ingest the claim as `implemented`.

No Git write action was performed.
