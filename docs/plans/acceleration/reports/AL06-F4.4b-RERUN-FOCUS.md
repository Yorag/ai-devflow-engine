# AL06-F4.4b-RERUN-FOCUS Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL06-F4.4b-RERUN-FOCUS` |
| Lane | `AL06` |
| Task | `F4.4b` |
| Branch | `feat/al-frontend-runtime-ui` |
| Coordination Base | `534c492` |
| Evidence report | `docs/plans/acceleration/reports/AL06-F4.4b-RERUN-FOCUS.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a checkpoint commit |

Worker HEAD is intentionally not declared here; the main coordination session reads it during ingest.

## Scope

Executed slice scope: `F4.4b RerunAction response shape and focus fix`.

This pass:

- added the frontend `RunCommandResponse` type matching backend `{ session, run }`
- changed `createRerun()` to return `RunCommandResponse` from the existing `POST /api/sessions/{sessionId}/runs` path
- changed `RerunAction` successful submit handling to focus `response.run.run_id`
- kept `RerunAction` visibility current-run-only and driven by top-level `system_status.retry_action = retry:<same run_id>`
- updated frontend and E2E route fixtures to return backend-shaped `{ session, run }`
- updated E2E terminal fixture payloads from the old mock marker to `retry:<run_id>` so the existing rerun focus scenario exercises the real contract

This pass did not modify backend rerun contracts, backend projectors, runtime services, package manifests, lock files, configuration files, the shared coordination store, the acceleration execution plan checkpoint snapshot, platform-plan final status, or split-plan final status.

## Source Trace

- Platform task: `docs/plans/function-one-platform-plan.md` lists `F4.4b | RerunAction 响应形状与焦点修复 | Week 11 | [ ] | 前端`.
- Split task: `docs/plans/function-one-platform/04-human-loop-and-runtime.md#f44b`.
- Start gate: `docs/plans/function-one-acceleration-execution-plan.md` states F4.4b only fixes frontend consumption of backend `POST /api/sessions/{sessionId}/runs` `{ session, run }` response shape and new run boundary focus; it must not modify backend rerun API, system status projection, or QA-E2E scenario semantics.
- Backend contract source: `backend/app/schemas/run.py` defines `RunCommandResponse` as `{ session: SessionRead, run: RunSummaryProjection }`.
- Frontend source: `docs/specs/frontend-workspace-global-design-v1.md` section 13.2 requires successful rerun to move page focus to the new run segment.
- Product source: `docs/specs/function-one-product-overview-v1.md` sections 8 and 10.5 keep rerun scoped to current failed or terminated runs and require a new `PipelineRun`, not old-run resume.
- Implementation plan: `docs/plans/implementation/f4.4b-rerun-response-focus.md`.

Source Trace Conflict Gate result: no conflict found. The E2E file change updates only route-fixture payload shape and mock command response shape so the existing scenario assertions exercise the current backend contract; scenario assertions were not weakened or changed.

## Changed Files

- `docs/plans/implementation/f4.4b-rerun-response-focus.md`
- `docs/plans/acceleration/reports/AL06-F4.4b-RERUN-FOCUS.md`
- `frontend/src/api/types.ts`
- `frontend/src/api/runs.ts`
- `frontend/src/api/__tests__/client.test.ts`
- `frontend/src/features/runs/RerunAction.tsx`
- `frontend/src/features/runs/__tests__/RerunAction.test.tsx`
- `frontend/src/mocks/handlers.ts`
- `e2e/tests/function-one-control-flow.spec.ts`

## TDD Evidence

Execution path: `superpowers:subagent-driven-development`.

Implementer subagent model: `gpt-5.5`, reasoning effort `xhigh`.

RED commands:

```powershell
npm --prefix frontend run test -- RerunAction
npm --prefix frontend run build
```

RED results:

- `npm --prefix frontend run test -- RerunAction`: exit `1`; key output `1 failed`, `8 passed`, `focusSpy` was not called after the test changed the rerun mock to `{ session, run }`.
- `npm --prefix frontend run build`: exit `1`; key output `RunCommandResponse` was not exported and the new mocked response shape exposed missing `run` / `session` fields on the old `RunSummaryProjection` return type.

GREEN commands reported by the implementer:

```powershell
npm --prefix frontend run test -- RerunAction
npm --prefix frontend run test -- client
npm --prefix frontend run test -- WorkspaceShell
npm --prefix frontend run build
git diff --check
```

GREEN results reported by the implementer:

- `npm --prefix frontend run test -- RerunAction`: exit `0`; `Test Files 1 passed`, `Tests 9 passed`.
- `npm --prefix frontend run test -- client`: exit `0`; `Test Files 1 passed`, `Tests 7 passed`.
- `npm --prefix frontend run test -- WorkspaceShell`: exit `0`; `Test Files 1 passed`, `Tests 31 passed`.
- `npm --prefix frontend run build`: exit `0`; TypeScript and Vite build passed.
- `git diff --check`: exit `0`; CRLF normalization warnings only, no whitespace errors.

## Debugging And E2E Fixture Alignment

After the first full frontend verification pass, the required Playwright command initially could not run because local `e2e/node_modules` was absent and `npm --prefix e2e run test` resolved to a global Conda `playwright.exe` that returned `unknown command 'test'`.

The user explicitly approved dependency installation. `npm --prefix e2e ci` installed 3 packages from the existing `e2e/package-lock.json`; package manifests and lock files did not change.

After installation, `npm --prefix e2e run test -- function-one-control-flow.spec.ts` executed the scenario and failed at `Retry run` visibility in both tests. Root cause investigation found the E2E route fixture still emitted old `retryAction: "create_rerun"` payloads, while current `RerunAction` correctly requires `retry:<run_id>`. The fixture was updated to:

- emit `retry:${runId}` for terminated system status
- emit `retry:${entry.run_id}` for failed tool-denial system status
- return `{ session, run }` from the mock rerun POST route

Scenario assertions were not changed.

## Review

Spec / plan compliance review:

- Reviewer model: `gpt-5.5`, reasoning effort `xhigh`.
- Result: no Critical or Important findings.
- Minor note: evidence report still needed to be created at review time; this report resolves that.
- Confirmed `createRerun()` returns `RunCommandResponse`, `RerunAction` focuses `response.run.run_id`, visibility remains current terminal run plus `retry:<same run_id>`, and no forbidden backend, central tracking, platform status, split status, or coordination-store changes were made.

Code quality / testing review:

- Reviewer model: `gpt-5.5`, reasoning effort `xhigh`.
- Result: no Critical, Important, or Minor findings.
- Confirmed precise TypeScript types, nested response focus coverage, client response-shape coverage, unchanged invalidation / error handling / submit guarding / visibility behavior, and coherent frontend mock response.

E2E fixture alignment re-review:

- Reviewer model: `gpt-5.5`, reasoning effort `xhigh`.
- Result: no Critical, Important, or Minor findings.
- Confirmed the E2E fixture changes stay within F4.4b response-focus purpose, do not weaken scenario assertions, and serialize the updated session with the new run response.

## Verification

Fresh final verification in the main session:

```powershell
npm --prefix frontend run test -- RerunAction
npm --prefix frontend run test -- client
npm --prefix frontend run test -- WorkspaceShell
npm --prefix frontend test
npm --prefix frontend run build
npm --prefix e2e run test -- function-one-control-flow.spec.ts
git diff --check
```

Results:

- `npm --prefix frontend run test -- RerunAction`: exit `0`; `Test Files 1 passed (1)`, `Tests 9 passed (9)`.
- `npm --prefix frontend run test -- client`: exit `0`; `Test Files 1 passed (1)`, `Tests 7 passed (7)`.
- `npm --prefix frontend run test -- WorkspaceShell`: exit `0`; `Test Files 1 passed (1)`, `Tests 31 passed (31)`.
- `npm --prefix frontend test`: exit `0`; `Test Files 29 passed (29)`, `Tests 237 passed (237)`.
- `npm --prefix frontend run build`: exit `0`; `tsc --noEmit && vite build`, `137 modules transformed`.
- `npm --prefix e2e run test -- function-one-control-flow.spec.ts`: exit `0`; `2 passed`.
- `git diff --check`: exit `0`; CRLF normalization warnings only, no whitespace errors.

## Diff And Worktree State

Pre-evidence implementation diff stat:

```text
e2e/tests/function-one-control-flow.spec.ts        |  9 +++--
frontend/src/api/__tests__/client.test.ts          | 43 +++++++++++++++++++++-
frontend/src/api/runs.ts                           |  8 +++-
frontend/src/api/types.ts                          |  5 +++
frontend/src/features/runs/RerunAction.tsx         |  4 +-
frontend/src/features/runs/__tests__/RerunAction.test.tsx   | 29 +++++++++++----
frontend/src/mocks/handlers.ts                     |  5 ++-
7 files changed, 86 insertions(+), 17 deletions(-)
```

After the final full frontend suite, build, Playwright verification, and `git diff --check`, only this evidence report and `docs/plans/implementation/f4.4b-rerun-response-focus.md` were updated. No code, tests, package manifests, lock files, configuration, backend files, central tracking docs, platform-plan status, split-plan status, acceleration execution plan, or coordination store changes were made after final verification.

## Remaining Risks

- The E2E fixture update is intentionally narrow and only aligns existing test payloads with the current backend contract. Future QA-E2E live work should continue to validate the real backend response shape on the QA lane or integration branch.
- `npm --prefix e2e ci` populated local `e2e/node_modules` for verification after user approval. It did not change tracked package manifests or lock files.

## Commit Readiness

The local result is `reported`. Suggested checkpoint commit subject:

```text
fix(runtime-ui): focus rerun response boundary
```

After a checkpoint commit contains this report, the implementation plan, code, tests, and E2E fixture alignment, the main coordination session can scan and ingest the claim as `implemented`.
