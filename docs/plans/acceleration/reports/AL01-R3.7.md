# AL01-R3.7 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL01-R3.7` |
| Lane | `AL01` |
| Task | `R3.7` |
| Coordination Base | `5707ce3` |
| Evidence report | `docs/plans/acceleration/reports/AL01-R3.7.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |
| Implementation plan | `docs/plans/implementation/r3.7-stage-artifact-store.md` |

Worker HEAD is intentionally not declared here. The main coordination session reads the branch head during ingest.

## Scope

Implemented `R3.7` StageArtifact storage service behavior on the existing generic runtime artifact boundary.

Changed files:

- `backend/app/services/artifacts.py`
- `backend/tests/services/test_artifact_store.py`
- `docs/plans/implementation/r3.7-stage-artifact-store.md`
- `docs/plans/acceleration/reports/AL01-R3.7.md`

This slice did not modify runtime schema, Alembic revisions, API routes, OpenAPI contracts, EventStore payload semantics, projection service code, frontend files, coordination store, central checkpoint snapshot, platform-plan final status, or split-plan final status.

## Source Trace

- Platform plan task: `docs/plans/function-one-platform-plan.md` lists `R3.7 | StageArtifact 存储 | Week 4-5 | [ ] | 后端`.
- Split-plan task: `docs/plans/function-one-platform/03-run-projection-and-feed.md#r37` requires an `ArtifactStore` that persists stage input, process, output, artifacts, and metrics; binds `StageRun.input_ref` / `output_ref` through stable references; keeps artifact indexing inside runtime ownership; and records artifact write failure, payload truncation, and metric writes in runtime logs.
- Backend spec: `docs/specs/function-one-backend-engine-design-v1.md:848-863` defines `StageArtifact` as the unified runtime container and requires downstream reads to rely on stable references rather than raw graph state.
- Approved runtime boundary: `C1.7` already fixed the generic `StageArtifactModel` (`artifact_id`, `run_id`, `stage_run_id`, `artifact_type`, `payload_ref`, `process`, `metrics`, `created_at`) as the runtime truth boundary.
- AL02 reader compatibility trace: `Q3.3`, `Q3.4`, and `Q3.4a` already read typed `input_snapshot`, `output_snapshot`, trace refs, log refs, and artifact payloads from `StageArtifactModel.process`, so this slice wrote into that established generic container instead of creating a second artifact schema.

## TDD Evidence

Initial RED:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_artifact_store.py -v
Exit code: 1
Key output: ModuleNotFoundError: No module named 'backend.app.services.artifacts'
```

Initial GREEN:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_artifact_store.py -v
Exit code: 0
Key output: 11 passed in 1.28s
```

Review-driven fix RED:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_artifact_store.py -v
Exit code: 1
Key output: 8 failed, 12 passed; failures reproduced missing updated_at changes, missing run/stage ownership guard, missing failure-path logs, raw SQLAlchemy leakage, and blocked-redaction metadata loss
```

Review-driven fix GREEN:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_artifact_store.py -v
Exit code: 0
Key output: 20 passed in 2.58s
```

Final integrity/logging fix RED:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_artifact_store.py -v
Exit code: 1
Key output: 2 failed, 20 passed; failures reproduced missing corrupted artifact/stage run-id mismatch rejection in complete_stage_output() and success log trace retaining wrong-run
```

Final integrity/logging fix GREEN:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_artifact_store.py -v
Exit code: 0
Key output: 22 passed in 2.28s
```

## Review

Execution path: `superpowers:subagent-driven-development`.

Implementer and reviewer subagents:

- Implementer: `gpt-5.5`, reasoning `high`
- Spec compliance reviewer: `gpt-5.5`, reasoning `high`
- Code quality reviewer: `gpt-5.5`, reasoning `high`

Spec compliance review:

- Initial review returned FAIL with Important findings:
  - failure-path artifact writes were not logged
  - blocked-redaction log summaries could lose required stable metadata
  - tests did not prove metric-write and failure-path logging behavior
- Repair added explicit failure-path runtime logs, stable summary metadata merging even when payload redaction blocks the content body, and focused test coverage for metric-write and failure-path logging.
- Final re-review PASS with no remaining Critical or Important findings.

Code quality review:

- First review returned FAIL with Important findings:
  - `create_stage_input()` did not enforce `run_id == stage.run_id`
  - storage failures leaked raw SQLAlchemy exceptions instead of `ArtifactStoreError`
  - `StageRun.updated_at` was not advanced when binding `input_ref` / `output_ref`
  - integrity and storage failure tests were missing
- Second review after the first repair returned FAIL with Important findings:
  - `complete_stage_output()` still accepted corrupted artifact/stage run-id mismatch
  - success logs trusted mismatched caller trace identity instead of normalizing to actual run/stage identity
- Final repair added the missing output-path integrity guard and normalized success log traces to actual artifact `run_id` / `stage_run_id`.
- Final re-review PASS with no remaining Critical or Important findings.

## Verification

Fresh main-session verification:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_artifact_store.py -v
Exit code: 0
Key output: 22 passed in 2.09s
```

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_artifact_store.py backend/tests/services/test_stage_run_store.py backend/tests/db/test_runtime_model_boundary.py -q
Exit code: 0
Key output: 42 passed in 4.96s
```

```text
Command: uv run --no-sync python -m pytest backend/tests/projections/test_stage_inspector_projection.py backend/tests/projections/test_control_item_detail_projection.py -q
Exit code: 0
Key output: 11 passed in 3.05s
```

After these passing verification commands, only tracking docs were updated: this worker evidence report and the implementation plan. No code, tests, configuration, dependency manifests, lock files, API routes, runtime schema files, platform-plan statuses, split-plan statuses, acceleration checkpoint snapshot, or coordination store were modified afterward.

## Mock-First And Owner Conflict

- Mock-first status: not mock-first; implemented directly against the approved generic runtime artifact boundary and current AL02 readers.
- Owner conflict: none observed. The code changes stay inside AL01 ownership for runtime run truth, `StageRun` stable refs, and `StageArtifact` storage semantics.

## Remaining Risks

- `get_stage_artifact()` failure logging uses a fallback synthetic trace context when no caller trace is available, so those failure logs remain attributable to the artifact store but may not carry a user/session/run identity unless the caller supplies one through a write-path method.
- The service assumes persisted `process` and `metrics` values remain mapping-shaped JSON objects, which matches the approved C1.7 runtime boundary and current AL02 reader expectations.

## Commit Readiness

The local worker result is `reported`. After a user-approved checkpoint commit containing the implementation plan, code, tests, and this evidence report, the main coordination session can ingest the claim as `implemented`.

No Git write action was performed.
