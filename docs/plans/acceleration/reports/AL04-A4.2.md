# AL04-A4.2 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL04-A4.2` |
| Lane | `AL04` |
| Task | `A4.2` |
| Branch | `feat/al-tools-deterministic-delivery` |
| Coordination Base | `32d5a87` |
| Evidence result | `reported` |
| Expected committed ingest result | `implemented` |
| Implementation plan | `docs/plans/implementation/a4.2-deterministic-six-stage-runtime.md` |

Worker HEAD is not declared by this worker report. The main coordination session reads branch HEAD during ingest.

## Gate Summary

- Current branch: `feat/al-tools-deterministic-delivery`
- Active claim status at worker start: `claimed`
- Claim branch matched the current branch.
- Platform task status: A4.2 `[ ]`
- Split-plan task status: A4.2 `[ ]`
- Owner boundary respected: implementation stayed inside deterministic runtime, focused runtime tests, this implementation plan, and this worker evidence report.
- No coordination store, acceleration execution plan, platform-plan final status, or split-plan final status was updated.

## Scope Outcome

- Added `DeterministicRuntimeEngine` under `backend/app/runtime/deterministic.py`.
- Implemented stable six-stage advancement for:
  - `requirement_analysis`
  - `solution_design`
  - `code_generation`
  - `test_generation_execution`
  - `code_review`
  - `delivery_integration`
- Persisted each stage advancement through StageRun, StageArtifact, domain events, checkpoint refs, and runtime log summary refs.
- Kept `solution_validation` internal to the `solution_design` artifact process and stage update events.
- Consumed frozen `RuntimeExecutionContext` snapshot refs without reading latest runtime settings.
- Preserved A4.2 scope boundaries:
  - no concrete tool execution;
  - no terminal success/failure/termination control;
  - no DeliveryRecord or `demo_delivery`;
  - no API/OpenAPI/frontend changes.
- Completed a real R3.2 startup shape by reusing and completing an existing in-order running `requirement_analysis` StageRun instead of duplicating it.
- Aligned new StageRun `graph_node_key` and `stage_contract_ref` with frozen graph stage keys.
- Added stable bounded deterministic ids for StageRun and StageArtifact rows.
- Rejected out-of-order non-terminal StageRun rows before creating a new earlier stage.

## Changed Files

- `backend/app/runtime/deterministic.py`
- `backend/tests/runtime/test_deterministic_runtime.py`
- `docs/plans/implementation/a4.2-deterministic-six-stage-runtime.md`
- `docs/plans/acceleration/reports/AL04-A4.2.md`

## TDD Evidence

Initial RED:

```text
uv run pytest backend/tests/runtime/test_deterministic_runtime.py -v
Exit code: 1
Key output: ModuleNotFoundError: No module named 'backend.app.runtime.deterministic'
```

Initial GREEN:

```text
uv run pytest backend/tests/runtime/test_deterministic_runtime.py -v
Exit code: 0
Key output: 6 passed
```

Spec-review fix RED:

```text
uv run pytest backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_completes_existing_running_initial_stage backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_uses_frozen_stage_contract_keys -v
Exit code: 1
Key output: 2 failed; existing running requirement_analysis could not advance, and stage_contract_ref used synthetic refs.
```

Spec-review fix GREEN:

```text
uv run pytest backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_completes_existing_running_initial_stage backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_uses_frozen_stage_contract_keys -v
Exit code: 0
Key output: 2 passed
```

Code-quality fix RED:

```text
uv run pytest backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_bounds_stage_and_artifact_ids backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_rejects_out_of_order_active_stage -v
Exit code: 1
Key output: 2 failed; deterministic ids exceeded schema length and out-of-order active stage was not rejected.
```

Code-quality fix GREEN:

```text
uv run pytest backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_bounds_stage_and_artifact_ids backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_rejects_out_of_order_active_stage -v
Exit code: 0
Key output: 2 passed
```

Additional non-terminal active-state hardening RED:

```text
uv run pytest backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_rejects_out_of_order_waiting_stage -v
Exit code: 1
Key output: later waiting code_review with earlier gaps was not rejected.
```

Additional non-terminal active-state hardening GREEN:

```text
uv run pytest backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_rejects_out_of_order_waiting_stage backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_rejects_out_of_order_active_stage backend/tests/runtime/test_deterministic_runtime.py::test_deterministic_runtime_completes_existing_running_initial_stage -v
Exit code: 0
Key output: 3 passed
```

## Review Result

Spec compliance review:

- Initial review found two Important findings:
  - `stage_contract_ref` used synthetic values instead of frozen stage contract keys;
  - the runtime could not advance an existing R3.2-created running initial stage.
- Both findings were fixed and re-reviewed.
- Re-review result: no Critical or Important findings remain.

Code quality and test sufficiency review:

- Initial review found two Important findings:
  - deterministic stage/artifact ids were not bounded to schema limits;
  - out-of-order active persisted state could create a new earlier active stage.
- Both findings were fixed through TDD.
- Follow-up hardening also covers out-of-order waiting stage state.
- Re-review result: no Critical, Important, or Minor findings remain.

## Verification

Focused verification:

```text
uv run pytest backend/tests/runtime/test_deterministic_runtime.py -v
Exit code: 0
Key output: 11 passed
```

Impacted regression:

```text
uv run pytest backend/tests/runtime/test_deterministic_runtime.py backend/tests/runtime/test_runtime_engine_contract.py backend/tests/services/test_stage_run_store.py backend/tests/services/test_artifact_store.py backend/tests/events/test_event_store.py -v
Exit code: 0
Key output: 74 passed
```

Full backend suite:

```text
uv run pytest -q
Exit code: 0
Key output: 1014 passed, 3 warnings in 283.41s (0:04:43)
Warnings: existing backend/tests/providers/test_langchain_adapter.py UserWarning about explicit temperature parameters passed through model_kwargs.
```

Post-full-suite tracking-only updates:

- After the final full backend suite passed, only `docs/plans/implementation/a4.2-deterministic-six-stage-runtime.md` and this evidence report were updated with verification evidence.
- No code, test, configuration, dependency manifest, lockfile, or runtime-behavior artifact changed after the full suite.

## Owner Conflict Check

No owner conflict was introduced.

- AL01 run startup and graph compiler code was consumed, not modified.
- AL02 projection/SSE files were not modified.
- AL03 runtime orchestration services were consumed, not modified.
- AL05 Provider/LangGraph runtime files were not modified.
- AL06 frontend files were not modified.
- Shared coordination store, central checkpoint snapshot, platform plan, and split-plan final statuses were not modified.

## Remaining Risk

- A4.2 intentionally leaves clarification, approval, tool confirmation, failure, termination, DeliveryRecord, and `demo_delivery` for later slices.
- `PipelineRun.current_stage_run_id` remains pointed at the most recently completed stage until a later runtime-control slice updates terminal or waiting semantics.

## Commit Readiness

Local result is `reported`. After a checkpoint commit containing code, tests, implementation plan, and this evidence report, the main coordination session can ingest this claim as `implemented`.
