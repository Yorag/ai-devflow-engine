# AL01-R3.4a Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL01-R3.4a` |
| Lane | `AL01` |
| Task | `R3.4a` |
| Coordination Base | `74578b0` |
| Evidence report | `docs/plans/acceleration/reports/AL01-R3.4a.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |
| Implementation plan | `docs/plans/implementation/r3.4a-provider-model-binding-snapshots.md` |

Worker HEAD is intentionally not declared here. The main coordination session reads the branch head during ingest.

## Scope

Implemented R3.4a Provider and model binding snapshot freeze.

Changed files:

- `backend/app/domain/provider_snapshot.py`
- `backend/app/repositories/runtime.py`
- `backend/app/services/runs.py`
- `backend/tests/services/test_provider_model_binding_snapshots.py`
- `docs/plans/implementation/r3.4a-provider-model-binding-snapshots.md`
- `docs/plans/acceleration/reports/AL01-R3.4a.md`

This slice did not modify API routes, migrations, Provider services, configuration services, LangGraph runtime, Provider adapters, StageAgentRuntime, runtime-limit snapshots, Provider call policy snapshots, graph compilation, first-run API startup transaction, coordination store, central checkpoint snapshot, platform-plan final status, or split-plan final status.

## Source Trace

- Platform plan task: `docs/plans/function-one-platform-plan.md` lists `R3.4a | Provider 与模型绑定快照固化 | Week 4 | [ ] | 后端`.
- Split-plan task: `docs/plans/function-one-platform/03-run-projection-and-feed.md#r34a` requires run-start ProviderSnapshot and ModelBindingSnapshot freeze, structured runtime snapshot persistence, stable errors for missing/invalid configuration, and immunity to later Provider/default model/capability changes.
- Backend spec: `docs/specs/function-one-backend-engine-design-v1.md` requires run execution to consume frozen Provider/model binding snapshots and to cover `agent_role`, `context_compression`, `structured_output_repair`, and `validation_pass` binding semantics.
- Product spec: `docs/specs/function-one-product-overview-v1.md` requires Provider configuration, credential rotation, default model changes, and capability changes to affect only later runs after a run has started.

## TDD Evidence

Initial RED:

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py -v
Exit code: 1
Key output: collection failed with ModuleNotFoundError: No module named 'backend.app.domain.provider_snapshot'
```

Initial GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py -v
Exit code: 0
Key output: 7 passed
```

Spec review fix RED:

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py -v
Exit code: 1
Key output: 3 intended failures for non-default model binding and strict capability validation.
```

Spec review fix GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py -v
Exit code: 0
Key output: 10 passed
```

Code quality fix RED:

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py -v
Exit code: 1
Key output: 2 failed, 10 passed; reversed provider snapshot order and nested mutability regressions reproduced.
```

Code quality fix GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py -v
Exit code: 0
Key output: 12 passed in 0.80s
```

Final mutability hardening RED:

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py::test_snapshot_payloads_are_deeply_immutable_after_build -v
Exit code: 1
Key output: augmented dict union raised after mutating the snapshot model_parameters value.
```

Final mutability hardening GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py::test_snapshot_payloads_are_deeply_immutable_after_build -v
Exit code: 0
Key output: 1 passed in 0.37s
```

## Review

Execution path: `superpowers:subagent-driven-development`.

Implementer and reviewer subagents:

- Model: `gpt-5.5`
- Reasoning effort: `xhigh`

Spec compliance review:

- Initial review requested changes for non-default internal model binding on the same Provider and strict capability validation.
- Fix added provider/model-specific snapshots through `required_model_ids_by_provider` and explicit required-key/type checks before schema construction.
- Re-review confirmed both code/spec findings were fixed. The remaining request was only to update this evidence report and the implementation plan.

Code quality review:

- Initial review requested changes for shallow-frozen snapshot payloads and order-dependent agent-role default model selection.
- Fix added frozen capability models, recursively immutable model parameters, copied binding capabilities, `is_default_model`, explicit default selection, and reversed-order plus immutability regression tests.
- Re-review requested one remaining fix for `_FrozenDict.__ior__`.
- Final re-review approved the `__ior__` fix and found no new Critical or Important findings.

No Critical or Important findings remain.

## Verification

Fresh main-session verification:

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py -v
Exit code: 0
Key output: 12 passed in 0.54s
```

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py backend/tests/services/test_template_snapshot.py backend/tests/db/test_runtime_model_boundary.py backend/tests/providers/test_provider_registry.py -q
Exit code: 0
Key output: 33 passed in 2.11s
```

```text
Command: uv run python -m pytest backend/tests/services/test_provider_model_binding_snapshots.py backend/tests/providers/test_provider_registry.py -q
Exit code: 0
Key output: 19 passed in 0.61s
```

```text
Command: uv run python -m pytest -q
Exit code: 0
Key output: 407 passed in 51.19s
```

After full-suite verification, only this worker evidence report and the implementation plan execution record were updated. No code, tests, configuration, dependency manifests, lock files, platform-plan statuses, split-plan statuses, acceleration execution checkpoint snapshot, or coordination store were modified after the full-suite run.

## Mock-First And Owner Conflict

- Mock-first status: not mock-first; implemented against existing C1.7 runtime snapshot tables, C1.10 snapshot schemas, R3.4 template snapshots, and existing Provider control models.
- Owner conflict: none observed. Files are within AL01 run/snapshot owner scope.

## Remaining Risks

- First-run startup transaction wiring is intentionally deferred to the run startup slice that composes PipelineRun, template snapshot, Provider/model binding snapshots, runtime-limit snapshots, GraphDefinition, first message event, and initial StageRun.
- Provider adapter and LangGraph runtime consumption of these snapshots are intentionally handled by later runtime/provider slices.

## Commit Readiness

The local worker result is `reported`. After a user-approved checkpoint commit containing the implementation plan, code, tests, and this evidence report, the main coordination session can ingest the claim as `implemented`.

No Git write action was performed.
