# AL06-F3.5 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL06-F3.5` |
| Lane | `AL06` |
| Task | `F3.5` |
| Coordination Base | `74578b0` |
| Evidence report | `docs/plans/acceleration/reports/AL06-F3.5.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |

Worker HEAD is intentionally not declared here; the main coordination session reads it during ingest.

## Scope

Assigned owner scope: frontend workspace store, SSE reducer, Feed, StageNode, Inspector, Composer, Approval, Tool Confirmation, Delivery UI.

Executed slice scope: `F3.5 Run Boundary 与 Run Switcher` only.

This pass did not modify backend projection payloads, SSE payload schemas, API routes, OpenAPI generation, event schemas, API type files, event reducer files, package manifests, lock files, central Claim Ledger, platform plan status, split-plan final status, or the coordination store.

## Source Trace

- Platform plan task: `docs/plans/function-one-platform-plan.md` lists `F3.5 | Run Boundary 与 Run Switcher | Week 5-6 | [ ] | 前端`.
- Split-plan task: `docs/plans/function-one-platform/03-run-projection-and-feed.md#f35` creates `RunBoundary`, `RunSwitcher`, `groupEntriesByRun()`, `scrollToRunBoundary()`, and focused tests.
- Frontend Run Switcher boundary: `docs/specs/frontend-workspace-global-design-v1.md` sections `5` and `8.1` define Run Switcher as same-session page-local navigation only.
- Backend projection boundary: `docs/specs/function-one-backend-engine-design-v1.md` section `8.1` requires `SessionWorkspaceProjection.runs[]` and `narrative_feed[].run_id` to support frontend same-page run segmentation.

## Changed Files

- Created `docs/plans/implementation/f3.5-run-boundary-switcher.md`
- Created `frontend/src/features/feed/__tests__/RunSwitcher.test.tsx`
- Created `frontend/src/features/feed/RunBoundary.tsx`
- Created `frontend/src/features/feed/RunSwitcher.tsx`
- Modified `frontend/src/features/feed/NarrativeFeed.tsx`
- Modified `frontend/src/features/workspace/WorkspaceShell.tsx`
- Modified `frontend/src/styles/global.css`
- Created `docs/plans/acceleration/reports/AL06-F3.5.md`

## TDD Evidence

Initial RED command:

```powershell
npm --prefix frontend run test -- RunSwitcher
```

Initial RED exit code: `1`.

Initial RED key output:

```text
Failed to resolve import "../RunBoundary" from "src/features/feed/__tests__/RunSwitcher.test.tsx"
```

Initial GREEN command:

```powershell
npm --prefix frontend run test -- RunSwitcher
```

Initial GREEN exit code: `0`.

Initial GREEN key output:

```text
Test Files  1 passed (1)
Tests  9 passed (9)
```

Spec review fix RED command:

```powershell
npm --prefix frontend run test -- RunSwitcher
```

Spec review fix RED exit code: `1`.

Spec review fix RED key output:

```text
expected undefined to be 'run-orphan'
```

Spec review fix GREEN command:

```powershell
npm --prefix frontend run test -- RunSwitcher
```

Spec review fix GREEN exit code: `0`.

Spec review fix GREEN key output:

```text
Test Files  1 passed (1)
Tests  9 passed (9)
```

Code quality review fix RED command:

```powershell
npm --prefix frontend run test -- RunSwitcher
```

Code quality review fix RED exit code: `1`.

Code quality review fix key failures:

```text
switcher rendered for one real run plus fallback
fallback appeared as a switcher item
empty entries showed the global empty state instead of Run 1 boundary
```

Code quality review fix GREEN command:

```powershell
npm --prefix frontend run test -- RunSwitcher
```

Code quality review fix GREEN exit code: `0`.

Code quality review fix GREEN key output:

```text
Test Files  1 passed (1)
Tests  11 passed (11)
```

## Review

Spec / plan compliance review result:

- Initial review requested changes.
- Important finding: missing run metadata fallback fabricated `RunSummaryProjection` semantics.
- Fix result: fallback group now carries `runId`, `attemptIndex: null`, and entries only; `RunBoundary` renders `Run metadata unavailable`, run id, and entry count without status, trigger source, timestamps, stage, or current/historical marker.
- Spec compliance re-review approved with no Critical, Important, or Minor findings.

Code quality / testing / regression review result:

- Initial review requested changes.
- Important finding 1: one backend-known run plus fallback group rendered `RunSwitcher`.
- Important finding 2: zero-entry run summaries were hidden by the global empty feed state.
- Important finding 3: fallback run ids could appear inside the switcher and create narrow-screen overflow risk.
- Fix result: `RunSwitcher` now counts and renders only backend-known run groups; `NarrativeFeed` renders run boundaries for non-empty `runs[]` even when `entries` is empty; fallback groups remain visible only as neutral feed boundaries.
- Code quality re-review approved. Previous Important findings were resolved. No Critical or Important findings remained.

## Verification

Final fresh focused verification command:

```powershell
npm --prefix frontend run test -- RunSwitcher
```

Exit code: `0`.

Key output:

```text
Test Files  1 passed (1)
Tests  11 passed (11)
```

Final fresh impacted regression commands:

```powershell
npm --prefix frontend run test -- FeedEntryRenderer
npm --prefix frontend run test -- StageNode
```

Exit codes: `0` and `0`.

Key output:

```text
FeedEntryRenderer: Tests  7 passed (7)
StageNode: Tests  7 passed (7)
```

Final impacted frontend suite command:

```powershell
npm --prefix frontend run test
```

Exit code: `0`.

Key output:

```text
Test Files  13 passed (13)
Tests  94 passed (94)
```

Final impacted frontend build command:

```powershell
npm --prefix frontend run build
```

Exit code: `0`.

Key output:

```text
tsc --noEmit && vite build
110 modules transformed
built in 1.17s
```

After these verification commands, only this worker evidence report and the implementation plan execution record were updated.

## Mock-First Status

This slice is implemented against the existing frozen frontend `RunSummaryProjection`, `SessionWorkspaceProjection`, and `TopLevelFeedEntry.run_id` contract. It does not finalize backend projection generation or real SSE ordering. Later integration must still verify the real backend workspace projection returns `runs[]` and `narrative_feed[]` in the expected order and with matching `run_id` values.

## Remaining Risks

- Real API/SSE projection compatibility still needs integration verification when AL02/AL06 real-stream wiring is ready.
- Runtime commands such as retry creation, pause/resume, approvals, tool confirmations, and Composer binding are intentionally out of scope for F3.5.

## Commit Readiness

Commit approval can be requested for this reported local checkpoint. After a user-approved checkpoint commit, the main coordination session can ingest it as `implemented`.
