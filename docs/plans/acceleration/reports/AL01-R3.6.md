# AL01-R3.6 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL01-R3.6` |
| Lane | `AL01` |
| Task | `R3.6` |
| Coordination Base | `969a43c` |
| Evidence report | `docs/plans/acceleration/reports/AL01-R3.6.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |
| Implementation plan | `docs/plans/implementation/r3.6-stage-run-store.md` |

Worker HEAD is intentionally not declared here. The main coordination session reads the branch head during ingest.

## Scope

Implemented `R3.6` StageRun persistence and lifecycle storage/service behavior.

Changed files:

- `backend/app/db/models/runtime.py`
- `backend/app/repositories/runtime.py`
- `backend/app/services/stages.py`
- `backend/tests/db/test_runtime_model_boundary.py`
- `backend/tests/api/test_approval_api.py`
- `backend/tests/projections/test_stage_inspector_projection.py`
- `backend/tests/projections/test_workspace_projection.py`
- `backend/tests/services/test_approval_commands.py`
- `backend/tests/services/test_approval_creation.py`
- `backend/tests/services/test_clarification_flow.py`
- `backend/tests/services/test_stage_run_store.py`
- `docs/plans/implementation/r3.6-stage-run-store.md`
- `docs/plans/acceleration/reports/AL01-R3.6.md`
- `docs/plans/function-one-platform/03-run-projection-and-feed.md`

This slice did not modify API routes, OpenAPI contracts, EventStore payload semantics, graph compilation logic, runtime engine wiring, migrations, frontend files, coordination store, central checkpoint snapshot, platform-plan final status, or split-plan final status.

## Source Trace

- Platform plan task: `docs/plans/function-one-platform-plan.md` lists `R3.6 | StageRun 持久化 | Week 4-5 | [ ] | 后端`.
- Split-plan task: `docs/plans/function-one-platform/03-run-projection-and-feed.md#r36` now explicitly requires `StageRun` creation/state progression plus persisted `graph_node_key` and `stage_contract_ref`, source-stage waiting semantics, six-stage type enforcement, no approval pseudo-stage creation, and runtime-log summaries with inherited `TraceContext`.
- Backend spec: `docs/specs/function-one-backend-engine-design-v1.md` requires `StageRun` to persist `graph_node_key`, `stage_contract_ref`, stage status, attempt index, start/end timestamps, and to keep approval/tool-confirmation waits on the source `StageRun` instead of creating extra business stages.
- Pre-implementation Source Trace Conflict Gate result: the original split-plan omitted spec-required `StageRun` fields. The worker session first revised `docs/plans/function-one-platform/03-run-projection-and-feed.md` to explicitly bring `graph_node_key` and `stage_contract_ref` into `R3.6` and to correct `backend/app/repositories/runtime.py` from `Create` to `Modify`, then implemented the aligned slice.

## TDD Evidence

Initial RED:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_stage_run_store.py -v
Exit code: 1
Key output: ImportError: cannot import name 'StageRunRepository' from 'backend.app.repositories.runtime'
```

Initial GREEN:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_stage_run_store.py -v
Exit code: 0
Key output: 11 passed in 1.18s
```

Regression RED after adding required `StageRun` fields:

```text
Command: uv run --no-sync python -m pytest backend/tests/api/test_approval_api.py backend/tests/services/test_approval_creation.py backend/tests/services/test_approval_commands.py backend/tests/services/test_clarification_flow.py backend/tests/projections/test_workspace_projection.py backend/tests/projections/test_stage_inspector_projection.py -q
Exit code: 1
Key output: NOT NULL constraint failed: stage_runs.graph_node_key
```

Code-quality repair RED:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_stage_run_store.py -v
Exit code: 1
Key output: 5 failed, 10 passed; failures reproduced non-current rewrite, terminal rewrite, raw graph_node_key leakage, and redaction failure propagation
```

Code-quality repair GREEN:

```text
Command: uv run --no-sync python -m pytest backend/tests/services/test_stage_run_store.py -v
Exit code: 0
Key output: 15 passed in 4.75s
```

## Review

Execution path: `superpowers:subagent-driven-development`.

Implementer and reviewer subagents:

- Implementer: `gpt-5.5`, reasoning `high`
- Spec compliance reviewer: `gpt-5.5`, reasoning `high`
- Code quality reviewer: `gpt-5.5`, reasoning `high`

Spec compliance review:

- PASS on first review.
- No Critical or Important findings.
- Confirmed the slice stayed within the revised `R3.6` boundary, persisted `graph_node_key` and `stage_contract_ref`, kept waits on source `StageRun`, and exposed exactly `start_stage()`, `mark_stage_waiting()`, and `complete_stage()`.

Code quality review:

- Initial review returned FAIL with two Important findings:
  - `StageRunService` reintroduced raw metadata after redaction and let redaction failures propagate outside the non-fatal log-write guard.
  - waiting/completion transitions could rewrite non-current or terminal historical `StageRun` rows.
- Repair narrowed the service behavior:
  - lifecycle logs now derive only from redacted payload output and keep the full log-preparation/write path non-fatal
  - waiting/completion both enforce `current active stage` and reject `terminal` row rewrites
  - focused tests now cover redaction safety, redaction failure isolation, non-current mutation rejection, and terminal rewrite rejection
- Re-review PASS with no remaining findings.

## Verification

Fresh main-session verification:

```text
Command: uv run --no-sync python -m pytest backend/tests/db/test_runtime_model_boundary.py backend/tests/services/test_stage_run_store.py -q
Exit code: 0
Key output: 20 passed in 5.88s
```

```text
Command: uv run --no-sync python -m pytest backend/tests/api/test_approval_api.py backend/tests/services/test_approval_creation.py backend/tests/services/test_approval_commands.py backend/tests/services/test_clarification_flow.py backend/tests/projections/test_workspace_projection.py backend/tests/projections/test_stage_inspector_projection.py -q
Exit code: 0
Key output: 41 passed in 34.10s
```

After these passing verification commands, only tracking docs were updated: this worker evidence report and the implementation plan. No code, tests, configuration, dependency manifests, lock files, API routes, runtime engine files, platform-plan statuses, split-plan statuses, acceleration checkpoint snapshot, or coordination store were modified afterward.

## Mock-First And Owner Conflict

- Mock-first status: not mock-first; implemented directly against the existing runtime model boundary, AL01 snapshot/compiler prerequisites, and current runtime-facing tests.
- Owner conflict: none observed. The code changes stay within AL01 ownership for `StageRun` / runtime truth.

## Remaining Risks

- `start_stage()` still creates the `StageRun` row before checking whether the owning `PipelineRun` exists. In normal service usage this should be called with an existing run and the session would be rolled back on error, but the ordering means callers must not suppress that failure and continue using the same unrolled-back session state.
- Approval-gated migration verification (`uv run --no-sync alembic -c backend/alembic.ini upgrade head`) was intentionally not run in this worker session because the repository rules require explicit user approval for migrations or migration-like commands.

## Commit Readiness

The local worker result is `reported`. After a user-approved checkpoint commit containing the implementation plan, code, tests, split-plan correction, and this evidence report, the main coordination session can ingest the claim as `implemented`.

No Git write action was performed.
