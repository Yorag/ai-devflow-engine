# AL04-A4.4 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL04-A4.4` |
| Lane | `AL04` |
| Task | `A4.4` |
| Branch | `feat/al-tools-deterministic-delivery` |
| Coordination Base | `73a54ad` |
| Evidence result | `reported` |
| Expected committed ingest result | `implemented` |
| Implementation plan | `docs/plans/implementation/a4.4-deterministic-terminal-control.md` |

Worker HEAD is not declared by this worker report. The main coordination session reads branch HEAD during ingest.

## Gate Summary

- Current branch: `feat/al-tools-deterministic-delivery`
- Active claim status at worker start: `claimed`
- Claim branch matched the current branch.
- Platform task status: A4.4 `[ ]`
- Split-plan task status: A4.4 `[ ]`
- Owner boundary respected: implementation stayed inside deterministic runtime, focused runtime tests, this implementation plan, and this worker evidence report.
- No coordination store, acceleration execution plan, platform-plan final status, or split-plan final status was updated.

## Scope Outcome

- Added configurable deterministic terminal control for completed, failed, and terminated paths.
- Configured failed and terminated paths run after the source `StageRun` and deterministic `StageArtifact` exist and before normal stage completion.
- Failed and terminated paths mark `PipelineRun`, current `StageRun`, and `Session` terminal.
- Failed and terminated paths append top-level `system_status` through existing `TerminalStatusProjector` with `retry:<run_id>` for the current run tail.
- Configured completion marks the run and session completed without `system_status`, `DeliveryRecord`, `demo_delivery`, or `delivery_result`.
- Direct `terminate()` rejects already terminal current stages, preserves waiting source thread status for the runtime boundary, and can create the deterministic source artifact for a non-terminal current stage before terminalizing.
- Runtime boundary terminate failure rolls back pending runtime, event, and control session mutations.
- Terminal source identity guards reject mismatched current stage or artifact inputs before mutation.
- Terminal runtime logs include direct failure point, source stage, graph thread, event refs, artifact refs, result ref, and retry clue.
- Runtime log write failure does not erase terminal domain facts.
- Preserved A4.4 scope boundaries:
  - no API/OpenAPI or frontend changes;
  - no DeliveryRecord, `demo_delivery`, `delivery_result`, `git_auto_delivery`, or Git delivery behavior;
  - no LangGraph runtime implementation;
  - no service/schema ownership changes;
  - no coordination store or central status updates.

## Changed Files

- `backend/app/runtime/deterministic.py`
- `backend/tests/runtime/test_deterministic_terminal_states.py`
- `docs/plans/implementation/a4.4-deterministic-terminal-control.md`
- `docs/plans/acceleration/reports/AL04-A4.4.md`

## TDD Evidence

Initial terminal RED:

```text
uv run pytest backend/tests/runtime/test_deterministic_terminal_states.py -v
Exit code: 1
Key output: AttributeError: 'DeterministicRuntimeEngine' object has no attribute 'configure_terminal_control'
```

Second terminal RED:

```text
uv run pytest backend/tests/runtime/test_deterministic_terminal_states.py -v
Exit code: 1
Key output: completed terminal exhaustion and direct terminate behavior were not implemented yet.
```

Spec-review fix RED:

```text
uv run pytest backend/tests/runtime/test_deterministic_terminal_states.py -v
Exit code: 1
Key output: direct terminate did not reject a completed current stage before runtime boundary calls.
```

Code-quality review-fix RED:

```text
uv run pytest backend/tests/runtime/test_deterministic_terminal_states.py -v
Exit code: 1
Key output: 6 failed, 5 passed; missing rollback after runtime terminate failure, source thread sent as RUNNING, direct terminate rejecting missing artifact, missing terminal source identity validation, and missing STAGE_STARTED refs were reproduced.
```

Final GREEN:

```text
uv run pytest backend/tests/runtime/test_deterministic_terminal_states.py -v
Exit code: 0
Key output: 11 passed in 2.56s
```

## Review Result

Spec compliance review:

- Initial review found one Important issue:
  - direct `terminate()` could rewrite a completed historical `StageRun` as terminated.
- Finding was verified, fixed, and re-reviewed.
- Final result: approved with no Critical or Important findings.

Code quality and test sufficiency review:

- Initial review found Important issues:
  - `terminate_thread` failure lacked rollback protection;
  - termination always sent a RUNNING thread status to the runtime boundary;
  - direct `terminate()` rejected a valid active stage with no deterministic artifact;
  - terminal helpers did not guard mismatched stage/artifact/current-run inputs.
- Minor issue fixed:
  - `STAGE_STARTED` event refs were missing from terminal result/log refs when terminal control starts a new stage in the same call.
- Findings were verified, fixed, and re-reviewed.
- Final result: approved with no Critical or Important findings.

## Verification

Focused terminal verification:

```text
uv run pytest backend/tests/runtime/test_deterministic_terminal_states.py -v
Exit code: 0
Key output: 11 passed in 2.56s
```

Impacted runtime and contract verification:

```text
uv run pytest backend/tests/runtime/test_deterministic_terminal_states.py backend/tests/runtime/test_deterministic_runtime.py backend/tests/runtime/test_deterministic_interrupts.py backend/tests/runtime/test_runtime_engine_contract.py -v
Exit code: 0
Key output: 45 passed in 7.42s
```

H4.6 terminate and rerun service regression:

```text
uv run pytest backend/tests/services/test_terminate_run.py backend/tests/services/test_rerun_command_projection.py -q
Exit code: 0
Key output: 25 passed in 5.02s
```

Full backend suite:

```text
uv run pytest -q
Exit code: 0
Key output: 1053 passed, 3 warnings in 294.13s (0:04:54)
Warnings: existing backend/tests/providers/test_langchain_adapter.py UserWarning about explicit temperature parameters passed through model_kwargs.
```

Additional static check attempted:

```text
uv run ruff check backend/app/runtime/deterministic.py backend/tests/runtime/test_deterministic_terminal_states.py
Exit code: 1
Key output: Failed to spawn: `ruff`; program not found in repo-local environment.
```

Post-full-suite tracking-only updates:

- After the final full backend suite passed, only `docs/plans/implementation/a4.4-deterministic-terminal-control.md` and this evidence report were updated with worker evidence.
- No code, test, configuration, dependency manifest, lockfile, or runtime-behavior artifact changed after the full suite.

## Owner Conflict Check

No owner conflict was introduced.

- AL04 deterministic runtime files were modified for the assigned slice.
- AL03 run lifecycle, terminate, rerun, and terminal projection services were consumed through existing public behavior, not modified.
- AL05 Provider/LangGraph runtime files were not modified.
- AL06 frontend files were not modified.
- Shared coordination store, central checkpoint snapshot, platform plan, and split-plan final statuses were not modified.

## Remaining Risk

- The deterministic runtime continues to use the repository's existing multi-session commit pattern; code-quality re-review noted no new A4.4-specific blocker from that pattern.
- `ruff` could not be run because it is not available in the repo-local environment.

## Commit Readiness

The local worker result is `reported`. After a user-approved checkpoint commit contains the implementation, focused tests, implementation plan, and this evidence report, the main coordination session can scan the branch HEAD and ingest this claim as `implemented`.
