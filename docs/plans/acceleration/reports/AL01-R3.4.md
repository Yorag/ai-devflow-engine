# AL01-R3.4 Worker Evidence Report

## Claim

- Claim: `AL01-R3.4`
- Lane: `AL01`
- Task: `R3.4`
- Coordination Base: `67f5290`
- Evidence report: `docs/plans/acceleration/reports/AL01-R3.4.md`
- Local result: `reported`
- Expected post-checkpoint ingest status: `implemented`
- Implementation plan: `docs/plans/implementation/r3.4-template-snapshot.md`

Worker HEAD is intentionally not declared here. The main coordination session reads the branch head during ingest.

## Scope

Implemented R3.4 template snapshot freezing and attach boundary only.

Changed files:
- `backend/app/domain/template_snapshot.py`
- `backend/app/services/runs.py`
- `backend/tests/services/test_template_snapshot.py`
- `docs/plans/implementation/r3.4-template-snapshot.md`
- `docs/plans/acceleration/reports/AL01-R3.4.md`

No API routes, migrations, Provider snapshots, model binding snapshots, runtime-limit snapshots, Provider call policy snapshots, GraphDefinition compiler, first-run startup transaction, coordination store, central checkpoint snapshot, platform-plan final status, or split-plan final status were modified.

## Dirty Status

`git status --short --untracked-files=all` before this evidence report was written:

```text
?? backend/app/domain/template_snapshot.py
?? backend/app/services/runs.py
?? backend/tests/services/test_template_snapshot.py
?? docs/plans/implementation/r3.4-template-snapshot.md
```

New-file line count snapshot before this evidence report was written:

```text
backend/app/domain/template_snapshot.py                  | 193 +
backend/app/services/runs.py                             | 35 +
backend/tests/services/test_template_snapshot.py          | 413 +
docs/plans/implementation/r3.4-template-snapshot.md       | 512 +
```

## TDD Evidence

Initial RED:

```text
Command: uv run python -m pytest backend/tests/services/test_template_snapshot.py -v
Exit code: 1
Key output: ModuleNotFoundError: No module named 'backend.app.domain.template_snapshot'
```

Initial GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_template_snapshot.py -v
Exit code: 0
Key output: 3 passed
```

Review hardening RED:

```text
Command: uv run python -m pytest backend/tests/services/test_template_snapshot.py -v
Exit code: 1
Key output: 4 failed, 3 passed; current code did not reject null Provider binding, string auto-regression flag, string retry count, or schema version override.
```

Second review hardening RED:

```text
Command: uv run python -m pytest backend/tests/services/test_template_snapshot.py -v
Exit code: 1
Key output: 2 failed, 7 passed; malformed binding rows raised raw TypeError/KeyError instead of intentional ValueError.
```

Final focused GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_template_snapshot.py -v
Exit code: 0
Key output: 9 passed in 1.90s
```

## Verification

Fresh main-session verification:

```text
Command: uv run python -m pytest backend/tests/services/test_template_snapshot.py -v
Exit code: 0
Key output: 9 passed in 1.90s
```

```text
Command: uv run python -m pytest backend/tests/services/test_template_snapshot.py backend/tests/services/test_template_seed.py backend/tests/services/test_user_template_service.py backend/tests/db/test_runtime_model_boundary.py -q
Exit code: 0
Key output: 35 passed in 3.27s
```

```text
Command: uv run python -m pytest backend/tests/domain/test_run_state_machine.py backend/tests/events/test_event_store.py backend/tests/services/test_template_snapshot.py -q
Exit code: 0
Key output: 35 passed in 2.41s
```

```text
Command: uv run python -m pytest -q
Exit code: 0
Key output: 365 passed in 49.10s
```

After full-suite verification, only the implementation plan and this worker evidence report were updated. No code, tests, configuration, dependency manifests, lock files, platform-plan statuses, split-plan statuses, acceleration execution checkpoint snapshot, or coordination store were modified after the full-suite run.

## Review

Execution path: `superpowers:subagent-driven-development`.

Implementer subagent:
- Model: `gpt-5.5`
- Reasoning effort: `xhigh`
- Result: `DONE`

Spec compliance review:
- Initial review found one Important issue: tests only verified the first stage's role/provider/prompt binding.
- Fix added full stage-slot binding freeze assertions.
- Re-review found no Critical, Important, or Minor spec compliance issues.

Code quality and regression review:
- Initial review found two Important issues: silent coercion of malformed JSON-backed template data, and attach coverage using only a transient `PipelineRunModel`.
- Fixes added strict scalar validation, malformed-row rejection, stable schema version coverage, exact snapshot key-set assertions, and persisted runtime-row attach coverage.
- Re-review found no Critical or Important issues. Two Minor hardening items were also fixed: malformed binding rows now raise intentional `ValueError`, and final attach assertion re-queries through a new runtime session.

No Critical or Important findings remain.

## Mock-First And Owner Conflict

- Mock-first status: not mock-first; implemented against existing control/runtime models and template service.
- Owner conflict: none observed. The files are within AL01 run/snapshot owner scope.

## Commit Readiness

The local worker result is `reported`. After a user-approved checkpoint commit containing the implementation plan, code, tests, and this evidence report, the main coordination session can ingest the claim as `implemented`.

No Git write action was performed.
