# AL06-F4.4 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL06-F4.4` |
| Lane | `AL06` |
| Task | `F4.4` |
| Coordination Base | `5cc45f2` |
| Evidence report | `docs/plans/acceleration/reports/AL06-F4.4.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |

Worker HEAD is intentionally not declared here; the main coordination session reads it during ingest.

## Scope

Assigned owner scope: frontend workspace store, SSE reducer, Feed, StageNode, Inspector, Composer, Approval, Tool Confirmation, Delivery UI.

Executed slice scope: `F4.4 重新尝试 UI 与历史审批禁用态` only.

This pass implemented:

- current-run-only rerun action inside top-level `system_status` entries
- explicit rerun warning that the new run restarts from `Requirement Analysis`
- explicit rerun warning that undelivered workspace changes are not inherited
- rerun command submission through the canonical frontend `createRerun()` API client
- query invalidation plus post-refresh focus to the new run boundary
- focusable run boundaries for post-rerun navigation
- terminal-current-run approval disabling without changing historical approval semantics
- mock rerun route support with terminal-state precondition guarding
- focused and full frontend regression coverage

It did not change backend contracts, tool-confirmation behavior, reducer semantics, coordination state, platform plan final status, split-plan final status, or central checkpoint snapshots.

## Source Trace

- Platform task: `docs/plans/function-one-platform-plan.md` lists `F4.4 | 重新尝试 UI 与历史审批禁用态 | Week 6 | [ ] | 前端`.
- Split task: `docs/plans/function-one-platform/04-human-loop-and-runtime.md#f44`.
- Frontend spec source: `docs/specs/frontend-workspace-global-design-v1.md`, especially the historical approval and rerun interaction sections.
- Product boundary source: `docs/specs/function-one-product-overview-v1.md`, especially the rule that rerun only belongs to the current failed or terminated run tail entry.
- Backend command source: `docs/specs/function-one-backend-engine-design-v1.md`, especially `POST /api/sessions/{sessionId}/runs`.
- Implementation plan: `docs/plans/implementation/f4.4-rerun-ui-historical-approval.md`.

## Changed Files

- Modified `docs/plans/implementation/f4.4-rerun-ui-historical-approval.md`
- Created `docs/plans/acceleration/reports/AL06-F4.4.md`
- Modified `frontend/src/features/approvals/ApprovalBlock.tsx`
- Modified `frontend/src/features/approvals/__tests__/ApprovalBlock.test.tsx`
- Modified `frontend/src/features/feed/FeedEntryRenderer.tsx`
- Modified `frontend/src/features/feed/NarrativeFeed.tsx`
- Modified `frontend/src/features/feed/RunBoundary.tsx`
- Modified `frontend/src/features/feed/__tests__/FeedEntryRenderer.test.tsx`
- Created `frontend/src/features/runs/RerunAction.tsx`
- Created `frontend/src/features/runs/__tests__/RerunAction.test.tsx`
- Modified `frontend/src/features/workspace/WorkspaceShell.tsx`
- Modified `frontend/src/features/workspace/__tests__/WorkspaceShell.test.tsx`
- Modified `frontend/src/mocks/handlers.ts`
- Modified `frontend/src/styles/global.css`

## TDD Evidence

Implementer RED commands:

```powershell
npm --prefix frontend run test -- --run src/features/runs/__tests__/RerunAction.test.tsx
npm --prefix frontend run test -- --run src/features/approvals/__tests__/ApprovalBlock.test.tsx
npm --prefix frontend run test -- --run src/features/feed/__tests__/FeedEntryRenderer.test.tsx
npm --prefix frontend run test -- --run src/features/workspace/__tests__/WorkspaceShell.test.tsx
```

Implementer RED result:

- `RerunAction.test.tsx`: failed because `RerunAction.tsx` did not exist yet.
- `ApprovalBlock.test.tsx`: failed because the new terminal-current-run guard did not yet disable `Approve`.
- `FeedEntryRenderer.test.tsx`: failed because historical rerender still showed `Retry run`.
- `WorkspaceShell.test.tsx`: failed before rerun workspace refresh and boundary-focus behavior were fully wired.

Implementer initial GREEN result:

- `RerunAction.test.tsx`: exit `0`, `1` file passed, `3` tests passed.
- `ApprovalBlock.test.tsx`: exit `0`, `1` file passed, `5` tests passed.
- `FeedEntryRenderer.test.tsx`: exit `0`, `1` file passed, `10` tests passed.
- `WorkspaceShell.test.tsx`: exit `0`, `1` file passed, `28` tests passed.

Review-driven TDD/regression loops:

- Spec finding RED: `npm --prefix frontend run test -- --run src/features/approvals/__tests__/ApprovalBlock.test.tsx`
  - exit `1`
  - key output: `1 failed | 5 passed`
  - failure: terminal current-run approval still left `Reject approval with reason` mounted
- Spec finding GREEN: same command
  - exit `0`
  - key output: `1 passed`, `6 passed`
- Mock rerun precondition RED: `npm --prefix frontend run test -- --run src/features/workspace/__tests__/WorkspaceShell.test.tsx`
  - exit `1`
  - key output: `1 failed | 28 passed`
  - failure: expected mock rerun precondition response `409`, received `200`
- Mock rerun precondition GREEN: same command
  - exit `0`
  - key output: `1 passed`, `29 passed`
- Build/type RED: `npm --prefix frontend run build`
  - exit `1`
  - key output: `Argument of type '"invalid_state"' is not assignable...`
- Build/type GREEN:
  - `npm --prefix frontend run test -- WorkspaceShell`: exit `0`, `29` tests passed
  - `npm --prefix frontend run test -- RerunAction`: exit `0`, `3` tests passed
  - `npm --prefix frontend run build`: exit `0`, `132` modules transformed, build succeeded

## Review

Spec / plan compliance review result:

- First pass found two Important issues:
  - terminal current-run approvals could leave an already-open reject form visible
  - mock rerun route allowed non-terminal rerun
- Fixes applied in-slice:
  - `ApprovalBlock` now hides `RejectReasonForm` once `canReject` becomes false
  - mock rerun route now requires both session and current run to be `failed` or `terminated`
- Spec re-review result: no Critical or Important findings remained

Code quality / testing / regression review result:

- First pass found one Important issue:
  - mock rerun precondition failure used undeclared error code `invalid_state`, which broke strict build
- Fixes applied in-slice:
  - rerun precondition failure now uses declared `validation_error` with explicit message and HTTP `409`
  - dead `isCurrentRun` state was removed from `RerunActionState`
- Code-quality re-review result: no Critical or Important findings remained

Residual minor notes only:

- mock sidebar session list remains static after rerun because project session list responses still come from static `mockSessionList`
- default `mockApiRequestOptions` is intentionally stateless and not suitable for rerun mutation persistence tests

## Verification

Fresh focused verification run in the main session:

```powershell
npm --prefix frontend run test -- RerunAction
npm --prefix frontend run test -- ApprovalBlock
npm --prefix frontend run test -- FeedEntryRenderer
npm --prefix frontend run test -- WorkspaceShell
npm --prefix frontend run test -- RunSwitcher
npm --prefix frontend run build
```

Exit codes: all `0`.

Key output:

```text
RerunAction: Test Files 1 passed, Tests 3 passed
ApprovalBlock: Test Files 1 passed, Tests 6 passed
FeedEntryRenderer: Test Files 1 passed, Tests 10 passed
WorkspaceShell: Test Files 1 passed, Tests 29 passed
RunSwitcher: Test Files 1 passed, Tests 11 passed
vite build: 132 modules transformed, built in 1.23s
```

Fresh full frontend verification run in the main session:

```powershell
npm --prefix frontend test
```

Exit code: `0`.

Key output:

```text
frontend test: Test Files 22 passed, Tests 183 passed
```

After the final full-suite verification passed, only this worker evidence report and `docs/plans/implementation/f4.4-rerun-ui-historical-approval.md` were updated. No production code, tests, config, manifest, or lockfile changed after the final verification pass.

## Mock-First Status

This slice is not mock-first. It consumes the existing backend-facing rerun API client and only adds local mock route support for frontend verification.

## Remaining Risks

- The terminal approval disabled copy is shared for terminal current-run states and currently says “terminated” even if a future snapshot supplies `failed` or `completed` in the same guard path; this is a wording precision issue, not a contract break.
- Default `mockApiRequestOptions` remains stateless by design, so any future rerun integration test must continue using a stable `createMockApiFetcher()` instance or a custom persistent fetcher.

## Commit Readiness

Commit approval can be requested for this reported local checkpoint. After a user-approved checkpoint commit, the main coordination session can ingest it as `implemented`.
