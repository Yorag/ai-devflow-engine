# AL05-A4.9 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | AL05-A4.9 |
| Lane | AL05 |
| Task | A4.9 |
| Coordination Base | 2451ebd |
| Worker HEAD | Filled by main coordination session during ingest |
| Evidence report | docs/plans/acceleration/reports/AL05-A4.9.md |
| Implementation plan | docs/plans/implementation/a4.9-langchain-provider-adapter.md |
| Local result | reported |
| Expected ingest result after user-approved checkpoint commit | implemented |

## Gate Summary

- Current branch: `feat/al-provider-langgraph-context`
- Active lane status: `claimed`
- Claim row: `AL05-A4.9` is `claimed`
- Claim branch: `feat/al-provider-langgraph-context`
- Claim Coordination Base: `2451ebd`
- Platform task status: A4.9 is not completed and remains eligible on this branch
- Split-plan task status: A4.9 `[ ]`
- Start gate: AL05 may immediately start provider adapter work after A4.8
- Owner scope honored: only `backend/app/providers/langchain_adapter.py`, `backend/app/providers/__init__.py`, focused adapter tests, implementation plan, and this evidence report were changed

## Execution Path

Execution path: `subagent-driven-development`

- User explicitly allowed subagents, and all subagents used `gpt-5.5`.
- One bounded implementer subagent executed the TDD loops.
- Separate `gpt-5.5` spec-compliance and code-quality reviewers were used for the two-stage review gate.
- A second static re-review pass was required after reviewer feedback on malformed OpenAI raw tool-call arguments; both final reviewers returned `pass`.
- Main agent retained slice selection, source-trace review, review sequencing, fresh verification, and evidence updates.

## Changed Files

- `backend/app/providers/langchain_adapter.py`
- `backend/app/providers/__init__.py`
- `backend/tests/providers/test_langchain_adapter.py`
- `docs/plans/implementation/a4.9-langchain-provider-adapter.md`
- `docs/plans/acceleration/reports/AL05-A4.9.md`

## Unchanged Central Files

- `docs/plans/function-one-acceleration-execution-plan.md`
- `docs/plans/function-one-platform-plan.md`
- `docs/plans/function-one-platform/06-langgraph-provider-context-stage-agent.md`
- Shared coordination store

## Implementation Summary

- Added a bounded `LangChainProviderAdapter` under `backend/app/providers/` for model construction, tool binding, structured invocation, normalized model-call metadata, and safe trace summaries.
- Added `ModelCallResult`, `ModelCallToolRequest`, `ModelCallUsage`, and `ModelCallTraceSummary` as strict adapter contracts, plus `raw_response_ref` and `invalid_tool_call_candidates` for later `model_call_trace` / A4.9c consumption.
- Implemented frozen-budget model creation with `ProviderCallPolicySnapshotRead` request timeout and `min(max_output_tokens, requested_max_output_tokens)` output budgeting.
- Resolved `env:` provider credential refs before real `ChatOpenAI` construction and returned a structured adapter error when the referenced env value is unavailable.
- Normalized LangChain tool calls, OpenAI-compatible raw tool calls, invalid tool-call candidates, usage metadata, native reasoning hashes, and provider failure metadata without widening into retry/circuit-breaker or decision-parser behavior.
- Hardened output summarization so unknown objects no longer leak `repr()` content into trace metadata.

## TDD Evidence

Initial RED:

```text
Command: uv run pytest backend/tests/providers/test_langchain_adapter.py -v
Exit code: 1
Key output: 5 failed, 11 passed
```

Initial RED failure themes:

- env credential refs were passed through as `env:...` instead of resolved before SDK use
- missing env credential did not raise a structured adapter error
- `ModelCallResult` lacked `raw_response_ref`
- invalid tool-call candidates were not surfaced
- malformed usage metadata still escaped normalization

Initial GREEN:

```text
Command: uv run pytest backend/tests/providers/test_langchain_adapter.py -v
Exit code: 0
Key output: 16 passed, 3 warnings
```

Review-fix RED:

```text
Command: uv run pytest backend/tests/providers/test_langchain_adapter.py -v
Exit code: 0
Key output: 16 passed, 3 warnings, but static review still blocked malformed OpenAI raw tool-call argument handling
```

Review-fix GREEN:

```text
Command: uv run pytest backend/tests/providers/test_langchain_adapter.py -v
Exit code: 0
Key output: 17 passed, 3 warnings
```

Final regression:

```text
Command: uv run pytest backend/tests/providers/test_langchain_adapter.py backend/tests/providers/test_provider_registry.py backend/tests/fixtures/test_fixture_contracts.py -v
Exit code: 0
Key output: 50 passed, 3 warnings
```

## Review Result

Initial spec / plan compliance review:

- Important finding fixed: `ModelCallResult` did not return an original response reference.
- Important finding fixed: OpenAI-compatible raw `function.arguments` were dropped instead of normalized.

Initial code-quality / regression review:

- Important finding fixed: `AIMessage.invalid_tool_calls` now surface as adapter metadata instead of disappearing.
- Important finding fixed: malformed usage metadata no longer crashes adapter normalization.
- Important finding fixed: fallback output summarization no longer uses arbitrary `repr()` values.
- Important finding fixed: focused tests now cover env credential resolution, raw response refs, OpenAI raw tool calls, invalid tool-call candidates, malformed usage metadata, and repr-leak prevention.

Final re-review status:

- Important finding fixed: malformed OpenAI raw tool-call arguments now surface as `invalid_tool_call_candidates` instead of empty executable tool requests.
- Spec / plan reviewer: `pass`
- Code-quality reviewer: `pass`
- No remaining Critical or Important findings

## Verification

Fresh focused verification run by main agent:

```powershell
uv run pytest backend/tests/providers/test_langchain_adapter.py -v
```

Result:

```text
Exit code: 0
17 passed, 3 warnings
```

Fresh final verification run by main agent:

```powershell
uv run pytest backend/tests/providers/test_langchain_adapter.py backend/tests/providers/test_provider_registry.py backend/tests/fixtures/test_fixture_contracts.py -v
```

Result:

```text
Exit code: 0
50 passed, 3 warnings
```

Warning note:

- `langchain-openai` warns that `temperature` is passed via `model_kwargs`; this is inherited from the frozen model-binding snapshot parameters and does not fail the focused suite.

After the final fresh verification, only `docs/plans/implementation/a4.9-langchain-provider-adapter.md` and `docs/plans/acceleration/reports/AL05-A4.9.md` were updated. No production code, tests, configuration, dependency manifests, lock files, coordination-store data, central checkpoint snapshot, platform-plan final status, or split-plan final status changed after verification.

## Remaining Risk

- `raw_response_ref` is a stable `sha256:` reference over safe summarized response content, not persisted raw payload storage. Later slices that persist `model_call_trace` may want a stronger stable-reference strategy, but this A4.9 slice now satisfies the reviewed response-reference boundary without widening into storage concerns.
- The adapter surfaces malformed raw tool-call payloads as candidates and safe error metadata only; classification and business handling remain intentionally deferred to A4.9c / A4.9d.

## Commit Readiness

This local worker result is `reported`.

The branch now contains a coherent, freshly verified A4.9 checkpoint. After a user-approved checkpoint commit including the code, tests, implementation plan, and this evidence report, the main coordination session can ingest this claim as `implemented`.

No Git write action, merge, next claim, central Claim Ledger update, platform-plan status update, split-plan final status update, or coordination-store write was performed.
