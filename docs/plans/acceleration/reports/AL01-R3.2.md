# AL01-R3.2 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL01-R3.2` |
| Lane | `AL01` |
| Task | `R3.2` |
| Coordination Base | `b56995f` |
| Evidence report | `docs/plans/acceleration/reports/AL01-R3.2.md` |
| Local result | `reported` |
| Implementation plan | `docs/plans/implementation/r3.2-start-first-run.md` |

Worker HEAD is intentionally not declared here. The main coordination session reads the branch head during ingest.

## Scope

Implemented the main first-run startup path for `POST /api/sessions/{sessionId}/messages` with `message_type = new_requirement`, including:

- request schema widening and route dispatch between `new_requirement` and `clarification_reply`
- first-run startup orchestration across control/runtime/event/graph stores
- first `PipelineRun`, `GraphDefinition`, `GraphThread`, `workspace_ref`, first `StageRun(requirement_analysis)`, and first `user_message` event creation
- startup trace/log/audit integration
- focused API/service coverage for first-run startup, stale-session reload, prompt-validation hook integration, and credential prefix propagation

Changed files:

- `backend/app/api/routes/sessions.py`
- `backend/app/repositories/runtime.py`
- `backend/app/schemas/session.py`
- `backend/app/services/runs.py`
- `backend/app/services/sessions.py`
- `backend/tests/api/test_clarification_reply_api.py`
- `backend/tests/api/test_session_message_api.py`
- `backend/tests/services/test_provider_call_policy_snapshot.py`
- `backend/tests/services/test_runtime_limit_snapshot.py`
- `backend/tests/services/test_start_first_run.py`
- `backend/app/repositories/graph.py`
- `docs/plans/implementation/r3.2-start-first-run.md`
- `docs/plans/acceleration/reports/AL01-R3.2.md`

This worker did not update the coordination store, checkpoint snapshot, platform-plan final task status, split-plan final task status, or any AL02-owned fixture files.

## Source Trace

- Platform plan task: `docs/plans/function-one-platform-plan.md` row `R3.2`
- Split-plan task: `docs/plans/function-one-platform/03-run-projection-and-feed.md#r32`
- Dependency trace: `C2.8`, `R3.4`, `R3.4a`, `R3.4b`, `R3.5`
- Backend spec trace: `docs/specs/function-one-backend-engine-design-v1.md` `POST /api/sessions/{sessionId}/messages` startup semantics and run snapshot requirements
- Product spec trace: `docs/specs/function-one-product-overview-v1.md` draft-only first requirement auto-start and no second independent chain in an existing Session

## Execution Path

Implementation used inline `superpowers:executing-plans`.

Reason:

- The user explicitly allowed subagents, but the implementation work touched tightly coupled control/runtime/event/graph startup orchestration and could not be safely decomposed into a bounded implementer write scope.
- `gpt-5.5` reviewer subagents were still used for read-only spec/code review checkpoints.

## TDD Evidence

Focused baseline before continuation review hardening:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py backend/tests/api/test_session_message_api.py backend/tests/api/test_clarification_reply_api.py -v
Exit code: 0
Key output: 12 passed
```

Review hardening RED 1:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py -v
Exit code: 1
Key output: 3 failed; PipelineRun.trace_id still matched the request trace_id, and rejected/failed startup logs were not persisted under JsonlLogWriter.
```

Review hardening GREEN 1:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py -v
Exit code: 0
Key output: 6 passed in 4.55s
```

Review hardening RED 2:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py -v
Exit code: 1
Key output: 3 failed; injected credential_env_prefixes were not propagated, and the run-start prompt-validation hook surface was missing.
```

Review hardening GREEN 2:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py -v
Exit code: 0
Key output: 9 passed in 5.78s
```

Prompt-validation mapping RED:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py::test_session_service_start_run_from_new_requirement_maps_default_prompt_validation_rejection_to_422 -v
Exit code: 1
Key output: default run-start prompt validation rejection surfaced as status_code 500 instead of 422.
```

Prompt-validation mapping GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py::test_session_service_start_run_from_new_requirement_maps_default_prompt_validation_rejection_to_422 -v
Exit code: 0
Key output: 1 passed in 0.42s
```

Simultaneous-startup claim RED:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py::test_session_service_start_run_from_new_requirement_rejects_true_simultaneous_startup -v
Exit code: 1
Key output: two simultaneous first-run requests could both pass the draft/null start gate; the second request did not fail with a stable conflict before product startup work began.
```

Simultaneous-startup claim GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py -v
Exit code: 0
Key output: 11 passed in 7.29s, including the new true-simultaneous startup regression proving the second request is rejected after the control-plane first-run claim is taken.
```

## Review

Reviewer subagents:

- Model: `gpt-5.5`
- Scope: read-only spec compliance and code-quality/regression review

Fixed during this continuation:

- fresh run `trace_id` generation with inherited `request_id` / `correlation_id`
- rejected/failed startup runtime log persistence before a run log file exists
- single run-start `system_prompt` validation hook surface with stable `422` mapping
- configured `credential_env_prefixes` propagation into Provider snapshot freezing
- control-plane first-run claim that rejects a second simultaneous `new_requirement` request with `409` before it can create a second startup artifact chain

Residual confirmed blockers:

- **Atomic multi-store startup is not achieved.** `RunLifecycleService._commit_all()` still commits runtime, control, event, and graph stores sequentially. A later commit failure can still leave partially committed startup product truth.

Residual non-blocking or inherited risks:

- Rejected/failed startup audit writes remain best-effort and follow the broader runtime-command failed-path pattern already used elsewhere in the codebase.
- The impacted regression failures in `backend/tests/projections/test_workspace_projection.py` and `backend/tests/api/test_query_api.py` remain external fixture/schema drift on this behind branch because those tests still seed `PlatformRuntimeSettings` without `internal_model_bindings`.
- Template snapshot persistence remains the existing `template_snapshot_ref` contract inherited from the R3.4/R3.5 baseline; this worker did not introduce a new template snapshot persistence layer.

## Verification

Fresh focused verification:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py -v
Exit code: 0
Key output: 11 passed in 7.29s
```

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py backend/tests/api/test_session_message_api.py backend/tests/api/test_clarification_reply_api.py -v
Exit code: 0
Key output: 19 passed in 19.44s
```

Impacted regression verification:

```text
Command: uv run python -m pytest backend/tests/services/test_start_first_run.py backend/tests/api/test_session_message_api.py backend/tests/api/test_clarification_reply_api.py backend/tests/services/test_session_service.py backend/tests/services/test_graph_compiler.py backend/tests/services/test_template_snapshot.py backend/tests/services/test_provider_model_binding_snapshots.py backend/tests/services/test_runtime_limit_snapshot.py backend/tests/services/test_provider_call_policy_snapshot.py backend/tests/events/test_event_store.py backend/tests/db/test_graph_model_boundary.py backend/tests/projections/test_workspace_projection.py backend/tests/api/test_query_api.py -q
Exit code: 1
Key output: 112 passed, 18 failed; all failures remained in workspace/query fixtures that seed PlatformRuntimeSettings without internal_model_bindings.
```

## Blocker Summary

Blocker 1:

- Issue: R3.2 requires startup to behave as one service transaction with no half-started run.
- Verified fact: `_commit_all()` still commits runtime, control, event, and graph stores sequentially; late commit failure can leave partial committed product truth.
- Suggested owner: `AL01` follow-up prerequisite slice, not an ad hoc extension of the current worker claim. Recommended new task: `R3.2a` for a reusable high-impact multi-store publication boundary.

External integration blocker:

- Issue: impacted regression still fails on this branch.
- Verified fact: all remaining failures are confined to `backend/tests/projections/test_workspace_projection.py` and `backend/tests/api/test_query_api.py`, where fixtures still omit required `internal_model_bindings`.
- Suggested owner: `AL02` / integration sync, not this worker branch.

## Commit Readiness

This worker is locally `reported`.

The original `R3.2` blocker on unresolved cross-store startup atomicity has been addressed in the same branch by `AL01-R3.2a`, which added the reusable first-run publication boundary now integrated into `RunLifecycleService.start_first_run()`.

This claim is commit-ready only as part of the combined checkpoint that includes:

- the `R3.2` business-semantic startup implementation
- the `R3.2a` publication-boundary infrastructure
- both implementation plans
- both worker evidence reports

No Git write action was performed.

## Task Definition Fix Proposal

Recommended authoritative correction in the main coordination session:

- Add a new AL01 prerequisite task before `R3.2`:
  - `R3.2a` `首个 run 启动多库可见性边界`
- Move the strong “single public semantic / no externally visible intermediate state” requirement to `R3.2a` as reusable infrastructure.
- Make `R3.2` explicitly depend on `R3.2a` and focus on first-run `new_requirement` business semantics.
- Reuse the same boundary later for `H4.4`, because code-review approval -> delivery snapshot -> runtime resume has the same consistency requirement.

Why this correction is needed:

- The backend spec fixes persistence as multi-SQLite by responsibility and does not require database-level distributed transactions as a baseline architecture.
- The same spec also requires `R3.2` and `H4.4` to expose a single public semantic with no observable intermediate state.
- The repository currently has no dedicated reusable consistency/publication boundary implementing that contract, so the current `R3.2` slice is carrying missing infrastructure prerequisites that should be planned explicitly.

Suggested AL01 queue correction:

- Current: `... -> R3.7 -> R3.2 -> R3.3 -> ...`
- Proposed: `... -> R3.7 -> R3.2a -> R3.2 -> R3.3 -> ...`

Suggested gate correction:

- `R3.2` final completion requires `R3.2a`.
- `H4.4` final completion should also require the same reusable boundary from `R3.2a`.

Suggested handling of the current branch:

- Keep this branch as the combined `R3.2` + `R3.2a` checkpoint base.
- Do not discard the implementation; it should become the execution base after the new prerequisite slice is planned and delivered.
- A draft implementation plan for the proposed prerequisite slice is now available at `docs/plans/implementation/r3.2a-first-run-publication-boundary.md`.
