# AL01-R3.5 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL01-R3.5` |
| Lane | `AL01` |
| Task | `R3.5` |
| Coordination Base | `be201ab` |
| Evidence report | `docs/plans/acceleration/reports/AL01-R3.5.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |
| Implementation plan | `docs/plans/implementation/r3.5-graph-definition-compiler.md` |

Worker HEAD is intentionally not declared here. The main coordination session reads the branch head during ingest.

## Scope

Implemented the pure `GraphDefinition` compiler/domain/test slice for `R3.5`.

Changed files:

- `backend/app/domain/graph_definition.py`
- `backend/app/services/graph_compiler.py`
- `backend/tests/services/test_graph_compiler.py`
- `docs/plans/implementation/r3.5-graph-definition-compiler.md`
- `docs/plans/acceleration/reports/AL01-R3.5.md`

This slice did not modify API routes, run-start transaction wiring, runtime orchestration, graph-db schema/models, migrations, frontend files, coordination store, central checkpoint snapshot, platform-plan final status, or split-plan final status.

## Source Trace

- Platform plan task: `docs/plans/function-one-platform-plan.md` lists `R3.5 | GraphDefinition 固定主链编译 | Week 4 | [ ] | 后端`.
- Split-plan task: `docs/plans/function-one-platform/03-run-projection-and-feed.md#r35` requires a fixed six-stage `GraphDefinition`, fixed approval interrupts, internal `solution_design` validation semantics, stable `source_node_group -> stage_type` mapping, stage contracts as the source of stage runtime semantics, runtime-limit provenance, and compile success/failure log-summary behavior.
- Backend spec: `docs/specs/function-one-backend-engine-design-v1.md` requires `GraphDefinition` to express the fixed six-stage main chain, keep `Solution Validation` internal to `solution_design`, use `GraphDefinition.stage_contracts` as the single source of truth for stage contracts, consume stage `allowed_tools` from those contracts, and use `CodeReviewArtifact.regression_decision` for review regression semantics.
- Dependency trace: the integration-branch doc convergence commit `8129c54` corrected the R3.2/R3.3 status drift and made R3.2 explicitly call R3.5's `GraphCompiler.compile()` instead of introducing a second graph-compilation truth source.

## TDD Evidence

Earlier worker GREEN state before this continuation:

```text
Command: uv run python -m pytest backend/tests/services/test_graph_compiler.py -v
Exit code: 0
Key output: 11 passed in 0.08s
```

```text
Command: uv run python -m pytest backend/tests/services/test_graph_compiler.py backend/tests/db/test_graph_model_boundary.py backend/tests/tools/test_tool_execution_gate.py backend/tests/runtime/test_runtime_engine_contract.py -q
Exit code: 0
Key output: 49 passed in 0.57s
```

Mutability hardening RED:

```text
Command: uv run python -m pytest backend/tests/services/test_graph_compiler.py -v
Exit code: 1
Key output: 1 failed, 10 passed; the new immutability regression proved FrozenDict still allowed in-place dict union via __ior__.
```

Mutability hardening GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_graph_compiler.py -v
Exit code: 0
Key output: 11 passed in 0.08s
```

Code-quality review fix RED:

```text
Command: uv run python -m pytest backend/tests/services/test_graph_compiler.py -v
Exit code: 1
Key output: 2 failed, 10 passed; failures reproduced the wrong code-review regression discriminator field and incomplete source_node_group_map coverage for referenced node/gate keys.
```

Code-quality review fix GREEN:

```text
Command: uv run python -m pytest backend/tests/services/test_graph_compiler.py -v
Exit code: 0
Key output: 12 passed in 0.08s
```

## Review

Execution path: `superpowers:subagent-driven-development`.

Implementer and reviewer subagents:

- Model: `gpt-5.4`
- Reasoning effort: `xhigh`

Spec compliance review:

- Earlier in the worker session, initial spec compliance review found two Important issues: `code_review` routed unconditionally to `code_generation`, and `stage_contracts` were too thin to remain the single source of truth.
- Those issues were fixed through TDD before this continuation.
- Re-review found no remaining Critical or Important spec compliance issues.

Code quality review:

- Earlier in the worker session, code quality review found three Important issues: shallow/deep immutability gaps, `log_summary_recorder` exceptions masking compile behavior, and retry drift between template and runtime-limit snapshot.
- Those issues were fixed through TDD before this continuation.
- The continuation code-quality review found two Important issues:
  - the compiled review-regression condition used `review_decision` instead of spec-required `regression_decision`
  - `source_node_group_map` omitted stable node/gate keys referenced by the compiled graph payload
- Fixes changed the condition field to `regression_decision`, added a direct regression-route test, and expanded `source_node_group_map` to cover `solution_design.authoring`, `solution_design.approval_gate`, `code_review.approval_gate`, and `delivery_complete` in addition to the existing stage/node-group keys.
- Final re-review found no Critical or Important findings.

## Verification

Fresh main-session verification:

```text
Command: uv run python -m pytest backend/tests/services/test_graph_compiler.py -v
Exit code: 0
Key output: 12 passed in 0.08s
```

```text
Command: uv run python -m pytest backend/tests/services/test_graph_compiler.py backend/tests/db/test_graph_model_boundary.py backend/tests/tools/test_tool_execution_gate.py backend/tests/runtime/test_runtime_engine_contract.py -q
Exit code: 0
Key output: 50 passed in 0.70s
```

After the passing verification commands, only this worker evidence report and the implementation plan were updated. No code, tests, configuration, dependency manifests, lock files, API routes, runtime orchestration files, graph-db models, platform-plan statuses, split-plan statuses, acceleration checkpoint snapshot, or coordination store were modified afterward.

## Mock-First And Owner Conflict

- Mock-first status: not mock-first; implemented against existing `TemplateSnapshot`, `RuntimeLimitSnapshot`, stage enums, graph model boundary tests, runtime contract tests, and tool gate tests.
- Owner conflict: none observed. The changed files stay within the AL01 graph compiler/domain/test boundary.

## Remaining Risks

- Run-start transaction wiring that calls `GraphCompiler.compile()` remains intentionally deferred to `R3.2`; this slice only defines and verifies the pure compiler/domain contract.
- Downstream runtime wiring that consumes the compiled stable node/gate keys remains outside this slice; the mapping contract is now explicit and tested, but end-to-end consumption will still need later integration verification.

## Commit Readiness

The local worker result is `reported`. After a user-approved checkpoint commit containing the implementation plan, code, tests, and this evidence report, the main coordination session can ingest the claim as `implemented`.

No Git write action was performed.
