# AL02-Q3.2a Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL02-Q3.2a` |
| Lane | `AL02` |
| Task | `Q3.2a` |
| Branch | `feat/al-projections-streams` |
| Coordination Base | `805a0c8` |
| Evidence report | `docs/plans/acceleration/reports/AL02-Q3.2a.md` |
| Implementation plan | `docs/plans/implementation/q3.2a-run-summary-query-api.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |

Worker HEAD is intentionally not declared here; the main coordination session reads the branch head during ingest.

## Scope

Executed slice scope: Q3.2a only.

Implemented `GET /api/runs/{runId}` as a read-only domain run summary query. The endpoint returns all `RunSummaryProjection` fields plus `current_stage_run_id`, reuses the existing AL02 publication-boundary visibility rules, validates that the stage id belongs to the target run before exposing it, and does not expose raw graph thread, checkpoint, workspace, LangGraph, runtime snapshot, or log internals.

Changed files for worker implementation:

- `backend/app/schemas/run.py`
- `backend/app/services/projections/timeline.py`
- `backend/app/api/routes/query.py`
- `backend/tests/projections/test_timeline_projection.py`
- `backend/tests/api/test_query_api.py`
- `docs/plans/implementation/q3.2a-run-summary-query-api.md`
- `docs/plans/acceleration/reports/AL02-Q3.2a.md`

The current worktree also contains these main-coordinator-injected static planning files, explicitly identified by the user before implementation:

- `docs/plans/function-one-acceleration-execution-plan.md`
- `docs/plans/function-one-platform-plan.md`
- `docs/plans/function-one-platform/03-run-projection-and-feed.md`

Those three files are treated as non-worker implementation scope. This worker did not change coordination store state, acceleration checkpoint snapshot, platform-plan final task status, split-plan final task status, specs, frontend files, migrations, dependency manifests, lock files, or environment files.

## Source Trace

- Platform task: `docs/plans/function-one-platform-plan.md` lists `Q3.2a | Run 状态摘要查询 API | Week 5 | [ ] | 后端`.
- Split task: `docs/plans/function-one-platform/03-run-projection-and-feed.md#q32a`.
- Backend spec: `docs/specs/function-one-backend-engine-design-v1.md` section 9 requires `GET /api/runs/{runId}` to return domain-level run status summary, not raw graph thread details, and to include all `RunSummaryProjection` fields plus `current_stage_run_id`.
- Existing Q3.2 dependency: `TimelineProjectionService` already owns run timeline visibility and current-stage projection helpers.

## Implementation Summary

- Added `RunStatusSummaryProjection` as a route-specific schema extending `RunSummaryProjection` with `current_stage_run_id`.
- Added `TimelineProjectionService.get_run_summary()`.
- Reused `PublicationBoundaryService.assert_run_visible(..., not_found_message="Run summary was not found.")`.
- Returned unified `404` for missing or hidden run context through the existing query API error path.
- Added stage ownership validation so a cross-run or missing `current_stage_run_id` is not exposed.
- Added `GET /api/runs/{runId}` in `backend/app/api/routes/query.py`.
- Extended projection, API, and OpenAPI tests.

## TDD Evidence

Projection RED:

```text
Command: uv run python -m pytest backend/tests/projections/test_timeline_projection.py -k "run_summary_projection" -v
Exit code: 1
Key output: 3 failed; AttributeError: 'TimelineProjectionService' object has no attribute 'get_run_summary'
```

Projection GREEN:

```text
Command: uv run python -m pytest backend/tests/projections/test_timeline_projection.py -k "run_summary_projection" -v
Exit code: 0
Key output: 3 passed, 11 deselected
```

API/OpenAPI RED:

```text
Command: uv run python -m pytest backend/tests/api/test_query_api.py -k "run_summary or query_workspace_route_is_documented_in_openapi" -v
Exit code: 1
Key output: 2 failed; assert 404 == 200; KeyError: '/api/runs/{runId}'
```

API/OpenAPI GREEN:

```text
Command: uv run python -m pytest backend/tests/api/test_query_api.py -k "run_summary or query_workspace_route_is_documented_in_openapi" -v
Exit code: 0
Key output: 2 passed, 17 deselected
```

Combined implementer GREEN:

```text
Command: uv run python -m pytest backend/tests/projections/test_timeline_projection.py backend/tests/api/test_query_api.py -v
Exit code: 0
Key output: 33 passed
```

## Review

Spec / plan compliance review:

- Initial status: `CHANGES_REQUESTED`.
- Important finding: three static plan files were present in the worktree diff outside implementer-owned files. Resolution: user explicitly identified them as main-coordinator-injected Q3.2a static plan补齐 files before implementation; this report records them as non-worker implementation scope.
- Important finding: closeout evidence was not yet written. Resolution: this report and the implementation plan execution notes now record evidence.
- No code-level compliance blocker was found in the five backend implementation files.

Code quality / testing / regression review:

- Status: `APPROVED`.
- Findings: no Critical, Important, or Minor issues.
- Residual risk noted by reviewer: the new summary tests cover missing/hidden session and invalid cross-run `current_stage_run_id`; they do not add a separate hidden-project or pending-publication test for this endpoint. The implementation uses `PublicationBoundaryService.assert_run_visible()`, and impacted publication-boundary verification passed.

## Verification

Fresh focused and impacted verification in the main session:

```text
Command: uv run python -m pytest backend/tests/projections/test_timeline_projection.py backend/tests/api/test_query_api.py -v
Exit code: 0
Key output: 33 passed in 15.17s
```

```text
Command: uv run python -m pytest backend/tests/api/test_startup_publication_visibility.py backend/tests/api/test_sse_stream.py -v
Exit code: 0
Key output: 10 passed in 6.31s
```

Fresh full backend verification in the main session:

```text
Command: uv run python -m pytest -q
Exit code: 0
Key output: 1283 passed, 3 warnings in 420.35s
Warnings: existing LangChain adapter tests warn that {'temperature'} should be specified explicitly instead of via model_kwargs.
```

After full backend verification, only `docs/plans/implementation/q3.2a-run-summary-query-api.md` and this evidence report were updated.

## Remaining Risks

- The lane branch was one integration closeout commit behind at planning time; that commit changed plan documents for AL06 delivery UI proofs and did not affect Q3.2a backend code or API contracts.
- This worker did not add a dedicated QA-V6.4 test file because that file is not present in this worktree; Q3.2a OpenAPI coverage was added to `backend/tests/api/test_query_api.py`, the existing query OpenAPI test surface.

## Commit Readiness

Suggested commit message:

```text
feat(query): add run summary projection route
```

After a user-approved checkpoint commit, the main coordination session can scan this branch and ingest `AL02-Q3.2a` as `implemented`.
