# AL05-A4.9b Worker Evidence Report

- Claim: `AL05-A4.9b`
- Lane: `AL05`
- Task: `A4.9b`
- Branch: `feat/al-provider-langgraph-context`
- Worktree: `C:\Users\lkw\Desktop\github\agent-project\ai-devflow-engine\.worktrees\feat-al-provider-langgraph-context`
- Coordination base: `c8f6776`
- Local result: `reported`
- Expected ingest result after user-approved checkpoint commit: `implemented`

## Main Coordination Decision

The main coordination session selected A4.9b owner-conflict handling option 2.

- `backend/app/services/artifacts.py` was not modified.
- No `ArtifactStore.append_compressed_context_block()` helper was added.
- Compressed context persistence uses existing `ArtifactStore.append_process_record(process_key="compressed_context_block", ...)`.
- Context-compression model-call trace persistence uses existing `ArtifactStore.append_process_record(process_key="context_compression_model_call_trace", ...)`.
- Both compression process records are list-preserving so repeated compression attempts on the same `StageArtifact` do not overwrite earlier evidence.

## Scope

Implemented A4.9b context size guarding, observation budgeting, sliding working windows, and context-compression process records for A4.9a context envelopes.

Changed implementation files:

- `backend/app/context/size_guard.py`
- `backend/app/context/compression.py`
- `backend/app/context/builder.py`
- `backend/app/context/__init__.py`
- `backend/app/prompts/renderer.py`

Changed test files:

- `backend/tests/context/test_context_size_guard.py`
- `backend/tests/context/test_context_compression.py`

Tracking file:

- `docs/plans/implementation/a4.9b-context-size-guard.md`

## Implementation Summary

- Added deterministic context token estimation and threshold decisions from frozen Provider/model capability snapshots and frozen `RuntimeLimitSnapshotRead.context_limits`.
- Added observation budgeting for large bash stdout/stderr, tool output, tool errors, grep results, test output, diffs, remote delivery results, file reads, and model output process previews while preserving full refs.
- Added sliding-window indexing for older same-section process blocks with stable `context-index://...` refs.
- Added `ContextCompressionRunner` and `CompressedContextBlock` contracts using the built-in `compression_prompt` and `ModelCallType.CONTEXT_COMPRESSION`.
- Wired `ContextEnvelopeBuilder` to apply budgets before render, invoke compression when required, replace compressible sections with a compressed reference block, and re-check the final envelope size.
- Kept stage/run failed-state mutation, API routes, projections, runtime loop, frontend, platform plan status, split-plan status, and coordination store updates out of scope.

## TDD Evidence

- `uv run pytest backend/tests/context/test_context_size_guard.py -v`
  - RED: missing `backend.app.context.size_guard`.
  - GREEN: size guard tests passed.
- `uv run pytest backend/tests/context/test_context_compression.py -v`
  - RED: missing compression renderer/runner integration and required builder request fields.
  - GREEN: compression renderer, runner, and builder integration tests passed.
- Reviewer-driven red tests:
  - `uv run pytest backend/tests/context/test_context_size_guard.py backend/tests/context/test_context_compression.py -v`
  - RED: 6 expected failures covering time-dependent sliding-window refs, missing pinned `specified_action`, undercounted token estimates, zero trigger threshold, repeated compression overwrite, and missing post-compression size recheck.
  - GREEN: same command passed with `26 passed`.

## Review Evidence

- Spec / plan compliance review after initial fixes:
  - Result: no Critical or Important findings.
  - Minor: implementation plan still documented `model_call_trace` instead of `context_compression_model_call_trace`.
- Code-quality / testing / regression review:
  - Result: Important findings for stable sliding-window refs, post-compression size recheck and pinned `specified_action`, repeated compression overwrite risk, and conservative token estimation.
  - Fixes: removed `built_at` from index digest, pinned `specified_action`, re-ran size guard after compression, made compression records list-preserving, used visible-summary token estimate as a lower bound, and clamped trigger thresholds to at least `1`.
- Re-review after fixes:
  - Spec / plan compliance: no Critical or Important findings.
  - Code quality / testing / regression: no Critical or Important findings.
  - Remaining minor risk: missing `compression_prompt` in a custom registry still maps to `unsupported_model_call_type`; existing prompt renderer tests cover this compatibility behavior.

## Verification

- `uv run pytest backend/tests/context/test_context_size_guard.py backend/tests/context/test_context_compression.py -v`
  - Exit code: `0`
  - Result: `26 passed`
- `uv run pytest backend/tests/context/test_context_size_guard.py backend/tests/context/test_context_compression.py backend/tests/context/test_context_envelope_builder.py backend/tests/context/test_context_schemas.py backend/tests/prompts/test_prompt_renderer.py backend/tests/prompts/test_prompt_renderer_manifest_metadata.py backend/tests/providers/test_langchain_adapter.py backend/tests/services/test_artifact_store.py -v`
  - Exit code: `0`
  - Result: `97 passed, 3 warnings`
  - Warning detail: existing LangChain warning that `temperature` should be passed explicitly rather than through `model_kwargs`.

## Risks And Notes

- This worker branch did not update the shared coordination store, acceleration execution plan checkpoint snapshot, platform plan status, split-plan status, `backend/app/services/artifacts.py`, API routes, projections, frontend, or runtime loop.
- `ruff` was not used as a normal verification path because this slice's declared verification path is pytest and no new lint dependency was installed.
