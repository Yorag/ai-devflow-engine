# AL04-W5.6 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | AL04-W5.6 |
| Lane | AL04 |
| Task | W5.6 |
| Coordination Base | ea1aa8d |
| Worker HEAD | Filled by main coordination session during ingest |
| Evidence report | docs/plans/acceleration/reports/AL04-W5.6.md |
| Local result | reported |
| Expected ingest result after user-approved checkpoint commit | implemented |

## Gate Summary

- Current branch: `feat/al-tools-deterministic-delivery`
- Active claim status: `claimed`
- Claim branch: `feat/al-tools-deterministic-delivery`
- Platform task status: W5.6 `[ ]`
- Split-plan task status: W5.6 `[ ]`
- Owner boundary respected: implementation stayed inside `backend/app/schemas/preview.py`, `backend/app/api/routes/preview_targets.py`, `backend/tests/api/test_preview_target_api.py`, and the minimal router registration change in `backend/app/api/router.py`, with local tracking updates only in this report and `docs/plans/implementation/w5.6-preview-target-query.md`

## Scope Outcome

- Implemented the W5.6 V1 preview-target public schema in `backend/app/schemas/preview.py`:
  - `PreviewTargetReference`
  - `PreviewTarget`
- Implemented the read-only preview-target query route in `backend/app/api/routes/preview_targets.py`:
  - `PreviewTargetServiceError`
  - `PreviewTargetService.get_preview_target()`
  - `GET /api/preview-targets/{previewTargetId}`
- Reused existing runtime truth and visibility boundaries:
  - preview-target records are read from `StageArtifactModel`
  - lookup is limited to `artifact_type == "preview_target"` and `artifact_id == preview_target_id`
  - `PublicationBoundaryService.assert_run_visible()` remains the authority for hidden/unpublished run filtering
- Added stable response fields required by W5.6:
  - `preview_target_id`
  - `project_id`
  - `run_id`
  - `stage_run_id`
  - `target_type`
  - `reference`
  - `created_at`
- Added stable structured reference payload fields:
  - `reference_id`
  - `reference_kind`
  - `source_ref`
  - `source_label`
  - optional `path`
  - optional `version_ref`
  - optional `metadata`
- Added focused API tests in `backend/tests/api/test_preview_target_api.py` covering:
  - success response shape
  - unified `404` for missing preview target
  - unified `404` for unpublished startup run visibility boundary
  - unified `500` for malformed reference payload
  - unified `500` for malformed top-level `process`
  - `/api/openapi.json` path/method and response schema registration
- Registered the route in `backend/app/api/router.py` with the smallest possible change
- Did not implement preview launch, preview hot update, new DB tables, migrations, producer flows, frontend changes, Inspector integration, Timeline integration, or plan/coordinator state updates

## TDD And Review Evidence

- Initial RED:
  - `uv run pytest backend/tests/api/test_preview_target_api.py -v`
  - expected failure before implementation because the preview-target route and schema were not registered
- Initial GREEN from first implementation pass:
  - `uv run pytest backend/tests/api/test_preview_target_api.py -v`
  - result: `4 passed`
  - `uv run pytest backend/tests/api/test_preview_target_api.py backend/tests/api/test_query_api.py -v`
  - result: `21 passed`
- Review sequence:
  - spec / plan compliance review approved with no Critical or Important findings
  - code quality / testing review found 1 Important issue:
    - non-dict `artifact.process` could raise `AttributeError` and bypass the intended unified `500` response
  - the finding was fixed inside W5.6 scope with follow-up TDD regression coverage
- Follow-up RED after review finding:
  - `uv run pytest backend/tests/api/test_preview_target_api.py -v`
  - expected failure because malformed top-level `process` still bypassed unified error handling before the fix
- Final GREEN after review fix:
  - `uv run pytest backend/tests/api/test_preview_target_api.py -v`
  - result: `5 passed`
  - `uv run pytest backend/tests/api/test_preview_target_api.py backend/tests/api/test_query_api.py -v`
  - result: `22 passed`
  - `uv run pytest backend/tests -q`
  - result: `1003 passed, 3 warnings in 207.23s (0:03:27)`
- Final review status:
  - spec / plan compliance review: approved with no Critical or Important findings
  - code quality / testing re-review: approved with no remaining Critical or Important findings

## Verification

Focused verification:

```powershell
uv run pytest backend/tests/api/test_preview_target_api.py -v
```

Result:

- `5 passed`

Impacted regressions:

```powershell
uv run pytest backend/tests/api/test_preview_target_api.py backend/tests/api/test_query_api.py -v
```

Result:

- `22 passed`

Full backend suite:

```powershell
uv run pytest backend/tests -q
```

Result:

- `1003 passed, 3 warnings in 207.23s (0:03:27)`
- warnings came from existing `backend/tests/providers/test_langchain_adapter.py` about explicit `temperature` parameters being passed via `model_kwargs`

Post-full-suite tracking-only updates:

- After the final full suite passed, only `docs/plans/implementation/w5.6-preview-target-query.md` and this evidence report were updated.
- No code, test, config, manifest, lockfile, or generated runtime-behavior artifact changed after the final full verification, so no additional rerun is required by workflow rules.

## Files Changed

- `backend/app/schemas/preview.py`
- `backend/app/api/routes/preview_targets.py`
- `backend/tests/api/test_preview_target_api.py`
- `backend/app/api/router.py`
- `docs/plans/implementation/w5.6-preview-target-query.md`
- `docs/plans/acceleration/reports/AL04-W5.6.md`

## Remaining Risk

- No Critical or Important review finding remains in W5.6 scope.
- W5.6 intentionally relies on `StageArtifactModel.process["target_type"]` and `process["reference"]` as the stored producer-side contract. When future slices start writing preview-target artifacts, they must keep that shape stable or update this reader and its tests in lockstep.
- W5.6 intentionally stops at the read-only query boundary; preview-target production, preview execution, and live update behavior remain future slices.

## Commit Readiness

- Recommended for checkpoint commit approval
- Reason:
  - W5.6 implementation, focused/impacted regressions, and fresh full backend suite verification are green
  - the diff remains one coherent AL04 W5.6 slice plus allowed local tracking docs
  - after a user-approved checkpoint commit, main coordination can ingest this claim as `implemented`
