# AL05-A4.9c Worker Evidence Report

- Claim: `AL05-A4.9c`
- Lane: `AL05`
- Task: `A4.9c`
- Branch: `feat/al-provider-langgraph-context`
- Worktree: `C:\Users\lkw\Desktop\github\agent-project\ai-devflow-engine\.worktrees\feat-al-provider-langgraph-context`
- Coordination base: `43f7c8e`
- Implementation plan: `docs/plans/implementation/a4.9c-agent-decision-parser.md`
- Local result: `reported`
- Expected ingest result after user-approved checkpoint commit: `implemented`

## Execution Path

Execution path: `subagent-driven-development`

- User explicitly allowed subagents, and all subagents used `gpt-5.5` with `xhigh` reasoning.
- A bounded implementer subagent owned only `backend/app/runtime/agent_decision.py` and `backend/tests/runtime/test_agent_decision_parser.py`.
- Separate read-only reviewers performed spec/plan compliance review and code-quality/testing/regression review.
- Main agent retained claim validation, Source Trace gate, review-fix integration, final fresh verification, Git gate, implementation plan updates, and this evidence report.

## Changed Files

Implementation files:

- `backend/app/runtime/agent_decision.py`

Test files:

- `backend/tests/runtime/test_agent_decision_parser.py`

Tracking files:

- `docs/plans/implementation/a4.9c-agent-decision-parser.md`
- `docs/plans/acceleration/reports/AL05-A4.9c.md`

## Unchanged Central Files

- Shared coordination store
- `docs/plans/function-one-acceleration-execution-plan.md`
- `docs/plans/function-one-platform-plan.md`
- `docs/plans/function-one-platform/06-langgraph-provider-context-stage-agent.md`
- Provider adapter, context builder, ToolRegistry, services, API routes, DB models, frontend files, dependency manifests, lock files, migrations, and environment files

## Implementation Summary

- Added strict `AgentDecision` schema objects for native tool calls, tool confirmations, stage artifact submissions, clarification requests, structured repair, retry plans, and stage failure.
- Added `AgentDecisionParser.parse_model_result()` to consume normalized `ModelCallResult` structured outputs and native tool-call requests only.
- Enforced native tool/function calling for `request_tool_call`; structured `request_tool_call` payloads are rejected.
- Validated native tool calls against `ContextEnvelope.available_tools`, current stage `allowed_tools`, schema version, and `ToolInputSchemaValidator`.
- Validated tool confirmation payloads without creating confirmations or executing tools.
- Validated stage artifact submissions against artifact type, evidence refs, optional JSON-schema `output_contract`, and known current-stage artifact required fields for string contracts.
- Added trace-ready accepted and rejected decision metadata with deterministic trace refs and bounded safe error details.

## TDD Evidence

Initial RED:

```text
Command: uv run pytest backend/tests/runtime/test_agent_decision_parser.py -v
Exit code: 1
Key output: 10 failures due to ModuleNotFoundError: No module named 'backend.app.runtime.agent_decision'
```

Initial GREEN:

```text
Command: uv run pytest backend/tests/runtime/test_agent_decision_parser.py -v
Exit code: 0
Key output: 10 passed in 0.16s
```

Reviewer-fix RED/GREEN:

```text
Command: uv run pytest backend/tests/runtime/test_agent_decision_parser.py -v
RED exit code: 1
Key output: failures for structured request_tool_call acceptance, missing stage artifact output fields, missing alternative-path judgment, blank ref/list items, schema fallback precedence, and unbounded safe_details.
GREEN exit code: 0
Key output: 19 passed in 0.16s
```

## Review Evidence

Spec / plan compliance review:

- Initial Important findings:
  - Structured `request_tool_call` was accepted instead of requiring native tool calls.
  - Stage artifact validation only checked type and did not validate output-contract payload fields.
- Fixes:
  - Rejected structured `request_tool_call` with `invalid_structured_output`.
  - Added known artifact required-field validation and JSON-schema `output_contract` validation.
  - Required `request_tool_confirmation.alternative_path_summary`.
- Re-review result: no Critical or Important findings remain.

Code-quality / testing / regression review:

- Initial Important findings:
  - Parser error `safe_details` were not consistently bounded.
  - Explicit JSON-schema `output_contract` validation still fell through to known-artifact fallback validation.
- Fixes:
  - Centralized safe-detail bounding in parser error factories.
  - Made mapping `output_contract` validation authoritative.
  - Added available-but-disallowed tool coverage, `risk_summary=None` coverage, explicit JSON-schema violation coverage, and schema-overrides-fallback coverage.
- Re-review result: no Critical, Important, or Minor findings remain.

## Verification

Fresh focused verification:

```powershell
uv run pytest backend/tests/runtime/test_agent_decision_parser.py -v
```

Result:

```text
Exit code: 0
19 passed in 0.16s
```

Fresh final verification:

```powershell
uv run pytest backend/tests/runtime/test_agent_decision_parser.py backend/tests/providers/test_langchain_adapter.py backend/tests/context/test_context_schemas.py -v
```

Result:

```text
Exit code: 0
51 passed, 3 warnings in 1.52s
```

Warning detail: existing LangChain adapter warnings that `temperature` should be specified explicitly instead of passed through `model_kwargs`.

## Remaining Risk

- Known-artifact required-field validation is a local fallback for current string `output_contract` stage contracts. When future stages provide explicit JSON schema mappings, the explicit schema path is authoritative and the fallback is skipped.
- This slice intentionally does not persist `StageArtifact.process`, create `ToolConfirmationRequest`, create `ClarificationRecord`, mutate run/stage state, or route Stage Agent iterations; those remain A4.9d and later responsibilities.

## Commit Readiness

The local worker result is `reported`.

After a user-approved checkpoint commit containing the implementation plan, parser code, tests, and this evidence report, the main coordination session can ingest this claim as `implemented`.

No Git merge, PR, push, central Claim Ledger update, platform-plan status update, split-plan final status update, acceleration checkpoint snapshot update, or coordination-store write was performed.
