# AL06-F4.2 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL06-F4.2` |
| Lane | `AL06` |
| Task | `F4.2` |
| Coordination Base | `147563b` |
| Evidence report | `docs/plans/acceleration/reports/AL06-F4.2.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |

Worker HEAD is intentionally not declared here; the main coordination session reads it during ingest.

## Scope

Assigned owner scope: frontend workspace store, SSE reducer, Feed, StageNode, Inspector, Composer, Approval, Tool Confirmation, Delivery UI.

Executed slice scope: `F4.2 Run 控制按钮与终止入口` only.

This pass implemented pause, resume, and terminate frontend control entry points without changing backend contracts, workspace projection types, reducer semantics, SSE event contracts, manifests, lock files, platform plan final status, split-plan final status, or the coordination store.

## Source Trace

- Platform task: `docs/plans/function-one-platform-plan.md` lists `F4.2 | Run 控制按钮与终止入口 | Week 6 | [ ] | 前端`.
- Split task: `docs/plans/function-one-platform/04-human-loop-and-runtime.md#f42`.
- Frontend interaction source: `docs/specs/frontend-workspace-global-design-v1.md` Composer and runtime-control sections, especially the rules that:
  - `waiting_clarification` keeps main Composer action `发送`
  - `waiting_clarification` still requires a secondary `暂停` entry for the current active run
  - `paused` exposes `恢复`
  - terminate belongs in the workspace top-right tool area and only targets the current active run
- Implementation plan: `docs/plans/implementation/f4.2-run-control-buttons.md`.

## Changed Files

- Created `docs/plans/implementation/f4.2-run-control-buttons.md`
- Created `frontend/src/features/composer/RunControlButtons.tsx`
- Created `frontend/src/features/composer/__tests__/RunControlButtons.test.tsx`
- Created `frontend/src/features/runs/TerminateRunAction.tsx`
- Modified `frontend/src/features/composer/Composer.tsx`
- Modified `frontend/src/features/composer/__tests__/Composer.test.tsx`
- Modified `frontend/src/features/workspace/WorkspaceShell.tsx`
- Modified `frontend/src/features/workspace/__tests__/WorkspaceShell.test.tsx`
- Modified `frontend/src/mocks/fixtures.ts`
- Modified `frontend/src/styles/global.css`
- Created `docs/plans/acceleration/reports/AL06-F4.2.md`

## TDD Evidence

Implementer initial RED commands:

```powershell
npm --prefix frontend run test -- --run src/features/composer/__tests__/RunControlButtons.test.tsx
npm --prefix frontend run test -- --run src/features/workspace/__tests__/WorkspaceShell.test.tsx
```

Implementer initial RED result:

- `RunControlButtons.test.tsx`: failed because the new run-control component did not exist yet and main lifecycle actions were not executable.
- `WorkspaceShell.test.tsx`: failed because the terminate toolbar action did not exist yet.

Implementer GREEN commands:

```powershell
npm --prefix frontend run test -- RunControlButtons
npm --prefix frontend run test -- WorkspaceShell
npm --prefix frontend run build
```

Implementer GREEN result:

- `RunControlButtons`: pass
- `WorkspaceShell`: pass
- `build`: pass

Main-session correction / regression command:

```powershell
npm --prefix frontend run test -- WorkspaceShell
```

Purpose:

- confirm the `waiting_clarification` workspace path now shows both `发送` and `暂停当前运行` after correcting mock workspace projection semantics

Main-session reviewer-fix regression command:

```powershell
npm --prefix frontend run test -- RunControlButtons
```

Purpose:

- prove secondary pause disables the main send button while in flight and that pause / resume invalidation includes the project session list

## Review

Spec / plan compliance review result:

- Important finding fixed: `frontend/src/mocks/fixtures.ts` still projected `waiting_clarification` `secondary_actions` as `["terminate"]`, which suppressed the required secondary pause entry in initial workspace snapshots.
- Fix applied: `waiting_clarification` mock snapshots now project `["pause", "terminate"]`, and workspace integration tests assert the page shows both `发送` and `暂停当前运行`.
- Re-review result: no remaining Critical or Important spec / plan findings.

Code quality / testing / regression review result:

- Important finding fixed: action busy state was split across Composer, secondary pause, and terminate controls, allowing conflicting concurrent mutations.
- Important finding fixed: pause / resume / terminate invalidated only `sessionWorkspace`, leaving `projectSessions` stale.
- Additional hardening applied while inside scope:
  - terminate visibility now respects projected `secondary_actions`
  - Composer input accessible name now matches the visible label `当前输入`
  - shared workspace test cleanup now restores stubbed globals with `vi.unstubAllGlobals()`
- Re-review result after fixes: no remaining Critical or Important findings in the implemented slice.

## Verification

Final fresh verification run in the main session:

```powershell
npm --prefix frontend run test -- RunControlButtons
npm --prefix frontend run test -- WorkspaceShell
npm --prefix frontend run build
```

Exit codes: all `0`.

Key output:

```text
RunControlButtons: Test Files 1 passed, Tests 7 passed
WorkspaceShell: Test Files 1 passed, Tests 23 passed
vite build: 125 modules transformed
✓ built in 1.01s
```

After the final verification commands, only this worker evidence report and the implementation plan execution record were updated. No code, test, configuration, manifest, or lockfile changed after the final verification pass.

## Mock-First Status

This slice is not mock-first. It uses existing run lifecycle APIs and existing workspace projection semantics.

## Remaining Risks

- The focused verification covers the claimed slice but not the full frontend suite.
- Busy-state coordination is now shared across Composer and toolbar actions inside the workspace shell, but any future run-control surface outside `WorkspaceShell` will need to use the same coordination pattern to avoid reintroducing concurrent mutations.

## Commit Readiness

Commit approval can be requested for this reported local checkpoint. After a user-approved checkpoint commit, the main coordination session can ingest it as `implemented`.
