# AL04-A4.3 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL04-A4.3` |
| Lane | `AL04` |
| Task | `A4.3` |
| Branch | `feat/al-tools-deterministic-delivery` |
| Coordination Base | `87a0ded` |
| Evidence result | `reported` |
| Expected committed ingest result | `implemented` |
| Implementation plan | `docs/plans/implementation/a4.3-deterministic-interrupts.md` |

Worker HEAD is not declared by this worker report. The main coordination session reads branch HEAD during ingest.

## Gate Summary

- Current branch: `feat/al-tools-deterministic-delivery`
- Active claim status at worker start: `claimed`
- Claim branch matched the current branch.
- Platform task status: A4.3 `[ ]`
- Split-plan task status: A4.3 `[ ]`
- Owner boundary respected: implementation stayed inside deterministic runtime, focused runtime tests, this implementation plan, and this worker evidence report.
- No coordination store, acceleration execution plan, platform-plan final status, or split-plan final status was updated.

## Scope Outcome

- Added configurable deterministic interrupt controls for clarification, solution-design approval, code-review approval, and high-risk tool confirmation.
- Delegated durable human-loop objects to existing AL03 services:
  - `ClarificationService`
  - `ApprovalService`
  - `ToolConfirmationService`
- Used the A4.0 runtime boundary through `RuntimeOrchestrationService` for interrupt creation and resume.
- Preserved the runtime boundary's returned `GraphInterruptRef` and checkpoint refs when available.
- Added deterministic resume support through `resume()` and `resume_from_interrupt()`.
- Kept resumed stages in the same run and graph thread and avoided exposing raw graph state.
- Reused a resumed stage's existing deterministic artifact before advancing, preventing duplicate artifact insertion and interrupt re-emission.
- Filtered interrupt event refs by session, run, stage run, and event type to avoid stale same-type refs.
- Logged resume failures while rolling back runtime/control/event sessions so waiting state remains intact.
- Preserved A4.3 scope boundaries:
  - no API/OpenAPI or frontend changes;
  - no terminal success, failure, pause, resume, or termination control from A4.4;
  - no DeliveryRecord, `demo_delivery`, or `git_auto_delivery`;
  - no LangGraph runtime implementation;
  - no AL03 service or schema changes;
  - no coordination store or central status updates.

## Changed Files

- `backend/app/runtime/deterministic.py`
- `backend/tests/runtime/test_deterministic_interrupts.py`
- `docs/plans/implementation/a4.3-deterministic-interrupts.md`
- `docs/plans/acceleration/reports/AL04-A4.3.md`

## TDD Evidence

Initial interrupt RED:

```text
uv run pytest backend/tests/runtime/test_deterministic_interrupts.py -v
Exit code: 1
Key output: AttributeError: 'DeterministicRuntimeEngine' object has no attribute 'configure_interrupts'
```

Initial interrupt GREEN:

```text
uv run pytest backend/tests/runtime/test_deterministic_interrupts.py -v
Exit code: 0
Key output: 10 passed
```

Review-fix RED:

```text
uv run pytest backend/tests/runtime/test_deterministic_interrupts.py -v
Exit code: 1
Key output: 4 failed; stale event refs, synthetic checkpoint ref, duplicate artifact after resume-then-advance, and missing resume failure log were reproduced.
```

Review-fix GREEN:

```text
uv run pytest backend/tests/runtime/test_deterministic_interrupts.py -v
Exit code: 0
Key output: 14 passed
```

## Review Result

Spec compliance review:

- Initial review found Critical/Important issues:
  - resume then advance could create a duplicate stage artifact;
  - resume failure path did not log failure while preserving waiting state;
  - event refs could include stale same-type events;
  - checkpoint refs could fall back to synthetic refs rather than preserving the runtime boundary result.
- Findings were verified, fixed, and re-reviewed.
- Re-review result: no Critical or Important findings remain.

Code quality and test sufficiency review:

- Initial review found Important issues:
  - resume then advance was unsafe for existing artifacts;
  - rejected approval resume needed to report the requested target stage;
  - stale event refs and synthetic checkpoint refs were not covered;
  - duplicate pending interrupt creation remains a potential concurrent-call race if two workers enter the same uncommitted stage simultaneously.
- The first four items were fixed and covered by tests.
- The duplicate pending race requires database uniqueness or AL03 service-level protection and is outside the A4.3 worker file scope.

## Verification

Focused runtime and contract verification:

```text
uv run pytest backend/tests/runtime/test_deterministic_interrupts.py backend/tests/runtime/test_deterministic_runtime.py backend/tests/runtime/test_runtime_engine_contract.py -v
Exit code: 0
Key output: 34 passed in 5.17s
```

Impacted AL03 service regression:

```text
uv run pytest backend/tests/services/test_clarification_flow.py backend/tests/services/test_approval_creation.py backend/tests/services/test_tool_confirmation_commands.py -q
Exit code: 0
Key output: 31 passed in 7.25s
```

Full backend suite:

```text
uv run pytest -q
Exit code: 0
Key output: 1042 passed, 3 warnings in 338.26s (0:05:38)
Warnings: existing backend/tests/providers/test_langchain_adapter.py UserWarning about explicit temperature parameters passed through model_kwargs.
```

Post-full-suite tracking-only updates:

- After the final full backend suite passed, only `docs/plans/implementation/a4.3-deterministic-interrupts.md` and this evidence report were updated with verification evidence.
- No code, test, configuration, dependency manifest, lockfile, or runtime-behavior artifact changed after the full suite.

## Owner Conflict Check

No owner conflict was introduced.

- AL03 human-loop services were consumed through public service APIs, not modified.
- AL04 deterministic runtime files were modified for the assigned slice.
- AL05 Provider/LangGraph runtime files were not modified.
- AL06 frontend files were not modified.
- Shared coordination store, central checkpoint snapshot, platform plan, and split-plan final statuses were not modified.

## Remaining Risk

- Concurrent duplicate pending interrupt creation across simultaneous `run_next()` calls is not fully prevented at the deterministic runtime layer. Robust protection belongs in database uniqueness constraints or AL03 service-level idempotency and is outside this slice's allowed file scope.
- A4.3 intentionally leaves terminal runtime controls, DeliveryRecord, `demo_delivery`, and LangGraph execution to later slices.

## Commit Readiness

Local result is `reported`. After a checkpoint commit containing code, tests, implementation plan, and this evidence report, the main coordination session can ingest this claim as `implemented`.
