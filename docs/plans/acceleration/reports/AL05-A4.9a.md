# AL05-A4.9a Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | AL05-A4.9a |
| Lane | AL05 |
| Task | A4.9a |
| Coordination Base | ea1aa8d |
| Worker HEAD | Filled by main coordination session during ingest |
| Evidence report | docs/plans/acceleration/reports/AL05-A4.9a.md |
| Implementation plan | docs/plans/implementation/a4.9a-context-envelope-builder.md |
| Local result | reported |
| Expected ingest result after user-approved checkpoint commit | implemented |

## Gate Summary

- Current branch: `feat/al-provider-langgraph-context`
- Active lane status: `claimed`
- Claim row: `AL05-A4.9a` is `claimed`
- Claim branch: `feat/al-provider-langgraph-context`
- Claim Coordination Base: `ea1aa8d`
- Platform task status: A4.9a is not completed and remains eligible on this branch
- Split-plan task status: A4.9a `[ ]`
- Start gate: AL05 may continue context-builder work after A4.8d and A4.9
- Owner scope honored under user-approved narrowing: no edit to `backend/app/services/artifacts.py`, no new `ArtifactStore` helper, only resolver/builder/context tests, implementation plan, and this evidence report changed

## Execution Path

Execution path: `subagent-driven-development`

- User explicitly allowed subagents, and all subagents used `gpt-5.5`.
- One bounded implementer subagent executed the builder TDD loop inside the allowed builder write set.
- Separate `gpt-5.5` static reviewers were used for resolver and builder spec/quality review checkpoints.
- Main agent retained slice selection, source-trace conflict review, trust-boundary fixes on resolver, review sequencing, fresh verification, and evidence updates.
- Builder and resolver write sets were kept disjoint during implementation; main agent integrated and re-verified the combined result.

## Changed Files

- `backend/app/context/source_resolver.py`
- `backend/app/context/builder.py`
- `backend/app/context/__init__.py`
- `backend/tests/context/test_context_source_resolver.py`
- `backend/tests/context/test_context_envelope_builder.py`
- `docs/plans/implementation/a4.9a-context-envelope-builder.md`
- `docs/plans/acceleration/reports/AL05-A4.9a.md`

## Unchanged Central Files

- `backend/app/services/artifacts.py`
- `docs/plans/function-one-acceleration-execution-plan.md`
- `docs/plans/function-one-platform-plan.md`
- `docs/plans/function-one-platform/06-langgraph-provider-context-stage-agent.md`
- Shared coordination store

## Implementation Summary

- Added `backend/app/context/source_resolver.py` to resolve approved implementation-plan inputs, explicit context references, working observations, reasoning-trace refs, and recent observations from explicit frozen inputs only.
- Tightened resolver trust boundaries so requirement / clarification / approval-style context references remain untrusted, tool/provider/model/process observations remain `REFERENCE_ONLY`, embedded foreign-run refs are filtered, unknown schemes are rejected, non-mapping `StageArtifact.process` values are ignored safely, and prompt-visible summaries are bounded.
- Added `backend/app/context/builder.py` with `ContextBuildRequest`, `ContextBuildResult`, and `ContextEnvelopeBuilder` for stage-call context assembly, prompt rendering integration, non-prompt section append, final render-hash recomputation, and `ContextManifest` construction.
- Builder uses only `PromptRenderer.render_messages(...)` for builtin prompt content and persists `context_manifest` exclusively via `ArtifactStore.append_process_record(process_key="context_manifest", ...)`.
- Builder hardens contract identity by validating `trace_context`, run-bound snapshots, `stage_contract_ref`, and provider/model-binding consistency before building.
- Structured-output-repair calls now require `parse_error`, reuse the repair prompt path, and record repair prompt provenance through `runtime_instructions` manifest records.
- Exported builder and resolver contracts from `backend/app/context/__init__.py`.

## TDD Evidence

Resolver RED/GREEN:

```text
Command: uv run pytest backend/tests/context/test_context_source_resolver.py -v
RED exit code: 1
Key output: focused failures while locking trust-boundary, foreign-ref, and bounded-summary behavior
GREEN exit code: 0
Key output: 6 passed in 0.16s
```

Builder RED/GREEN:

```text
Command: uv run pytest backend/tests/context/test_context_envelope_builder.py -v
RED exit code: 1
Key output: ModuleNotFoundError: No module named 'backend.app.context.builder'
GREEN exit code: 0
Key output: 8 passed in 0.15s
```

Combined focused verification:

```text
Command: uv run pytest backend/tests/context/test_context_source_resolver.py backend/tests/context/test_context_envelope_builder.py -v
Exit code: 0
Key output: 14 passed in 0.25s
```

## Review Result

Resolver review and fixes:

- Important finding fixed: `ContextReference` trust mapping no longer promotes requirement / clarification / approval feedback to trusted allowed context.
- Important finding fixed: artifact process refs for tool/provider/model/process observations no longer enter as `TRUSTED_REFERENCE`.
- Important finding fixed: embedded foreign-run refs inside otherwise allowed artifacts / change sets are now filtered.
- Important finding fixed: unknown schemes and overlong context-reference summaries are now rejected / bounded.

Builder review and fixes:

- Important finding fixed: `request.stage_contract_ref` is now validated against the current `graph_definition.stage_contracts[stage_type]` contract ref.
- Important finding fixed: provider/model-binding consistency is now validated instead of relying on a tautological post-build check.
- Important finding fixed: `structured_output_repair` prompt provenance now enters envelope/manifest records through prompt-backed `runtime_instructions` blocks.

Final static review status:

- Spec / plan reviewer: no Critical / Important findings remain
- Code-quality reviewer: no Critical / Important findings remain

## Verification

Fresh focused verification run by main agent:

```powershell
uv run pytest backend/tests/context/test_context_source_resolver.py backend/tests/context/test_context_envelope_builder.py -v
```

Result:

```text
Exit code: 0
14 passed in 0.25s
```

Fresh final verification run by main agent:

```powershell
uv run pytest backend/tests/context/test_context_source_resolver.py backend/tests/context/test_context_envelope_builder.py backend/tests/context/test_context_schemas.py backend/tests/prompts/test_prompt_renderer.py backend/tests/prompts/test_prompt_renderer_manifest_metadata.py backend/tests/services/test_artifact_store.py backend/tests/services/test_graph_compiler.py -v
```

Result:

```text
Exit code: 0
63 passed in 2.94s
```

After the final fresh verification, only `docs/plans/implementation/a4.9a-context-envelope-builder.md` and `docs/plans/acceleration/reports/AL05-A4.9a.md` were updated. No production code, tests, configuration, dependency manifests, lock files, coordination-store data, central checkpoint snapshot, platform-plan final status, or split-plan final status changed after verification.

## Remaining Risk

- `ContextReference` cross-run filtering still depends partly on known stable-ref formats because the current domain object does not carry its own `run_id` / `session_id`.
- `structured_output_repair` prompt content is now recorded through `runtime_instructions` manifest blocks for this slice; any future dedicated repair-only envelope semantics should preserve the same provenance guarantees.

## Commit Readiness

This local worker result is `reported`.

The branch now contains a coherent, freshly verified A4.9a checkpoint. After a user-approved checkpoint commit including the code, tests, implementation plan, and this evidence report, the main coordination session can ingest this claim as `implemented`.

No Git write action, merge, next claim, central Claim Ledger update, platform-plan status update, split-plan final status update, or coordination-store write was performed.
