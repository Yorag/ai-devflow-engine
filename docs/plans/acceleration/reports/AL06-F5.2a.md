# AL06-F5.2a Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL06-F5.2a` |
| Lane | `AL06` |
| Task | `F5.2a` |
| Coordination Base | `b81a53b` |
| Branch | `feat/al-frontend-runtime-ui` |
| Evidence report | `docs/plans/acceleration/reports/AL06-F5.2a.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |

Worker HEAD is intentionally not declared here; the main coordination session reads it during ingest.

## Scope

Assigned owner scope: frontend API client runtime-facing additions, workspace store, Feed, Inspector, Composer, Approval, and Delivery components.

Executed slice scope: `F5.2a demo_delivery 结果展示` only.

This checkpoint restores the previous mock-first F5.2a result to executable status by proving the frontend display contract against the real backend `delivery_result` / `DeliveryResultDetailProjection` payload shape:

- updated the demo `DeliveryResultFeedEntry` fixture to match `DeliveryService.append_delivery_result()` output
- rendered demo `branch_name` as `Display branch`, preserving the backend's display-only branch result point
- kept demo `commit_sha` and `code_review_url` hidden when they are `null`
- updated the demo `DeliveryResultDetailProjection` fixture to mirror backend Inspector projection sections, including `delivery_process`, `no_git_actions`, `git_write_actions`, stable refs, log refs, artifacts, and metrics
- strengthened DeliveryResultBlock, FeedEntryRenderer, and InspectorSections tests to prove the real payload shape
- reran `GitAutoDeliveryResultBlock` as a regression check without executing F5.2b work

It did not change backend contracts, API route code, SSE semantics, reducer semantics, `frontend/src/api/types.ts`, coordination state, platform plan final status, split-plan final status, or central checkpoint snapshots.

## Source Trace

- Platform task: `docs/plans/function-one-platform-plan.md` lists `F5.2a | demo_delivery 结果展示 | Week 9 | [/] | 前端`.
- Split task: `docs/plans/function-one-platform/08-git-delivery-and-result-display.md#f52a`.
- Implementation plan: `docs/plans/implementation/f5.2a-demo-delivery-result-ui.md`.
- Feed contract source: `backend/app/schemas/feed.py` and `backend/app/services/delivery.py`.
- Demo adapter source: `backend/app/delivery/demo.py`.
- Inspector contract source: `backend/app/services/projections/inspector.py`.
- Backend proof tests: `backend/tests/projections/test_delivery_result_detail_projection.py` and `backend/tests/e2e/test_full_api_flow.py`.

## Changed Files

- Modified `docs/plans/implementation/f5.2a-demo-delivery-result-ui.md`
- Modified `docs/plans/acceleration/reports/AL06-F5.2a.md`
- Modified `frontend/src/features/delivery/DeliveryResultBlock.tsx`
- Modified `frontend/src/features/delivery/__tests__/DeliveryResultBlock.test.tsx`
- Modified `frontend/src/features/feed/__tests__/FeedEntryRenderer.test.tsx`
- Modified `frontend/src/features/inspector/__tests__/InspectorSections.test.tsx`
- Modified `frontend/src/mocks/fixtures.ts`

## TDD Evidence

RED 1:

```powershell
npm --prefix frontend run test -- DeliveryResultBlock
```

- exit `1`
- key failure: expected `Demo delivery completed without Git writes.` and `Display branch` / `demo/run-completed`; current mock-first fixture still returned `Demo delivery generated a reviewable summary.` and omitted demo branch metadata

GREEN 1:

```powershell
npm --prefix frontend run test -- DeliveryResultBlock
npm --prefix frontend run test -- FeedEntryRenderer
```

- exit `0` for both commands
- key output:
  - `DeliveryResultBlock`: `Test Files 2 passed`, `Tests 13 passed`
  - `FeedEntryRenderer`: `Test Files 1 passed`, `Tests 10 passed`

RED 2:

```powershell
npm --prefix frontend run test -- InspectorSections
```

- exit `1`
- key failure: expected backend `DeliveryResultDetailProjection` output/process fields; current mock-first detail still exposed display-only `delivery_summary`, `delivery_target`, and `commit_message_preview`

GREEN 2:

```powershell
npm --prefix frontend run test -- InspectorSections
```

- exit `0`
- key output: `Test Files 1 passed`, `Tests 6 passed`

Review-driven correction:

- issue: feed `test_summary` and Inspector `output.test_summary` have different backend sources
- fix: keep feed `test_summary` as `Deterministic test path completed.` and set Inspector `output.test_summary` to `Deterministic test plan and execution summary produced.`
- recheck:
  - `npm --prefix frontend run test -- DeliveryResultBlock`, exit `0`, `2 passed`, `13 passed`
  - `npm --prefix frontend run test -- InspectorSections`, exit `0`, `1 passed`, `6 passed`

## Review

Spec / plan compliance review:

- F5.2a only; no F5.2b implementation performed.
- Feed summary and public result points come from `DeliveryResultFeedEntry`.
- Full input/process/output/artifacts/metrics remain in `DeliveryResultDetailProjection`.
- Demo branch is labeled `Display branch` to avoid implying real Git branch creation.
- Demo `commit_sha` and `code_review_url` remain hidden when `null`.
- No backend, schema, event, SSE, reducer, coordination store, platform plan, split-plan final status, or acceleration snapshot edits.

Code quality / testing / regression review:

- Tests were written red before implementation and rerun green.
- `GitAutoDeliveryResultBlock` remained green as a regression check.
- The component change is one metadata row in the existing grid; no new layout primitive, nested card, modal, or decorative treatment was added.
- Fixture remains typed against existing frontend API types.
- No dependency, manifest, lockfile, or generated artifact changes were made.

## Verification

Fresh final verification:

```powershell
npm --prefix frontend run test -- DeliveryResultBlock
npm --prefix frontend run test -- FeedEntryRenderer
npm --prefix frontend run test -- InspectorSections
npm --prefix frontend run test -- GitAutoDeliveryResultBlock
npm --prefix frontend run build
```

Exit codes: all `0`.

Key output:

```text
DeliveryResultBlock: Test Files 2 passed, Tests 13 passed
FeedEntryRenderer: Test Files 1 passed, Tests 10 passed
InspectorSections: Test Files 1 passed, Tests 6 passed
GitAutoDeliveryResultBlock: Test Files 1 passed, Tests 7 passed
vite build: 136 modules transformed, built in 1.17s
```

## Mock-First Status

The previous mock-first status is resolved for `demo_delivery`. This checkpoint now proves F5.2a against the real backend `delivery_result` / `DeliveryResultDetailProjection` payload shape for demo delivery.

This report does not complete F5.2b and does not claim git-auto-delivery real payload proof.

## Remaining Gate

Local result is `reported`.

After a user-approved worker checkpoint commit containing this report, implementation plan, code, and tests, the main coordination session can ingest `AL06-F5.2a` as `implemented`.

No blocker remains for this claim's demo-delivery payload proof.
