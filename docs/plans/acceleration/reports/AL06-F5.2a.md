# AL06-F5.2a Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL06-F5.2a` |
| Lane | `AL06` |
| Task | `F5.2a` |
| Coordination Base | `2451ebd` |
| Evidence report | `docs/plans/acceleration/reports/AL06-F5.2a.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |

Worker HEAD is intentionally not declared here; the main coordination session reads it during ingest.

## Scope

Assigned owner scope: frontend workspace store, SSE reducer, Feed, StageNode, Inspector, Composer, Approval, Tool Confirmation, Delivery UI.

Executed slice scope: `F5.2a demo_delivery 结果展示` only.

This pass implemented:

- a dedicated `DeliveryResultBlock` component for `delivery_result` feed rendering
- a frontend-only `buildDeliveryResultViewModel()` that stays within the current formal `DeliveryResultFeedEntry` contract
- feed wiring so `FeedEntryRenderer` delegates `delivery_result` rendering to the dedicated block
- demo-only `delivery_result` summary rendering that shows mode, final summary, current public result points, and the Inspector deep-link without fabricating git delivery highlights
- demo-only `DeliveryResultDetailProjection` mock alignment so Inspector output records now reflect display-only delivery semantics instead of git-like branch / commit / code review fields
- focused regression coverage for summary-only rendering, no-Git-leak behavior, and Inspector demo detail alignment

It did not change backend contracts, API route code, SSE semantics, reducer semantics, `git_auto_delivery` UI behavior, coordination state, platform plan final status, split-plan final status, or central checkpoint snapshots.

## Source Trace

- Platform task: `docs/plans/function-one-platform-plan.md` lists `F5.2a | demo_delivery 结果展示 | Week 9 | [ ] | 前端`.
- Split task: `docs/plans/function-one-platform/08-git-delivery-and-result-display.md#f52a`.
- Product boundary: `docs/specs/function-one-product-overview-v1.md:360-366`.
- Frontend summary/detail boundary: `docs/specs/frontend-workspace-global-design-v1.md:546-550` and `docs/specs/frontend-workspace-global-design-v1.md:914-929`.
- Backend domain/projection boundary: `docs/specs/function-one-backend-engine-design-v1.md:983-991`, `docs/specs/function-one-backend-engine-design-v1.md:1820-1824`, and `docs/specs/function-one-backend-engine-design-v1.md:1911-1923`.
- Implementation plan: `docs/plans/implementation/f5.2a-demo-delivery-result-ui.md`.

## Changed Files

- Modified `docs/plans/implementation/f5.2a-demo-delivery-result-ui.md`
- Created `docs/plans/acceleration/reports/AL06-F5.2a.md`
- Created `frontend/src/features/delivery/DeliveryResultBlock.tsx`
- Created `frontend/src/features/delivery/__tests__/DeliveryResultBlock.test.tsx`
- Modified `frontend/src/features/feed/FeedEntryRenderer.tsx`
- Modified `frontend/src/features/feed/__tests__/FeedEntryRenderer.test.tsx`
- Modified `frontend/src/features/inspector/__tests__/InspectorSections.test.tsx`
- Modified `frontend/src/mocks/fixtures.ts`
- Modified `frontend/src/styles/global.css`

## TDD Evidence

Implementer initial RED command:

```powershell
npm --prefix frontend run test -- --run src/features/delivery/__tests__/DeliveryResultBlock.test.tsx
```

Initial RED result:

- exit `1`
- key failure: failed to resolve `../DeliveryResultBlock` because the component did not exist yet

Fixture-alignment RED loop:

```powershell
npm --prefix frontend run test -- --run src/features/delivery/__tests__/DeliveryResultBlock.test.tsx
```

Result:

- exit `1`
- key failure: still red after fixture correction because `DeliveryResultBlock` and `buildDeliveryResultViewModel()` still did not exist yet

Feed integration RED command:

```powershell
npm --prefix frontend run test -- --run src/features/feed/__tests__/FeedEntryRenderer.test.tsx
```

Result:

- exit `1`
- key failure: old inline renderer did not satisfy `Demo delivery` / `Reference` summary expectations

Initial GREEN results after implementation:

- `npm --prefix frontend run test -- --run src/features/delivery/__tests__/DeliveryResultBlock.test.tsx`
  - exit `0`
  - key output: `1 passed`, `4 passed`
- `npm --prefix frontend run test -- --run src/features/feed/__tests__/FeedEntryRenderer.test.tsx`
  - exit `0`
  - key output: `1 passed`, `10 passed`

Inspector detail RED/GREEN loop:

- RED:
  - `npm --prefix frontend run test -- --run src/features/inspector/__tests__/InspectorSections.test.tsx`
  - exit `1`
  - key failure: old Inspector assertions still expected git-like demo detail output
- GREEN:
  - same command
  - exit `0`
  - key output: `1 passed`, `6 passed`

Review-driven fix loop:

- RED:
  - `npm --prefix frontend run test -- --run src/features/delivery/__tests__/DeliveryResultBlock.test.tsx`
  - exit `1`
  - key failure: intentionally leaked `Branch: feat/leaked`, `Commit`, and `Code review` text under demo summary-only expectations; also lacked reuse of the shared `feed-entry__title-row` hook
- GREEN:
  - same command
  - exit `0`
  - key output: `1 passed`, `6 passed`

## Review

Spec / plan compliance review result:

- First pass found one Important issue:
  - `DeliveryResultBlock` had already started visible `git_auto_delivery` behavior by reading and rendering `branch_name`, `commit_sha`, and `code_review_url`, which belongs to `F5.2b`, not `F5.2a`
- Fixes applied in-slice:
  - removed git-specific highlight rendering from `F5.2a`
  - removed preemptive non-demo visible branch behavior from the current block
  - kept `F5.2a` summary-only and demo-focused
- Residual minor note:
  - split-plan function-name suggestions (`formatDeliveryTarget()` / `formatDeliveryArtifacts()`) are still represented by the narrower `buildDeliveryResultViewModel()` implementation surface; visible behavior remains aligned to the approved slice

Code quality / testing / regression review result:

- First pass found two Medium issues and one Low issue:
  - demo “no Git fields” assertions could false-pass because they checked `Branch` / `Commit` / `Code review` rather than the actual rendered `Branch:`-style text
  - tests only covered demo mode, not future git mode; for this slice the correct fix was to avoid git-mode visible behavior entirely
  - the custom title-row class bypassed the existing mobile hook
- Fixes applied in-slice:
  - strengthened demo assertions to inspect full rendered text content
  - kept `F5.2a` demo-only and summary-only instead of adding `git_auto_delivery` visible coverage early
  - reused `feed-entry__title-row` in the delivery result block while keeping a small delivery-specific chip style
- Re-review result: no Critical or Important findings remained

## Verification

Fresh focused verification run in the main session:

```powershell
npm --prefix frontend run test -- DeliveryResultBlock
npm --prefix frontend run test -- FeedEntryRenderer
npm --prefix frontend run test -- InspectorSections
npm --prefix frontend run build
```

Exit codes: all `0`.

Key output:

```text
DeliveryResultBlock: Test Files 1 passed, Tests 6 passed
FeedEntryRenderer: Test Files 1 passed, Tests 10 passed
InspectorSections: Test Files 1 passed, Tests 6 passed
vite build: 136 modules transformed, built in 1.07s
```

After the final verification pass, only this worker evidence report and `docs/plans/implementation/f5.2a-demo-delivery-result-ui.md` were updated. No production code, tests, config, manifest, or lockfile changed after the final verification pass.

## Mock-First Status

This slice remains mock-first within the AL06 gate: it implements the reviewed `demo_delivery` feed/Inspector layering against the frozen frontend contract and mock payloads. Final integration still needs a later checkpoint to verify real backend `delivery_result` / `DeliveryResultDetailProjection` payloads match the current assumptions.

## Remaining Risks

- `F5.2a` intentionally does not cover `git_auto_delivery` visible behavior. `F5.2b` still needs its own dedicated rendering, tests, and review coverage for branch / commit / MR/PR information.
- The current demo summary relies on the existing narrow `DeliveryResultFeedEntry` contract. If later review decides the feed needs richer public summary fields, that must be a separate contract change rather than a silent extension of this slice.

## Commit Readiness

Commit approval can be requested for this reported local checkpoint. After a user-approved checkpoint commit, the main coordination session can ingest it as `implemented`.
