# AL06-F4.3 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL06-F4.3` |
| Lane | `AL06` |
| Task | `F4.3` |
| Coordination Base | `53a57a8` |
| Evidence report | `docs/plans/acceleration/reports/AL06-F4.3.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |

Worker HEAD is intentionally not declared here; the main coordination session reads it during ingest.

## Scope

Assigned owner scope: frontend workspace store, SSE reducer, Feed, StageNode, Inspector, Composer, Approval, Tool Confirmation, Delivery UI.

Executed slice scope: `F4.3 Approval Block 与 Reject 输入` only.

This pass implemented inline approval-request interaction for the current active run, inline reject-reason capture, delivery-readiness approve blocking for `git_auto_delivery`, read-only historical approval behavior, paused disabled approval copy, and reuse of the existing settings modal open path. It did not change backend contracts, approval projection schemas, SSE payload semantics, reducer semantics, tool confirmation behavior, rerun behavior, manifests, lock files, platform plan final status, split-plan final status, or the coordination store.

## Source Trace

- Platform task: `docs/plans/function-one-platform-plan.md` lists `F4.3 | Approval Block 与 Reject 输入 | Week 6 | [ ] | 前端`.
- Split task: `docs/plans/function-one-platform/04-human-loop-and-runtime.md#f43`.
- Frontend interaction source: `docs/specs/frontend-workspace-global-design-v1.md` approval sections, especially:
  - inline `Approve` / `Reject` actions inside Narrative Feed
  - inline reject reason input
  - `git_auto_delivery` readiness blocking only for `Approve`
  - paused current-run approvals remain disabled pending
  - historical approvals are read-only
- Backend projection source: `docs/specs/function-one-backend-engine-design-v1.md` approval projection fields `approve_action`, `reject_action`, `is_actionable`, `delivery_readiness_status`, `delivery_readiness_message`, `open_settings_action`, and `disabled_reason`.
- Implementation plan: `docs/plans/implementation/f4.3-approval-block-reject.md`.

## Changed Files

- Created `docs/plans/implementation/f4.3-approval-block-reject.md`
- Created `frontend/src/features/approvals/ApprovalBlock.tsx`
- Created `frontend/src/features/approvals/DeliveryReadinessNotice.tsx`
- Created `frontend/src/features/approvals/RejectReasonForm.tsx`
- Created `frontend/src/features/approvals/__tests__/ApprovalBlock.test.tsx`
- Modified `frontend/src/features/feed/FeedEntryRenderer.tsx`
- Modified `frontend/src/features/feed/NarrativeFeed.tsx`
- Modified `frontend/src/features/feed/__tests__/FeedEntryRenderer.test.tsx`
- Modified `frontend/src/features/settings/SettingsModal.tsx`
- Modified `frontend/src/features/workspace/WorkspaceShell.tsx`
- Modified `frontend/src/features/workspace/__tests__/WorkspaceShell.test.tsx`
- Modified `frontend/src/styles/global.css`
- Created `docs/plans/acceleration/reports/AL06-F4.3.md`

## TDD Evidence

Implementer initial RED commands:

```powershell
npm --prefix frontend run test -- --run src/features/approvals/__tests__/ApprovalBlock.test.tsx
npm --prefix frontend run test -- --run src/features/workspace/__tests__/WorkspaceShell.test.tsx
```

Implementer initial RED result:

- `ApprovalBlock.test.tsx`: failed because `ApprovalBlock` and related approval components did not exist yet.
- `WorkspaceShell.test.tsx`: failed before feed/workspace integration exposed the blocked-approval settings shortcut and inline reject flow through the workspace Narrative Feed.

Implementer GREEN commands:

```powershell
npm --prefix frontend run test -- --run src/features/approvals/__tests__/ApprovalBlock.test.tsx
npm --prefix frontend run test -- --run src/features/workspace/__tests__/WorkspaceShell.test.tsx
```

Implementer GREEN result:

- `ApprovalBlock.test.tsx`: pass, `4` tests passed.
- `WorkspaceShell.test.tsx`: pass, `25` tests passed.

Main-session review correction:

```powershell
<no additional command required during correction review>
```

Purpose:

- static review caught one integration bug before final review: approval API calls would have bypassed custom `request` fetchers because `request` was not yet threaded through `WorkspaceShell -> NarrativeFeed -> FeedEntryRenderer -> ApprovalBlock`
- implementer corrected the bug inside the existing write set before final verification

## Review

Spec / plan compliance review result:

- Approved with no Critical or Important findings.
- Confirmed:
  - inline Approval Block stays inside top-level `approval_request`
  - reject remains inline and does not create a modal workflow
  - `git_auto_delivery` readiness blocking disables only `Approve`
  - settings shortcut reuses the existing general settings modal
  - historical approvals are read-only
  - no `F4.3a` tool-confirmation behavior was changed

Code quality / testing / regression review result:

- Approved with no Critical or Important findings.
- Minor residual notes only:
  - direct non-workspace use without `currentRunId` can still expose clickable actions
  - reject API errors render twice while the reject form is open
  - `RejectReasonForm` uses a fixed textarea id

## Verification

Fresh focused verification run in the main session:

```powershell
npm --prefix frontend run test -- ApprovalBlock
npm --prefix frontend run test -- WorkspaceShell
npm --prefix frontend run build
```

Exit codes: all `0`.

Key output:

```text
ApprovalBlock: Test Files 1 passed, Tests 4 passed
WorkspaceShell: Test Files 1 passed, Tests 25 passed
vite build: 129 modules transformed
✓ built in 1.06s
```

Fresh full frontend suite verification run in the main session:

```powershell
npm --prefix frontend test
```

Exit code: `0`.

Key output:

```text
Test Files 19 passed
Tests 163 passed
```

After the final full-suite verification command passed, only this worker evidence report and the implementation plan execution record were updated. No production code, tests, config, dependency manifest, or lockfile changed after the final verification pass.

## Mock-First Status

This slice is not mock-first. It consumes the existing approval API client and existing workspace approval projection semantics.

## Remaining Risks

- `ApprovalBlock` still relies on workspace context (`currentRunId`, `sessionId`, `projectId`) being provided by the caller to avoid empty invalidation keys or mistakenly interactive history behavior in non-workspace direct renders.
- Reject API errors currently render both at the block level and inside the inline reject form while the form remains open.
- The reject textarea id is static; it is safe for the current one-active-approval interaction model, but would need hardening if multiple simultaneous reject forms were ever allowed.

## Commit Readiness

Commit approval can be requested for this reported local checkpoint. After a user-approved checkpoint commit, the main coordination session can ingest it as `implemented`.
