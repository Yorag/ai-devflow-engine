# AL04-D5.2 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL04-D5.2` |
| Lane | `AL04` |
| Task | `D5.2` |
| Branch | `feat/al-tools-deterministic-delivery` |
| Coordination Base | `9aeb3c0` |
| Evidence result | `reported` |
| Expected committed ingest result | `implemented` |
| Implementation plan | `docs/plans/implementation/d5.2-prepare-branch-create-commit.md` |

Worker HEAD is not declared by this worker report. The main coordination session reads branch HEAD during ingest.

## Gate Summary

- Worker discovery: `worker-start --json` returned claim `AL04-D5.2`, task `D5.2`, lane `AL04`, branch `feat/al-tools-deterministic-delivery`, status `claimed`, coordination base `9aeb3c0`, target `integration/function-one-acceleration`.
- Branch gate: current branch matched the claim branch and continued the AL04 delivery tools objective.
- Platform task status: D5.2 `[ ]`.
- Split-plan task status: D5.2 `[ ]`.
- D5.2 prerequisites D4.1-D4.3 and D5.1 were recorded complete before this worker claim.
- Owner boundary respected: implementation stayed inside the delivery tool module, focused delivery tests, this implementation plan, and this worker evidence report.
- No coordination store, acceleration execution plan, platform-plan final status, or split-plan final status was updated.

## Scope Outcome

Implemented the D5.2 `prepare_branch` and `create_commit` delivery tools:

- Added `PREPARE_BRANCH_TOOL_NAME`, `CREATE_COMMIT_TOOL_NAME`, `PrepareBranchTool`, and `CreateCommitTool`.
- Added schemas and delivery tool metadata for ToolRegistry execution.
- Set both Git write tools to high risk, `GIT_WRITE`, audit required, and workspace-bound through `repository_path`.
- Added `ScmDeliveryAdapter.run_git_cli()` using local `git` CLI, no shell, no GitPython, timeout handling, and sanitized Git environment.
- Added `prepare_branch()` to validate refs, create a controlled branch, read HEAD, return side-effect refs, and record concrete Git audit.
- Added `create_commit()` to discover changed paths, exclude `.runtime/logs`, unstage already-staged runtime logs, stage only non-runtime paths, create a commit, read the commit SHA, return changed files, and record concrete Git audit.
- Failed closed with `TOOL_AUDIT_REQUIRED_FAILED` when concrete Git write audit service was unavailable or failed.
- Preserved run-log and execution-gate behavior through `ToolRegistry.execute()`.

## Changed Files

- Modified: `backend/app/delivery/scm.py`
- Created: `backend/tests/delivery/test_prepare_branch_create_commit.py`
- Created: `docs/plans/implementation/d5.2-prepare-branch-create-commit.md`
- Created: `docs/plans/acceleration/reports/AL04-D5.2.md`

## TDD Evidence

Initial RED:

```text
uv run pytest backend/tests/delivery/test_prepare_branch_create_commit.py -v
Exit code: 1
Key output: import error for missing CREATE_COMMIT_TOOL_NAME from backend.app.delivery.scm
```

Initial GREEN:

```text
uv run pytest backend/tests/delivery/test_prepare_branch_create_commit.py -v
Exit code: 0
Key output: 6 passed in 3.21s
```

Workspace-boundary review fix RED:

```text
uv run pytest backend/tests/delivery/test_prepare_branch_create_commit.py -v
Exit code: 1
Key output: boundary regression expected BLOCKED but received WAITING_CONFIRMATION
```

Workspace-boundary review fix GREEN:

```text
uv run pytest backend/tests/delivery/test_prepare_branch_create_commit.py -v
Exit code: 0
Key output: 7 passed in 3.04s
```

Runtime-log deletion review fix RED:

```text
uv run pytest backend/tests/delivery/test_prepare_branch_create_commit.py -v
Exit code: 1
Key output: commit diff included D .runtime/logs/run-1.jsonl while changed_files hid it
```

Runtime-log deletion review fix GREEN:

```text
uv run pytest backend/tests/delivery/test_prepare_branch_create_commit.py -v
Exit code: 0
Key output: 8 passed in 3.76s
```

Concrete audit and hostile Git environment RED:

```text
uv run pytest backend/tests/delivery/test_prepare_branch_create_commit.py -v
Exit code: 1
Key output: create_commit returned TOOL_AUDIT_REQUIRED_FAILED with concrete AuditService; hostile GIT_DIR/GIT_WORK_TREE redirected git -C to the hostile repo
```

Concrete audit and hostile Git environment GREEN:

```text
uv run pytest backend/tests/delivery/test_prepare_branch_create_commit.py -v
Exit code: 0
Key output: 10 passed in 5.17s
```

## Review Result

Spec / plan compliance review:

- Initial review found one Important issue: `repository_path` was a free string while tools had `requires_workspace=False`, so the execution gate skipped workspace boundary validation.
- Fix set `requires_workspace=True` and `workspace_target_paths=("repository_path",)` on both tools and added a boundary regression test.
- Second review found one Important issue: `create_commit` staged broadly before excluding runtime logs, so tracked `.runtime/logs` deletions could be committed while hidden from `changed_files`.
- Fix changed staging to discover, unstage runtime logs, filter paths, and stage only non-runtime candidates.
- Third review approved with no findings.

Code quality, test sufficiency, and regression review:

- Initial review found one Critical issue: `_record_git_call()` did not match the concrete `AuditService.record_tool_call()` contract and omitted `exit_code`, `duration_ms`, `stdout_excerpt`, and `stderr_excerpt`.
- Initial review found one Important issue: `_sanitized_git_env()` retained ambient repo-affecting `GIT_*` variables.
- Fix passed the concrete audit fields, preserved metadata overrides, cleared ambient `GIT_*` variables before setting deterministic config, and removed unused `_RUNTIME_LOG_EXCLUSIONS`.
- Re-review approved with no Critical or Important findings. Reviewer did a static re-review only and did not rerun tests.

## Verification

Focused D5.2 verification:

```text
uv run pytest backend/tests/delivery/test_prepare_branch_create_commit.py -v
Exit code: 0
Key output: 10 passed in 5.17s
```

Impacted delivery/tool/compiler regression:

```text
uv run pytest backend/tests/delivery/test_prepare_branch_create_commit.py backend/tests/delivery/test_read_delivery_snapshot_tool.py backend/tests/tools/test_tool_protocol_registry.py backend/tests/tools/test_tool_execution_gate.py backend/tests/fixtures/test_fixture_contracts.py backend/tests/services/test_graph_compiler.py -v
Exit code: 0
Key output: 101 passed in 7.05s
```

Full backend suite:

```text
uv run pytest backend/tests -q
Exit code: 0
Key output: 1171 passed, 3 warnings in 202.08s
```

The warnings are the existing LangChain adapter `temperature` warnings in provider tests.

After the full backend suite passed, only the D5.2 implementation plan and this evidence report were updated. No code, test, configuration, dependency manifest, lockfile, or runtime-behavior artifact changed after the full suite.

## Residual Risks

- D5.2 only implements local branch and commit tools. Later D5.3-D5.4 slices still need push, code-review request creation, and end-to-end `git_auto_delivery` orchestration.
- Production workspace path resolution remains governed by the execution gate's `WorkspaceBoundaryPort`; D5.2 tests cover the declared target path and fixture repository path.

## Owner Conflict Check

No owner conflict was introduced.

- AL04 delivery tool files were modified for the assigned slice.
- AL01 run truth schema and state machine files were not modified.
- AL02 feed/SSE shared entry contracts were not modified.
- AL03 approval command and runtime-human-loop services were not modified.
- AL05 provider/LangGraph runtime files were not modified.
- AL06 frontend files were not modified.
- Central coordination store, acceleration execution plan, platform plan, and split-plan final statuses were not modified.

## Commit Readiness

The local worker result is `reported`. After a checkpoint commit including code, tests, implementation plan, and this evidence report, the main coordination session can scan the branch HEAD and ingest this claim as `implemented`.
