# AL06-F3.2 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL06-F3.2` |
| Lane | `AL06` |
| Task | `F3.2` |
| Coordination Base | `4ea6251` |
| Evidence report | `docs/plans/acceleration/reports/AL06-F3.2.md` |
| Local result | `reported` |
| Post-commit ingest expectation | `implemented` after a user-approved checkpoint commit |

Worker HEAD is intentionally not declared here; the main coordination session reads it during ingest.

## Scope

Assigned owner scope: frontend workspace store, SSE reducer, Feed, StageNode, Inspector, Composer, Approval, Tool Confirmation, Delivery UI.

Executed slice scope: `F3.2 SSE Client 与 Event Reducer` only.

This pass did not modify backend projection payloads, SSE payload schemas, API routes, OpenAPI generation, event schemas, package manifests, lock files, central Claim Ledger, platform plan status, split-plan final status, or the coordination store.

## Source Trace

- Platform plan task: `docs/plans/function-one-platform-plan.md` lists `F3.2 | SSE Client 与 Event Reducer | Week 5 | [ ] | 前端`.
- Split-plan task: `docs/plans/function-one-platform/03-run-projection-and-feed.md#f32` creates `frontend/src/features/workspace/sse-client.ts`, `frontend/src/features/workspace/event-reducer.ts`, and `frontend/src/features/workspace/__tests__/event-reducer.test.ts`.
- Frontend state boundary: `docs/specs/frontend-workspace-global-design-v1.md` section `1.2` requires `EventSource` increments to write through the workspace state model, not directly into UI components.
- Backend event contract: `docs/specs/function-one-backend-engine-design-v1.md` section `9.4`, `backend/app/schemas/events.py`, and `frontend/src/api/types.ts` define the `SessionEvent` envelope and canonical payload keys.
- Narrative Feed boundary: `docs/specs/frontend-workspace-global-design-v1.md` section `10.2` keeps `tool_confirmation` and `system_status` as top-level entries and Provider retry / circuit-breaker state inside `stage_node.items[]` provider-call projections.

## Changed Files

- Created `docs/plans/implementation/f3.2-sse-client-event-reducer.md`
- Created `frontend/src/features/workspace/__tests__/event-reducer.test.ts`
- Created `frontend/src/features/workspace/event-reducer.ts`
- Created `frontend/src/features/workspace/sse-client.ts`
- Created `docs/plans/acceleration/reports/AL06-F3.2.md`

## TDD Evidence

Initial RED command:

```powershell
npm --prefix frontend run test -- event-reducer
```

Initial RED exit code: `1`.

Initial RED key output:

```text
Failed to resolve import "../sse-client" from "src/features/workspace/__tests__/event-reducer.test.ts"
```

Initial GREEN command:

```powershell
npm --prefix frontend run test -- event-reducer
```

Initial GREEN exit code: `0`.

Initial GREEN key output:

```text
Test Files  1 passed (1)
Tests  6 passed (6)
```

Review-fix RED command:

```powershell
npm --prefix frontend run test -- event-reducer
```

Review-fix RED exit code: `1`.

Review-fix RED key output:

```text
1 failed | 6 passed
expected 'run-failed' to be 'run-retry'
```

Review-fix GREEN command:

```powershell
npm --prefix frontend run test -- event-reducer
```

Review-fix GREEN exit code: `0`.

Review-fix GREEN key output:

```text
Test Files  1 passed (1)
Tests  7 passed (7)
```

## Review

Spec / plan compliance review result:

- Approved.
- Reducer consumes the existing `SessionEvent` envelope and canonical payload keys.
- `tool_confirmation` and `system_status` remain top-level feed entries.
- Provider retry, waiting, circuit-breaker, and failure state are merged through `stage_updated.payload.stage_node.items[]` provider-call projections.
- No backend, schema, global plan status, or coordination store state changed.

Code quality / testing / regression review result:

- Initial review found one Important issue: an active `pipeline_run_created` event could append a retry run while leaving current/focused/snapshot/composer state bound to the previous terminal run.
- The issue was fixed with a failing test and a reducer update that promotes active run creation into current run focus, session projection, composer state, and refreshed snapshot state.
- Re-review reported no remaining Critical or Important issues.
- Remaining Minor note: `session_status_changed` scalar guard relies on backend `SessionEvent` validation for enum correctness.

## Verification

Final focused verification command:

```powershell
npm --prefix frontend run test -- event-reducer
```

Exit code: `0`.

Key output:

```text
Test Files  1 passed (1)
Tests  7 passed (7)
```

Final impacted frontend build command:

```powershell
npm --prefix frontend run build
```

Exit code: `0`.

Key output:

```text
tsc --noEmit && vite build
104 modules transformed
built in 1.05s
```

After these verification commands, only this implementation plan execution record and this worker evidence report were updated.

## Mock-First Status

This slice is implemented against the existing frozen frontend `SessionEvent`, `SessionWorkspaceProjection`, and feed projection types. It does not finalize the backend E3.2 SSE endpoint. Later integration must still verify the real backend SSE stream emits payloads accepted by this reducer.

## Remaining Risks

- The reducer trusts the backend-validated `SessionEvent` contract for enum correctness in `session_status_changed`.
- Real browser reconnect behavior is outside this slice because backend E3.2 SSE disconnect/replay is not implemented here.

## Commit Readiness

Commit approval can be requested for this reported local checkpoint. After a user-approved checkpoint commit, the main coordination session can ingest it as `implemented`.
