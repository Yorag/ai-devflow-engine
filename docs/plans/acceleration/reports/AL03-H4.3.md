# AL03-H4.3 Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | `AL03-H4.3` |
| Lane | `AL03` |
| Task | `H4.3` |
| Coordination Base | `74578b0` |
| Evidence result | `reported` |
| Expected committed ingest result | `implemented` |
| Implementation plan | `docs/plans/implementation/h4.3-approval-object-projection.md` |

Worker HEAD is not declared by this worker report. The main coordination session reads branch HEAD during ingest.

## Scope

Implemented only the H4.3 approval object creation and projection slice:

- `ApprovalService.create_solution_design_approval()`
- `ApprovalService.create_code_review_approval()`
- `ApprovalService.build_approval_request_projection()`
- runtime approval interrupt creation through `RuntimeOrchestrationService`
- `ApprovalRequestModel` creation for the two approved approval types
- source Session, PipelineRun, and StageRun transition to `waiting_approval`
- top-level `approval_request` event and workspace projection replay
- runtime log records for approval interrupt success, approval object creation, and interrupt failure

No approval command route, `ApprovalDecisionModel`, `approval_result`, delivery readiness blocking, delivery snapshot solidification, delivery integration resume, frontend change, DB schema change, platform plan status update, split-plan status update, or coordination store write was implemented.

## Changed Files

- Created: `backend/app/services/approvals.py`
- Created: `backend/tests/services/test_approval_creation.py`
- Created: `backend/tests/projections/test_approval_projection.py`
- Created: `docs/plans/implementation/h4.3-approval-object-projection.md`
- Created: `docs/plans/acceleration/reports/AL03-H4.3.md`

## TDD Evidence

Initial red:

```text
uv run python -m pytest backend/tests/services/test_approval_creation.py -v
Exit code: 1
ModuleNotFoundError: No module named 'backend.app.services.approvals'
```

Fixture repair red:

```text
uv run python -m pytest backend/tests/services/test_approval_creation.py -v
Exit code: 1
AttributeError: NEW_REQUIREMENT
```

The second red identified a test fixture using an obsolete enum member. The fixture was corrected to `RunTriggerSource.INITIAL_REQUIREMENT`.

Focused green:

```text
uv run python -m pytest backend/tests/services/test_approval_creation.py -v
Exit code: 0
5 passed in 1.32s
```

Projection green:

```text
uv run python -m pytest backend/tests/projections/test_approval_projection.py -v
Exit code: 0
2 passed in 1.05s
```

Review-fix green:

```text
uv run python -m pytest backend/tests/services/test_approval_creation.py -v
Exit code: 0
7 passed in 1.55s
```

## Verification

Final fresh focused verification:

```text
uv run python -m pytest backend/tests/services/test_approval_creation.py backend/tests/projections/test_approval_projection.py -v
Exit code: 0
9 passed in 1.94s
```

Final fresh impacted regression verification:

```text
uv run python -m pytest backend/tests/services/test_runtime_orchestration_boundary.py backend/tests/services/test_clarification_flow.py backend/tests/projections/test_workspace_projection.py backend/tests/schemas/test_run_feed_event_schemas.py -q
Exit code: 0
27 passed in 2.36s
```

Final fresh backend suite verification:

```text
uv run python -m pytest backend/tests -q
Exit code: 0
404 passed in 51.56s
```

After the full backend suite, only this implementation plan and evidence report were updated. No code, test, configuration, dependency, lock, or generated runtime behavior file was changed after the full suite.

## Review

Spec and plan compliance review:

- No Critical, Important, or Minor findings.
- H4.3 acceptance criteria are satisfied.
- No H4.4 or D4.0 behavior was introduced.
- No central coordination, platform plan, or split-plan status was updated.

Code quality, test sufficiency, and regression review:

- Initial review found two Important findings:
  - multi-SQLite commit operations can leave divergent stores if a later commit fails;
  - run-log failures after runtime interrupt creation could roll back domain state and leave an orphaned runtime interrupt.
- The log-failure/orphan-interrupt risk was fixed by making approval run-log writes non-blocking and by adding tests for failure-log stability and success-log non-rollback.
- Re-review found no remaining Critical or Important H4.3 blockers.
- Final static review found no Critical or Important blockers. Minor notes remain for the documented multi-SQLite transaction boundary, application-level duplicate pending approval race risk, and optional duplicate-guard test hardening to assert no second interrupt attempt.

## Residual Risks

- Cross-store commit atomicity remains a persistence architecture risk because runtime, control, and event sessions are committed independently. Existing human-loop services use the same boundary. H4.3 documents this risk instead of introducing a local transaction/outbox redesign.
- Duplicate pending approval prevention is serialized-call safe but not race-safe without a future DB constraint, lock, or single-writer orchestration boundary.
- `approval_request.delivery_readiness_status`, `delivery_readiness_message`, and `open_settings_action` are present but remain `null` until D4.0/H4.4 supplies delivery readiness semantics.

## Owner Conflict Check

No owner conflict was introduced.

- AL01 run truth schema was not modified.
- AL02 projection/SSE shared entry code was not modified.
- AL04 ToolProtocol and delivery/tool code was not modified.
- AL05 Provider/LangGraph runtime was not modified.
- AL06 frontend was not modified.
- Central Claim Ledger, platform plan, and split-plan final statuses were not modified.

## Commit Readiness

This local worker result is `reported`. After a user-approved checkpoint commit including code, tests, implementation plan, and this evidence report, the main coordination session can ingest this claim as `implemented`.
