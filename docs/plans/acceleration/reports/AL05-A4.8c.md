# AL05-A4.8c Worker Evidence Report

## Claim

| Field | Value |
| --- | --- |
| Claim | AL05-A4.8c |
| Lane | AL05 |
| Task | A4.8c |
| Coordination Base | 59529e1 |
| Worker HEAD | Filled by main coordination session during ingest |
| Evidence report | docs/plans/acceleration/reports/AL05-A4.8c.md |
| Implementation plan | docs/plans/implementation/a4.8c-prompt-registry-assets.md |
| Local result | reported |
| Expected ingest result after user-approved checkpoint commit | implemented |

## Gate Summary

- Current branch: `feat/al-provider-langgraph-context`
- Active lane status: `claimed`
- Claim row: `AL05-A4.8c` is `claimed`
- Claim branch: `feat/al-provider-langgraph-context`
- Claim Coordination Base: `59529e1`
- Platform task status: A4.8c is not completed and remains eligible on this branch
- Split-plan task status: A4.8c `[ ]`
- Start gate: AL05 may immediately start prompt registry and system prompt asset loading
- Owner scope honored: only `backend/app/prompts/*`, prompt asset markdown files, focused prompt tests, implementation plan, and this evidence report were changed

## Execution Path

Execution path: `subagent-driven-development`

- User explicitly allowed subagents, and all subagents used `gpt-5.5`.
- One bounded implementer subagent executed the TDD loops.
- Separate `gpt-5.5` spec-compliance and code-quality reviewers were used for the two-stage review gate.
- Main agent retained slice selection, source-trace review, review sequencing, fresh verification, and evidence updates.

## Changed Files

- `backend/app/prompts/__init__.py`
- `backend/app/prompts/definitions.py`
- `backend/app/prompts/registry.py`
- `backend/app/prompts/assets/runtime/runtime_instructions.md`
- `backend/app/prompts/assets/repairs/structured_output_repair.md`
- `backend/app/prompts/assets/compression/compression_context.md`
- `backend/app/prompts/assets/tools/tool_usage_common.md`
- `backend/tests/prompts/test_prompt_registry.py`
- `backend/tests/prompts/test_prompt_asset_loading.py`
- `docs/plans/implementation/a4.8c-prompt-registry-assets.md`
- `docs/plans/acceleration/reports/AL05-A4.8c.md`

## Unchanged Central Files

- `docs/plans/function-one-acceleration-execution-plan.md`
- `docs/plans/function-one-platform-plan.md`
- `docs/plans/function-one-platform/06-langgraph-provider-context-stage-agent.md`
- Shared coordination store

## Implementation Summary

- Added a backend-owned prompts module exposing `PromptRegistry`, `PromptAsset`, and structured prompt asset errors.
- Added `definitions.py` to centralize builtin prompt asset root paths, required prompt ids, and stage-type coverage mappings.
- Added builtin prompt markdown assets for `runtime_instructions`, `structured_output_repair`, `compression_prompt`, and `tool_usage_template`, each with explicit YAML front matter and repo-local `source_ref`.
- Implemented builtin asset scanning, front matter parsing, source-path validation, content-hash calculation, versioned lookup, `PromptVersionRef` resolution, and required-builtin presence checks.
- Reused `PromptAssetRead` as the runtime prompt asset contract instead of introducing a parallel data model.
- Preserved existing `TemplateService` role-seed loading behavior while validating `agent_role_seed.*` assets through the same prompt-schema boundary.
- Tightened the registry after review so missing `source_ref` is rejected and latest-version selection uses numeric date/sequence ordering instead of lexicographic sorting.

## TDD Evidence

Initial RED:

```text
Command: uv run pytest backend/tests/prompts/test_prompt_registry.py backend/tests/prompts/test_prompt_asset_loading.py -v
Exit code: 1
Key output: ModuleNotFoundError: No module named 'backend.app.prompts.registry'
```

Initial GREEN:

```text
Command: uv run pytest backend/tests/prompts/test_prompt_registry.py backend/tests/prompts/test_prompt_asset_loading.py -v
Exit code: 0
Key output: 9 passed in 0.18s
```

Initial regression:

```text
Command: uv run pytest backend/tests/prompts/test_prompt_registry.py backend/tests/prompts/test_prompt_asset_loading.py backend/tests/prompts/test_agent_role_seed_assets.py backend/tests/services/test_template_seed.py backend/tests/schemas/test_prompt_asset_schemas.py -v
Exit code: 0
Key output: 24 passed in 1.42s
```

Review-fix RED:

```text
Command: uv run pytest backend/tests/prompts/test_prompt_asset_loading.py -v
Exit code: 1
Key output: missing `source_ref` did not raise; `.9` selected over `.10`
```

Review-fix GREEN:

```text
Command: uv run pytest backend/tests/prompts/test_prompt_registry.py backend/tests/prompts/test_prompt_asset_loading.py -v
Exit code: 0
Key output: 11 passed in 0.13s
```

Review-fix regression:

```text
Command: uv run pytest backend/tests/prompts/test_prompt_registry.py backend/tests/prompts/test_prompt_asset_loading.py backend/tests/prompts/test_agent_role_seed_assets.py backend/tests/services/test_template_seed.py backend/tests/schemas/test_prompt_asset_schemas.py -v
Exit code: 0
Key output: 26 passed in 0.53s
```

## Review Result

Spec / plan compliance review:

- Initial finding fixed: missing `source_ref` is no longer synthesized; non-role builtin prompt assets now fail when `source_ref` is omitted.
- Pass: `PromptRegistry` consumes `PromptAssetRead` and does not introduce parallel prompt fields.
- Pass: builtin system prompt assets are repo files with YAML front matter, explicit `source_ref`, and stable version metadata.
- Pass: no PromptRenderer, ContextEnvelope, TemplateService rewiring, or out-of-scope runtime/context work was introduced.

Code-quality / regression review:

- Initial finding fixed: latest-version selection now uses numeric date/sequence ordering so `.10` is selected after `.9`.
- Pass: loader and lookup failures remain structured and deterministic.
- Pass: impacted role-seed and template-seed regressions remain green.

Final re-review status:

- Spec / plan reviewer: approved
- Code-quality reviewer: approved
- No remaining Critical or Important findings

## Verification

Focused verification:

```powershell
uv run pytest backend/tests/prompts/test_prompt_registry.py backend/tests/prompts/test_prompt_asset_loading.py -v
```

Result:

```text
Exit code: 0
11 passed in 0.16s
```

Fresh final verification:

```powershell
uv run pytest backend/tests/prompts/test_prompt_registry.py backend/tests/prompts/test_prompt_asset_loading.py backend/tests/prompts/test_agent_role_seed_assets.py backend/tests/services/test_template_seed.py backend/tests/schemas/test_prompt_asset_schemas.py -v
```

Result:

```text
Exit code: 0
26 passed in 0.84s
```

After the final fresh verification, only `docs/plans/implementation/a4.8c-prompt-registry-assets.md` and `docs/plans/acceleration/reports/AL05-A4.8c.md` were updated. No production code, tests, configuration, dependency manifests, lock files, coordination-store data, central checkpoint snapshot, platform-plan final status, or split-plan final status changed after verification.

## Remaining Risk

- Existing `agent_role_seed.*` assets still rely on A4.8c’s compatibility normalization for omitted non-identity front-matter fields (`prompt_type`, `authority_level`, `model_call_type`, `cache_scope`). This preserves C2.2 template-seeding behavior without broadening this slice into template-service or asset-migration work, but later prompt-asset hardening may want a dedicated follow-up slice if the repo decides all role assets must declare the full metadata set explicitly.

## Commit Readiness

This local worker result is `reported`.

The branch now contains a coherent, freshly verified A4.8c checkpoint. After a user-approved checkpoint commit including the code, tests, implementation plan, and this evidence report, the main coordination session can ingest this claim as `implemented`.

No Git write action, merge, next claim, central Claim Ledger update, platform-plan status update, split-plan final status update, or coordination-store write was performed.
