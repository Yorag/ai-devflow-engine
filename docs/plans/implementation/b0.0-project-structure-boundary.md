# B0.0 Project Structure Boundary Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task in the main agent session. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a tracked repository skeleton and document path ownership before production code begins.

**Architecture:** This is a skeleton-and-documentation slice. `.gitkeep` files make the initial backend, frontend, e2e, and API documentation directories visible to Git without defining runtime behavior; `docs/architecture/project-structure.md` records path ownership; README and B0.0 tracking rows point contributors to the current planning and architecture entry points.

**Tech Stack:** Markdown documentation, `.gitkeep` skeleton placeholders, PowerShell filesystem verification, ripgrep verification, manual source-trace review against the current platform plan and split specs.

---

## Source Trace

- Baseline task detail: `docs/plans/function-one-platform/00-project-skeleton-and-execution.md#b00`
- Platform task row: `docs/plans/function-one-platform-plan.md`
- Product and stage boundary support: `docs/specs/function-one-product-overview-v1.md`
- Frontend structure support: `docs/specs/frontend-workspace-global-design-v1.md`
- Backend module and storage boundary support: `docs/specs/function-one-backend-engine-design-v1.md`
- Execution overrides: `.codex/skills/slice-workflow/references/superpowers-execution-rules.md`

## Files

- Create: `backend/**/.gitkeep`
- Create: `frontend/src/**/.gitkeep`
- Create: `e2e/tests/.gitkeep`
- Create: `docs/api/.gitkeep`
- Create: `docs/architecture/project-structure.md`
- Modify: `README.md`
- Modify: `docs/plans/function-one-platform-plan.md`
- Modify: `docs/plans/function-one-platform/00-project-skeleton-and-execution.md`
- Modify: `docs/plans/function-one-delivery-branch-plan.md`

No production behavior is implemented in this slice. `.gitkeep` files are skeleton placeholders only; they do not define Python packages, frontend modules, API contracts, imports, runtime behavior, or test targets.

## Skeleton Placeholder Files

- `backend/alembic/.gitkeep`
- `backend/app/api/routes/.gitkeep`
- `backend/app/context/.gitkeep`
- `backend/app/core/.gitkeep`
- `backend/app/db/models/.gitkeep`
- `backend/app/delivery/.gitkeep`
- `backend/app/domain/.gitkeep`
- `backend/app/observability/.gitkeep`
- `backend/app/prompts/assets/compression/.gitkeep`
- `backend/app/prompts/assets/repairs/.gitkeep`
- `backend/app/prompts/assets/roles/.gitkeep`
- `backend/app/prompts/assets/runtime/.gitkeep`
- `backend/app/prompts/assets/tools/.gitkeep`
- `backend/app/providers/.gitkeep`
- `backend/app/repositories/.gitkeep`
- `backend/app/runtime/.gitkeep`
- `backend/app/schemas/.gitkeep`
- `backend/app/services/projections/.gitkeep`
- `backend/app/tools/.gitkeep`
- `backend/app/workspace/.gitkeep`
- `backend/tests/api/.gitkeep`
- `backend/tests/context/.gitkeep`
- `backend/tests/core/.gitkeep`
- `backend/tests/db/.gitkeep`
- `backend/tests/delivery/.gitkeep`
- `backend/tests/domain/.gitkeep`
- `backend/tests/e2e/.gitkeep`
- `backend/tests/errors/.gitkeep`
- `backend/tests/events/.gitkeep`
- `backend/tests/fixtures/.gitkeep`
- `backend/tests/observability/.gitkeep`
- `backend/tests/projections/.gitkeep`
- `backend/tests/prompts/.gitkeep`
- `backend/tests/providers/.gitkeep`
- `backend/tests/regression/.gitkeep`
- `backend/tests/runtime/.gitkeep`
- `backend/tests/schemas/.gitkeep`
- `backend/tests/services/.gitkeep`
- `backend/tests/support/.gitkeep`
- `backend/tests/tools/.gitkeep`
- `backend/tests/workspace/.gitkeep`
- `frontend/src/api/.gitkeep`
- `frontend/src/app/.gitkeep`
- `frontend/src/features/approvals/.gitkeep`
- `frontend/src/features/composer/.gitkeep`
- `frontend/src/features/delivery/.gitkeep`
- `frontend/src/features/errors/.gitkeep`
- `frontend/src/features/feed/.gitkeep`
- `frontend/src/features/inspector/.gitkeep`
- `frontend/src/features/runs/.gitkeep`
- `frontend/src/features/settings/.gitkeep`
- `frontend/src/features/templates/.gitkeep`
- `frontend/src/features/tool-confirmations/.gitkeep`
- `frontend/src/features/workspace/.gitkeep`
- `frontend/src/mocks/.gitkeep`
- `frontend/src/pages/.gitkeep`
- `frontend/src/styles/.gitkeep`
- `e2e/tests/.gitkeep`
- `docs/api/.gitkeep`

## Acceptance Criteria Mapping

- Project structure is traceable in a dedicated architecture document.
- Backend, frontend, e2e, and API documentation skeleton directories are visible in the working tree and are Git-trackable through `.gitkeep` placeholders.
- `B0.1`, `B0.2`, and `F0.1` file landing paths match the documented structure.
- `C1.10a`, `A4.8b-A4.8d`, and `A4.9a-A4.9b` context and prompt paths match the documented structure.
- B0.0 does not create `pyproject.toml`, `frontend/package.json`, `frontend/tsconfig.json`, `frontend/vite.config.ts`, `e2e/package.json`, or `e2e/playwright.config.ts`; those engineering entry points remain owned by B0.1 or later slices.
- Tracking updates are limited to B0.0 in the platform plan, B0.0 in the split plan, and DB00 in the delivery branch plan.

## Task 1: Red Verification For Missing Skeleton And Structure Document

**Files:**
- Verify target: `docs/architecture/project-structure.md`
- Verify target: `backend/app/context/.gitkeep`
- Verify target: `frontend/src/app/.gitkeep`
- Verify target: `e2e/tests/.gitkeep`
- Verify target: `docs/api/.gitkeep`
- Verify target: `README.md`

- [x] **Step 1: Run the planned skeleton existence command before implementation**

Run:

```powershell
Test-Path backend/app/context/.gitkeep, frontend/src/app/.gitkeep, e2e/tests/.gitkeep, docs/api/.gitkeep
```

Expected:

```text
False
False
False
False
```

The expected failure reason is that B0.0 has not created tracked skeleton placeholders yet.

- [x] **Step 2: Run the planned documentation trace command before implementation**

Run:

```powershell
rg -n "backend/|frontend/|docs/plans/implementation|\\.gitkeep" docs/architecture/project-structure.md README.md
```

Expected:

```text
rg: docs/architecture/project-structure.md: IO error for operation on docs/architecture/project-structure.md: The system cannot find the path specified. (os error 3)
```

The exact Windows wording can include `The system cannot find the file specified`; the expected failure reason is that `docs/architecture/project-structure.md` does not exist before B0.0.

## Task 2: Add Tracked Skeleton Placeholders

**Files:**
- Create: all files listed in `Skeleton Placeholder Files`.

- [x] **Step 1: Create `.gitkeep` files in the planned skeleton leaf directories**

Each `.gitkeep` file contains:

```text
Tracked skeleton placeholder. Remove when real files land.
```

These files make the skeleton visible to Git without creating production behavior, Python package semantics, frontend module semantics, API contracts, or test targets.

## Task 3: Add The Project Structure Reference

**Files:**
- Create: `docs/architecture/project-structure.md`

- [x] **Step 1: Create `docs/architecture/project-structure.md` with the structure, path responsibilities, and task traceability**

Create the file with these sections:

```markdown
# Project Structure

## Purpose

This document records the repository structure for function-one platform V1 implementation. It is both a tracked skeleton reference and a path ownership reference for implementation slices.

## Structure

`backend/` contains the FastAPI service, domain model, persistence adapters, runtime orchestration, observability, provider, tool, workspace, and delivery code.

`frontend/` contains the Vite React SPA, console routes, API client, feature modules, mocks, pages, and global styles.

`e2e/` contains Playwright configuration and browser-level cross-end tests.

`docs/api/` contains API and OpenAPI companion documentation.

`docs/architecture/` contains durable architecture notes such as this structure map.

`docs/plans/implementation/` contains one implementation plan per execution slice.

## Backend Boundaries

`backend/app/api/` owns API router aggregation, error response contracts, and OpenAPI exposure.

`backend/app/core/` owns startup configuration, `EnvironmentSettings`, application dependencies, and common middleware wiring.

`backend/app/db/` owns SQLAlchemy session helpers, database roles, and multi-SQLite bindings.

`backend/app/domain/` owns pure domain objects and enums, including stage status, run status, graph definitions, runtime snapshots, and future extension objects.

`backend/app/schemas/` owns Pydantic request, response, projection, event, and snapshot schemas.

`backend/app/services/` owns business services and projection assembly services.

`backend/app/observability/` owns runtime data preflight, JSONL logs, log indexes, audit records, trace context, redaction, rotation, retention, and diagnostic log queries.

`backend/app/runtime/` owns deterministic test runtime, LangGraph runtime integration, runtime limit consumption, and automated regression execution.

`backend/app/context/` owns `ContextEnvelope`, `ContextManifest`, context source resolution, context size guard, and compression process records.

`backend/app/prompts/` owns system prompt assets, `PromptRegistry`, `PromptRenderer`, prompt version traceability, and rendering helpers.

`backend/app/prompts/assets/` owns versioned built-in prompt files grouped by compression, repair, role, runtime, and tool usage concerns.

`backend/app/providers/` owns Provider registry and LangChain-compatible provider adapters.

`backend/app/tools/` owns `ToolProtocol`, `ToolRegistry`, tool input/output schemas, tool errors, audit references, and cross-tool contracts.

`backend/app/workspace/` owns isolated workspace management and workspace tools: `bash`, `read_file`, `edit_file`, `write_file`, `glob`, and `grep`.

`backend/app/delivery/` owns `demo_delivery`, `git_auto_delivery`, SCM adapters, delivery snapshots, and `DeliveryRecord` production.

## Frontend Boundaries

`frontend/src/api/` owns API client types, REST hooks, query keys, and cache integration.

`frontend/src/app/` owns router setup, `QueryClient`, global providers, and test utilities.

`frontend/src/features/` owns feature modules for approvals, composer, delivery, errors, feed, inspector, runs, settings, templates, tool confirmations, and workspace behavior.

`frontend/src/mocks/` owns mock fixtures and mock handlers derived from backend schemas and projection contracts.

`frontend/src/pages/` owns route-level page composition.

`frontend/src/styles/` owns global CSS and theme variables.

## Test Boundaries

`backend/tests/api/` owns FastAPI route, error response, and OpenAPI contract tests.

`backend/tests/core/` owns startup configuration and application dependency boundary tests.

`backend/tests/db/` owns database session, model, migration, and multi-SQLite boundary tests.

`backend/tests/domain/`, `backend/tests/schemas/`, `backend/tests/services/`, `backend/tests/projections/`, `backend/tests/events/`, `backend/tests/runtime/`, `backend/tests/tools/`, `backend/tests/workspace/`, `backend/tests/delivery/`, `backend/tests/providers/`, `backend/tests/prompts/`, and `backend/tests/context/` own tests for their matching production boundaries.

`backend/tests/observability/` owns runtime data, log, audit, redaction, trace context, rotation, retention, and diagnostic query tests.

`backend/tests/fixtures/` owns fake providers, fake tools, fixture repositories, mock remote contracts, and settings overrides used by tests.

`backend/tests/regression/` owns release-candidate cross-slice regression tests.

`backend/tests/support/` owns shared test helpers that do not define business fake semantics.

`e2e/tests/` owns browser-level Playwright tests.

## Slice Traceability

`B0.1` creates the first backend and frontend engineering baseline files under `backend/`, `backend/tests/`, and `frontend/`.

`B0.2` creates the FastAPI application and API error contract under `backend/app/api/`, `backend/app/core/`, and `backend/tests/api/`.

`F0.1` creates the SPA shell under `frontend/src/app/`, `frontend/src/pages/`, and `frontend/src/styles/`.

`C1.10a` creates prompt schema tests and prompt schemas under `backend/app/schemas/`, `backend/tests/schemas/`, and later prompt runtime assets under `backend/app/prompts/`.

`A4.8b-A4.8d` use `backend/app/context/`, `backend/app/prompts/`, `backend/tests/context/`, and `backend/tests/prompts/` for context and prompt runtime boundaries.

`A4.9a-A4.9b` use `backend/app/context/`, `backend/app/runtime/`, `backend/tests/context/`, and `backend/tests/runtime/` for context envelope building, context manifest recording, size guards, and compression process records.

## Skeleton Placeholder Rule

B0.0 tracks the initial directory skeleton with `.gitkeep` placeholders in leaf directories. Later slices remove a placeholder when they add the first real source file, test, asset, or documentation file to that directory. Placeholder files do not define runtime behavior, package boundaries, imports, API contracts, or test targets.
```

## Task 4: Update README Entry Points

**Files:**
- Modify: `README.md`

- [x] **Step 1: Replace the minimal README with repository entry points**

Update `README.md` to:

```markdown
# ai-devflow-engine

AI-driven requirement delivery workflow engine.

## Current Implementation Entry Points

- Platform plan: `docs/plans/function-one-platform-plan.md`
- Slice plans: `docs/plans/function-one-platform/`
- Implementation plans: `docs/plans/implementation/`
- Project structure: `docs/architecture/project-structure.md`

## Function-One Specification Set

- Product boundary: `docs/specs/function-one-product-overview-v1.md`
- Frontend workspace design: `docs/specs/frontend-workspace-global-design-v1.md`
- Backend engine and collaboration contract: `docs/specs/function-one-backend-engine-design-v1.md`

## Development Status

Implementation proceeds one platform slice at a time. B0.0 creates the tracked directory skeleton with `.gitkeep` placeholders; later slices replace those placeholders with real source files, tests, assets, or documentation.
```

## Task 5: Green Verification For Skeleton And Structure Traceability

**Files:**
- Verify target: `backend/**/*.gitkeep`
- Verify target: `frontend/src/**/*.gitkeep`
- Verify target: `e2e/tests/.gitkeep`
- Verify target: `docs/api/.gitkeep`
- Verify target: `docs/architecture/project-structure.md`
- Verify target: `README.md`

- [x] **Step 1: Run the skeleton existence command after implementation**

Run:

```powershell
Test-Path backend/app/context/.gitkeep, frontend/src/app/.gitkeep, e2e/tests/.gitkeep, docs/api/.gitkeep
```

Expected:

```text
True
True
True
True
```

- [x] **Step 2: Run the skeleton count command after implementation**

Run:

```powershell
(Get-ChildItem -Recurse -Force -Filter .gitkeep backend,frontend,e2e,docs\api | Measure-Object).Count
```

Expected:

```text
59
```

- [x] **Step 3: Run the documentation trace command after implementation**

Run:

```powershell
rg -n "backend/|frontend/|docs/plans/implementation|\\.gitkeep" docs/architecture/project-structure.md README.md
```

Expected:

```text
docs/architecture/project-structure.md:<line>:...
README.md:<line>:...
```

At least one match must appear for `backend/`, `frontend/`, `docs/plans/implementation`, and `.gitkeep`.

- [x] **Step 4: Run manual trace commands for required future slices**

Run:

```powershell
rg -n "B0\\.1|B0\\.2|F0\\.1|C1\\.10a|A4\\.8b|A4\\.8c|A4\\.8d|A4\\.9a|A4\\.9b|backend/app/context|backend/app/prompts|backend/tests/context|backend/tests/prompts" docs/architecture/project-structure.md
```

Expected:

```text
docs/architecture/project-structure.md:<line>:`B0.1` ...
docs/architecture/project-structure.md:<line>:`A4.8b-A4.8d` ...
docs/architecture/project-structure.md:<line>`backend/app/context/` ...
```

The output must show the B0.1, B0.2, F0.1, C1.10a, A4.8b-A4.8d, A4.9a-A4.9b trace lines and the context/prompts path lines.

## Task 6: Update B0.0 Tracking

**Files:**
- Modify: `docs/plans/function-one-platform-plan.md`
- Modify: `docs/plans/function-one-platform/00-project-skeleton-and-execution.md`
- Modify: `docs/plans/function-one-delivery-branch-plan.md`

- [x] **Step 1: Update only the B0.0 platform-plan status after green verification**

Change the B0.0 task row from:

```markdown
| B0.0 | 项目目录骨架与边界声明 | Week 1 | [ ] | 串行 | [00](function-one-platform/00-project-skeleton-and-execution.md#b00) |
```

to:

```markdown
| B0.0 | 项目目录骨架与边界声明 | Week 1 | [x] | 串行 | [00](function-one-platform/00-project-skeleton-and-execution.md#b00) |
```

- [x] **Step 2: Update only the B0.0 split-plan status and verification note**

Change the B0.0 status line from:

```markdown
**状态**：`[ ]`
```

to:

```markdown
**状态**：`[x]`
**实施计划**：`docs/plans/implementation/b0.0-project-structure-boundary.md`
**验证摘要**：`Test-Path backend/app/context/.gitkeep, frontend/src/app/.gitkeep, e2e/tests/.gitkeep, docs/api/.gitkeep` 通过；`rg -n "backend/|frontend/|docs/plans/implementation|\\.gitkeep" docs/architecture/project-structure.md README.md` 通过；人工核对 B0.1、B0.2、F0.1、C1.10a、A4.8b-A4.8d 与 A4.9a-A4.9b 的文件落点与骨架一致。
```

- [x] **Step 3: Update only the DB00 delivery-branch status after green verification**

Change the DB00 row in `docs/plans/function-one-delivery-branch-plan.md` from:

```markdown
| DB00 | `docs/project-structure-boundary` | B0.0 | 无 | S | merged | `README.md`, `docs/plans/*` | 项目目录骨架、路径职责和实施计划落点 |
```

to:

```markdown
| DB00 | `docs/project-structure-boundary` | B0.0 | 无 | S | ready_for_review | `README.md`, `docs/plans/*` | 项目目录骨架、路径职责和实施计划落点 |
```

`merged` is reserved for work already integrated into `main`; this branch uses `ready_for_review` once B0.0 verification passes and the branch is ready for PR/MR review.

## Completion Verification Checklist

- [x] `rg -n "backend/|frontend/|docs/plans/implementation" docs/architecture/project-structure.md README.md` exits `0`.
- [x] `Test-Path backend/app/context/.gitkeep, frontend/src/app/.gitkeep, e2e/tests/.gitkeep, docs/api/.gitkeep` returns four `True` values.
- [x] `(Get-ChildItem -Recurse -Force -Filter .gitkeep backend,frontend,e2e,docs\api | Measure-Object).Count` returns `59`.
- [x] `rg -n "backend/|frontend/|docs/plans/implementation|\\.gitkeep" docs/architecture/project-structure.md README.md` exits `0`.
- [x] `rg -n "B0\\.1|B0\\.2|F0\\.1|C1\\.10a|A4\\.8b|A4\\.8c|A4\\.8d|A4\\.9a|A4\\.9b|backend/app/context|backend/app/prompts|backend/tests/context|backend/tests/prompts" docs/architecture/project-structure.md` exits `0`.
- [x] `rg -n "DB00.*ready_for_review" docs/plans/function-one-delivery-branch-plan.md` exits `0`.
- [x] `git status --short` shows only B0.0 skeleton placeholders, B0.0 documentation, implementation plan, and tracking changes.
- [x] No commit, push, PR creation, merge, tag, rebase, worktree creation, or branch cleanup is run. If a commit is appropriate after verification, prepare a commit approval request only.

## Plan Self-Review

- Spec coverage: The plan covers the B0.0 skeleton placeholder files, directory responsibility documentation, future-slice path traceability, placeholder replacement rule, and tracking update rules.
- Placeholder scan: No unresolved placeholder markers remain.
- Type and path consistency: All paths match the split-plan structure and current repository agreement.
- API/OpenAPI checklist: Not applicable; no `backend/app/api/routes/*` changes.
- Log & Audit Integration: Not applicable; no user command interface, runtime lifecycle, tool call, workspace write, Git delivery, configuration change, or security-sensitive failure behavior is implemented.
- Frontend Design Gate: Not applicable; B0.0 has no visible frontend UI.
