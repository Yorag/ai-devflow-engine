# V6.8 Config Snapshot Regression Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` to implement this plan task-by-task. Fallback to `superpowers:executing-plans` only if subagents cannot be scheduled or the task boundary cannot be precisely constrained. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add cross-link regression coverage for configuration boundaries, run snapshot immutability, built-in prompt asset boundaries, frontend settings visibility, and project/session history behavior.

**Architecture:** V6.8 is a regression slice. The implementation adds focused backend and frontend tests against existing formal schemas, services, fixtures, API projections, and UI components; production code changes are allowed only if a red test exposes a real boundary regression in the current implementation.

**Tech Stack:** Python 3.11+, pytest, FastAPI/SQLAlchemy test fixtures, React 18, Vitest, Testing Library, existing frontend settings and workspace components.

---

## Source Trace

- Claim: `QA-CONFIG-V6.8`
- Task: `V6.8 配置边界与运行快照回归`
- Lane: `QA-CONFIG`
- Branch: `test/qa-config-snapshot-regression`
- Split-plan source: `docs/plans/function-one-platform/09-regression-hardening-and-logs.md#v68`
- Platform source: `docs/plans/function-one-platform-plan.md`
- Product source:
  - `docs/specs/function-one-product-overview-v1.md`: `配置真源与运行快照分层`; started runs must use frozen template, Provider/model binding, runtime limit, graph, and delivery channel snapshots.
  - `docs/specs/function-one-product-overview-v1.md`: feature one has no cross-session long-term memory; historical Sessions and runs are review, trace, diagnosis, and audit objects.
- Frontend source:
  - `docs/specs/frontend-workspace-global-design-v1.md`: settings modal exposes only user-visible DeliveryChannel, Provider, and project-scope import/export configuration.
  - `docs/specs/frontend-workspace-global-design-v1.md`: Provider model capabilities stay in collapsed `高级设置`; template editing excludes system prompt assets, prompt versions, platform settings, SQLite paths, and deterministic runtime.
  - `docs/specs/frontend-workspace-global-design-v1.md`: history views must not imply new Sessions inherit historical runs as Agent memory.
- Backend source:
  - `docs/specs/function-one-backend-engine-design-v1.md`: `EnvironmentSettings`, user-visible config objects, `PlatformRuntimeSettings`, `ConfigurationPackage`, built-in prompt assets, and run snapshots are distinct sources.
  - `docs/specs/function-one-backend-engine-design-v1.md`: `compression_prompt` is a built-in `PromptRegistry` asset, not environment, user config, `PlatformRuntimeSettings`, `ConfigurationPackage`, frontend setting, or template field.
  - `docs/specs/function-one-backend-engine-design-v1.md`: `ContextManifest` records actual model capability and `compression_threshold_ratio` values with snapshot references.
- Execution rules: `.codex/skills/slice-workflow/references/superpowers-execution-rules.md`

No Source Trace Conflict Gate conflict is identified. V6.8 is allowed by the QA gate to add regression tests in parallel. The slice must not update platform-plan or split-plan final status.

## File Map

- Create: `backend/tests/regression/test_config_snapshot_regression.py`
  - Owns environment, database path, runtime settings, Provider/model binding, delivery channel, configuration package, context size guard, and settings fixture regression assertions.
- Create: `backend/tests/regression/test_prompt_asset_boundary_regression.py`
  - Owns built-in prompt asset, `compression_prompt`, PromptRenderer, PromptRegistry, and compression process record regression assertions.
- Create: `backend/tests/regression/test_project_session_history_regression.py`
  - Owns project/session soft history, active-run delete/remove blocking, workspace visibility, and no cross-session memory regression assertions.
- Create: `frontend/src/features/settings/__tests__/SettingsBoundary.test.tsx`
  - Owns settings modal, Provider advanced settings, configuration package export/import, and template editor boundary regression assertions.
- Create: `frontend/src/features/workspace/__tests__/ProjectSessionHistory.test.tsx`
  - Owns project/sidebar/session history and historical run presentation boundary regression assertions.
- Update: `frontend/src/features/inspector/__tests__/InspectorPanel.test.tsx`
  - Owns a final full-suite test stability fix: delivery result detail assertions are scoped to the Inspector and allow the same stable ref to appear in multiple Inspector records.

No `backend/app/api/routes/*`, production schema, service, runtime, frontend component, package manifest, lock file, migration, coordination store, acceleration execution plan, platform plan, or split-plan status changes are planned. Because no API route changes are planned, no new API/OpenAPI checklist is required.

## Frontend Design Gate

V6.8 includes visible settings and workspace regression tests, so the frontend design quality gate applies.

- Register: product UI.
- Project context: no repo `PRODUCT.md` or `DESIGN.md` exists; global Impeccable product reference applies.
- Baseline tone: inherit the existing quiet, professional, high-information-density workspace UI.
- Adopt: existing tabs, dialog, inline form labels, collapsed `高级设置`, sidebar session affordances, and visible-but-not-decorative status summaries.
- Do not introduce: new palettes, marketing layout, decorative motion, nested cards, invented controls, or explanatory UI text that changes product semantics.
- Regression focus: settings modal must not show forbidden platform fields; template editor must not expose built-in prompt assets; historical Session/run surfaces must remain review/diagnosis UI and must not imply long-term Agent memory.
- Verification: Vitest component tests plus static review of text queries, disabled controls, focus-safe dialog usage inherited from existing tests.

## Log & Audit Integration

V6.8 exercises configuration and history boundaries but does not add production logging or audit behavior. Regression tests must verify that logs/audit do not become product truth:

- `ConfigurationPackage` export/import remains backed by formal control-plane objects and existing audit/log paths.
- Session delete and Project remove remain soft-history commands with existing audit behavior and active-run guards.
- Runtime snapshot assertions read formal snapshot objects and process records, not `.runtime/logs` or `log.db` as authoritative state.
- Prompt asset assertions use `PromptRegistry`, `PromptRenderer`, `ContextManifest`, and compression process records, not environment variables or frontend text.

## Subagent Execution Boundary

Implementer subagent:

- Must use `superpowers:test-driven-development`.
- Must work only in this worktree and only for claim `QA-CONFIG-V6.8` / task `V6.8`.
- Allowed files:
  - `backend/tests/regression/test_config_snapshot_regression.py`
  - `backend/tests/regression/test_prompt_asset_boundary_regression.py`
  - `backend/tests/regression/test_project_session_history_regression.py`
  - `frontend/src/features/settings/__tests__/SettingsBoundary.test.tsx`
  - `frontend/src/features/workspace/__tests__/ProjectSessionHistory.test.tsx`
  - Production files only if a red test proves a real V6.8 regression and the fix stays inside the owning boundary.
- Allowed commands:
  - `uv run pytest backend/tests/regression/test_config_snapshot_regression.py -v`
  - `uv run pytest backend/tests/regression/test_prompt_asset_boundary_regression.py -v`
  - `uv run pytest backend/tests/regression/test_project_session_history_regression.py -v`
  - `uv run pytest backend/tests/regression/test_config_snapshot_regression.py backend/tests/regression/test_prompt_asset_boundary_regression.py backend/tests/regression/test_project_session_history_regression.py -v`
  - `npm --prefix frontend run test -- SettingsBoundary`
  - `npm --prefix frontend run test -- ProjectSessionHistory`
  - `npm --prefix frontend run test -- SettingsBoundary ProjectSessionHistory`
- Must stop and report `APPROVAL_REQUIRED` before dependency install/sync, lock or manifest edits, migrations, environment/config edits, delete/move operations, external service writes, coordination store writes, or any Git write action.
- Must not update `docs/plans/function-one-acceleration-execution-plan.md`, `docs/plans/function-one-platform-plan.md`, split-plan final statuses, coordination store, implementation plan, evidence report, or Git state.
- Must not expose `compression_prompt`, `PlatformRuntimeSettings`, SQLite paths, deterministic runtime, or system prompt asset versions through frontend UI.
- Must report red command, red exit code and key output, green command, green exit code and key output, changed files, and any production fix rationale.

Reviewer order:

1. Spec / plan compliance reviewer checks V6.8 scope, source trace, all five required files, configuration-source separation, snapshot freeze semantics, built-in prompt asset boundaries, frontend settings/template boundaries, history/no-memory semantics, and forbidden tracking/Git writes.
2. Code quality / testing / regression reviewer checks test isolation, helper clarity, assertions against real schema/service/component behavior, no brittle style coupling, focused frontend queries, no hidden dependency on logs as truth, and impacted regression risk.

Reviewers default to static review and do not repeat tests unless the main agent asks. Critical or Important findings must be fixed and re-reviewed before final verification.

## Task 1: Backend Config And Snapshot Regression

**Files:**

- Create: `backend/tests/regression/test_config_snapshot_regression.py`

- [x] **Step 1: Write failing backend config regression tests**

Create `backend/tests/regression/test_config_snapshot_regression.py` with the following test names:

```python
def test_environment_settings_and_database_paths_remain_startup_only(tmp_path, monkeypatch) -> None:
    ...


def test_runtime_settings_and_provider_updates_do_not_mutate_started_run_snapshots(tmp_path) -> None:
    ...


def test_configuration_package_and_settings_override_do_not_cross_runtime_snapshot_boundary(tmp_path) -> None:
    ...


def test_context_size_guard_uses_frozen_model_capabilities_and_runtime_threshold() -> None:
    ...
```

The tests must assert:

- `EnvironmentSettings.model_fields` excludes Provider fields, DeliveryChannel fields, platform runtime limits, log policy, `compression_threshold_ratio`, prompt asset fields, `compression_prompt`, deterministic runtime, and per-role SQLite path fields.
- `DatabaseManager.from_environment_settings()` derives exactly the five SQLite paths from `platform_runtime_root` and does not read per-database environment fields.
- `RuntimeLimitSnapshotBuilder` freezes `PlatformRuntimeSettings.context_limits.compression_threshold_ratio`; later settings changes do not mutate the already-built snapshot.
- `ProviderSnapshotBuilder` and `ModelBindingSnapshotBuilder` freeze `context_window_tokens`, `max_output_tokens`, `supports_tool_calling`, `supports_structured_output`, and `supports_native_reasoning`; later Provider mutation does not mutate the snapshots.
- `ConfigurationPackageExport` rejects `compression_threshold_ratio`, runtime snapshots, audit/log bodies, platform database paths, and real secrets via strict schemas and existing service redaction.
- `settings_override_fixture()` only accepts test-local root and credential prefix overrides.
- `ContextSizeGuard.compression_trigger_tokens()` computes `floor(128000 * 0.8) == 102400`, and reserved output tokens only lower the threshold.
- Additional review fix: `test_delivery_channel_updates_do_not_mutate_started_run_snapshot_or_run_ref` covers `DeliverySnapshotService.prepare_delivery_snapshot`, `DeliveryChannelSnapshotModel`, and `PipelineRunModel.delivery_channel_snapshot_ref` freeze semantics after the source `DeliveryChannelModel` is mutated.

- [x] **Step 2: Run config regression RED**

Run:

```powershell
uv run pytest backend/tests/regression/test_config_snapshot_regression.py -v
```

Expected RED before the file exists:

```text
ERROR: file or directory not found: backend/tests/regression/test_config_snapshot_regression.py
```

After creating the file, any failing assertion must be a real boundary assertion, not syntax/import failure.

- [x] **Step 3: Implement the minimal test-local support**

Use existing helpers from:

- `backend/tests/core/test_environment_settings.py`
- `backend/tests/services/test_runtime_limit_snapshot.py`
- `backend/tests/services/test_provider_model_binding_snapshots.py`
- `backend/tests/services/test_configuration_package_service.py`
- `backend/tests/context/test_context_size_guard.py`
- `backend/tests/fixtures/test_fixture_contracts.py`

Do not duplicate production logic. Keep fixture builders inside the test module unless an existing helper already exists.

- [x] **Step 4: Run config regression GREEN**

Run:

```powershell
uv run pytest backend/tests/regression/test_config_snapshot_regression.py -v
```

Expected GREEN:

```text
backend/tests/regression/test_config_snapshot_regression.py::test_environment_settings_and_database_paths_remain_startup_only PASSED
backend/tests/regression/test_config_snapshot_regression.py::test_runtime_settings_and_provider_updates_do_not_mutate_started_run_snapshots PASSED
backend/tests/regression/test_config_snapshot_regression.py::test_configuration_package_and_settings_override_do_not_cross_runtime_snapshot_boundary PASSED
backend/tests/regression/test_config_snapshot_regression.py::test_context_size_guard_uses_frozen_model_capabilities_and_runtime_threshold PASSED
```

## Task 2: Backend Prompt Asset Boundary Regression

**Files:**

- Create: `backend/tests/regression/test_prompt_asset_boundary_regression.py`

- [x] **Step 1: Write failing prompt boundary tests**

Create `backend/tests/regression/test_prompt_asset_boundary_regression.py` with the following test names:

```python
def test_compression_prompt_is_builtin_asset_not_user_or_environment_configuration() -> None:
    ...


def test_prompt_renderer_records_compression_prompt_version_and_hash_without_template_field() -> None:
    ...


def test_compression_process_record_references_prompt_version_and_render_hash() -> None:
    ...


def test_prompt_registry_rejects_user_config_sources_for_builtin_prompt_assets(tmp_path) -> None:
    ...
```

The tests must assert:

- `PromptRegistry.load_builtin_assets().get("compression_prompt")` returns a built-in asset with `PromptType.COMPRESSION_PROMPT`, `PromptAuthorityLevel.SYSTEM_TRUSTED`, `PromptCacheScope.RUN_STATIC`, source ref under `backend://prompts/compression/`, and no front matter leaked into body.
- `PromptRegistry.list_by_type(PromptType.COMPRESSION_PROMPT)` contains `compression_prompt` and does not accept user template/system prompt text as a registered built-in prompt.
- `PromptRenderer.render_messages()` for `ModelCallType.CONTEXT_COMPRESSION` returns metadata with `prompt_id == "compression_prompt"`, a prompt version ref, a stable render hash, and no template-editable prompt field.
- `ContextCompressionRunner.compress()` writes a `CompressedContextBlock` with `compression_prompt_id`, `compression_prompt_version`, `compression_render_hash`, and model call trace records.
- A temporary prompt asset root with front matter `source_ref` pointing to an environment, frontend, or template path raises `PromptAssetMetadataError`.
- Review fix: compression process records are asserted by `process_key`, not append order.

- [x] **Step 2: Run prompt regression RED**

Run:

```powershell
uv run pytest backend/tests/regression/test_prompt_asset_boundary_regression.py -v
```

Expected RED before the file exists:

```text
ERROR: file or directory not found: backend/tests/regression/test_prompt_asset_boundary_regression.py
```

- [x] **Step 3: Implement the minimal test-local support**

Reuse test-local patterns from `backend/tests/context/test_context_compression.py`, `backend/tests/prompts/test_prompt_registry.py`, and `backend/tests/prompts/test_prompt_renderer.py`. Do not modify prompt assets unless a red test proves an actual metadata bug.

- [x] **Step 4: Run prompt regression GREEN**

Run:

```powershell
uv run pytest backend/tests/regression/test_prompt_asset_boundary_regression.py -v
```

Expected GREEN: all four prompt boundary tests pass.

## Task 3: Backend Project And Session History Regression

**Files:**

- Create: `backend/tests/regression/test_project_session_history_regression.py`

- [x] **Step 1: Write failing history regression tests**

Create `backend/tests/regression/test_project_session_history_regression.py` with the following test names:

```python
def test_new_session_does_not_read_other_session_history_as_context(tmp_path) -> None:
    ...


def test_session_delete_and_project_remove_hide_history_without_deleting_runtime_truth(tmp_path) -> None:
    ...


def test_active_run_blocks_session_delete_and_project_remove(tmp_path) -> None:
    ...
```

The tests must assert:

- A new `Session` has `status == draft`, `current_run_id is None`, no automatic historical run, artifact, approval, tool confirmation, or provider process context refs.
- Same-Session rerun history can remain queryable as same demand chain context, but cross-Session history must not be injected into new Session context.
- Session delete soft-hides the Session from workspace/list queries and keeps runtime `PipelineRunModel`, snapshots, delivery facts, and audit facts intact.
- Project remove soft-hides the non-default Project and visible Sessions, does not delete runtime rows, and rejects removal for active runs.
- Default Project removal is blocked.
- Review fix: soft-hide preservation explicitly checks `StageArtifactModel`, `ApprovalRequestModel`, `ApprovalDecisionModel`, and `ToolConfirmationRequestModel` rows for both Session delete and Project remove flows.

- [x] **Step 2: Run history regression RED**

Run:

```powershell
uv run pytest backend/tests/regression/test_project_session_history_regression.py -v
```

Expected RED before the file exists:

```text
ERROR: file or directory not found: backend/tests/regression/test_project_session_history_regression.py
```

- [x] **Step 3: Implement the minimal test-local support**

Reuse patterns from:

- `backend/tests/api/test_session_history_api.py`
- `backend/tests/api/test_project_remove_api.py`
- `backend/tests/services/test_project_remove_history.py`
- `backend/tests/services/test_session_history_commands.py`
- `backend/tests/projections/test_workspace_projection.py`

Do not add long-term memory, new history APIs, or production query aliases.

- [x] **Step 4: Run history regression GREEN**

Run:

```powershell
uv run pytest backend/tests/regression/test_project_session_history_regression.py -v
```

Expected GREEN: all three history regression tests pass.

## Task 4: Frontend Settings Boundary Regression

**Files:**

- Create: `frontend/src/features/settings/__tests__/SettingsBoundary.test.tsx`

- [x] **Step 1: Write failing frontend settings tests**

Create `frontend/src/features/settings/__tests__/SettingsBoundary.test.tsx` with the following test names:

```typescript
it("keeps platform runtime and prompt asset fields out of settings surfaces", async () => {
  // ...
});

it("keeps provider capabilities collapsed under advanced settings and out of platform settings", async () => {
  // ...
});

it("exports only project scoped user visible configuration", async () => {
  // ...
});

it("keeps template editing separate from system prompt assets", async () => {
  // ...
});
```

The tests must assert:

- Settings modal does not display environment variables, platform runtime root, SQLite paths, platform run limits, log policy, `compression_threshold_ratio`, built-in prompt assets, `prompt_id`, `prompt_version`, `runtime_instructions`, structured output repair prompt, `compression_prompt`, deterministic runtime, or real credential values.
- Provider capabilities appear only after opening `高级设置` and use the existing Provider settings controls.
- Configuration package export/import text excludes real secrets, platform internal settings, system prompt asset body, runtime snapshots, historical runs, logs, and audit bodies.
- Template editor exposes only allowed runtime configuration fields and excludes DeliveryChannel, SQLite, platform limits, prompt asset IDs/versions, `runtime_instructions`, structured output repair prompt, and `compression_prompt`.

- [x] **Step 2: Run settings regression RED**

Run:

```powershell
npm --prefix frontend run test -- SettingsBoundary
```

Expected RED before the file exists:

```text
No test files found
```

or a failing assertion after the file is created.

- [x] **Step 3: Implement the minimal test-local support**

Reuse patterns from:

- `frontend/src/features/settings/__tests__/SettingsModal.test.tsx`
- `frontend/src/features/templates/__tests__/TemplateEditor.test.tsx`

Do not change component copy or layout unless a red test proves a V6.8 regression. Preserve existing product UI conventions.

- [x] **Step 4: Run settings regression GREEN**

Run:

```powershell
npm --prefix frontend run test -- SettingsBoundary
```

Expected GREEN: all four settings boundary tests pass.

## Task 5: Frontend Project And Session History Regression

**Files:**

- Create: `frontend/src/features/workspace/__tests__/ProjectSessionHistory.test.tsx`

- [x] **Step 1: Write failing workspace history tests**

Create `frontend/src/features/workspace/__tests__/ProjectSessionHistory.test.tsx` with the following test names:

```typescript
it("keeps historical runs in the same session surface without making them current controls", async () => {
  // ...
});

it("does not present historical sessions as automatic memory for new sessions", async () => {
  // ...
});

it("keeps session and project destructive actions blocked for active or default boundaries", async () => {
  // ...
});
```

The tests must assert:

- Historical run boundaries render as historical and do not own pause/resume/terminate or tool confirmation submit actions.
- Draft/new Session UI does not show copy implying automatic memory inheritance from historical Sessions, runs, artifacts, approvals, tool confirmations, or provider process records.
- Active-run Session delete remains disabled, default Project remove remains disabled, and non-default Project remove is unavailable in the current shell until backend command support is intentionally exposed.

- [x] **Step 2: Run workspace history regression RED**

Run:

```powershell
npm --prefix frontend run test -- ProjectSessionHistory
```

Expected RED before the file exists:

```text
No test files found
```

or a failing assertion after the file is created.

- [x] **Step 3: Implement the minimal test-local support**

Reuse patterns from:

- `frontend/src/features/workspace/__tests__/WorkspaceShell.test.tsx`
- `frontend/src/features/feed/__tests__/RunSwitcher.test.tsx`
- `frontend/src/features/feed/__tests__/ToolConfirmationBlock.test.tsx`

Do not change component copy or layout unless a red test proves a V6.8 regression.

- [x] **Step 4: Run workspace history regression GREEN**

Run:

```powershell
npm --prefix frontend run test -- ProjectSessionHistory
```

Expected GREEN: all three workspace history tests pass.

## Task 6: Combined Slice Verification

**Files:**

- Test-only files from Tasks 1-5.
- This implementation plan and the worker evidence report.

- [x] **Step 1: Run combined backend V6.8 regression**

Run:

```powershell
uv run pytest backend/tests/regression/test_config_snapshot_regression.py backend/tests/regression/test_prompt_asset_boundary_regression.py backend/tests/regression/test_project_session_history_regression.py -v
```

Expected GREEN: all V6.8 backend regression tests pass.

- [x] **Step 2: Run combined frontend V6.8 regression**

Run:

```powershell
npm --prefix frontend run test -- SettingsBoundary ProjectSessionHistory
```

Expected GREEN: all V6.8 frontend regression tests pass.

- [x] **Step 3: Run impacted backend regression suites**

Run:

```powershell
uv run pytest backend/tests/core/test_environment_settings.py backend/tests/services/test_runtime_limit_snapshot.py backend/tests/services/test_provider_model_binding_snapshots.py backend/tests/services/test_configuration_package_service.py backend/tests/context/test_context_size_guard.py backend/tests/context/test_context_compression.py backend/tests/api/test_session_history_api.py backend/tests/api/test_project_remove_api.py backend/tests/services/test_project_remove_history.py -v
```

Expected GREEN: all selected impacted backend tests pass.

- [x] **Step 4: Run impacted frontend regression suites**

Run:

```powershell
npm --prefix frontend run test -- SettingsModal TemplateEditor WorkspaceShell
```

Expected GREEN: all selected impacted frontend tests pass.

- [x] **Step 5: Run slice-level full suites before reported/commit-ready**

Run:

```powershell
uv run pytest -q
npm --prefix frontend run test -- --run
npm --prefix frontend run build
```

Expected GREEN: backend suite, frontend suite, and frontend build pass. If full frontend suite or build is too slow or environment-blocked, report the exact failure output and do not claim full verification.

## Completion Verification Checklist

- [x] All five required V6.8 regression test files exist.
- [x] Backend tests cover EnvironmentSettings, database path derivation, PlatformRuntimeSettings, runtime snapshots, Provider/model binding snapshots, ConfigurationPackage, settings override fixture, Context Size Guard threshold, built-in prompt assets, ContextCompression process records, and project/session history.
- [x] Frontend tests cover settings modal boundary, Provider advanced settings, configuration package boundary, template editor boundary, historical run presentation, no cross-session memory copy, and disabled destructive actions.
- [x] No production code changes are present.
- [x] No coordination store, acceleration execution plan, platform plan, or split-plan final status update is present.
- [x] TDD red/green evidence is recorded.
- [x] Spec/plan compliance review and code quality review have no open Critical or Important findings.
- [x] Final fresh verification has been run according to `slice-workflow` and `verification-before-completion`.

## Execution Evidence

Current local result: `reported`.

- Worker discovery: `uv run python .codex/skills/acceleration-workflow/scripts/coordination_store.py worker-start --json` returned claim `QA-CONFIG-V6.8`, task `V6.8`, lane `QA-CONFIG`, branch `test/qa-config-snapshot-regression`, status `claimed`, branch head `2e682ec`, target `integration/function-one-acceleration` at `292aab7`, worktree dirty, and branch behind target by `16` commits. No sync, merge, rebase, coordination store update, platform plan update, split-plan status update, or checkpoint snapshot update was performed.
- Dependency approval was granted by the user; `uv sync --extra dev` and `npm --prefix frontend ci` installed already-declared local dependencies.
- TDD red evidence used missing-file RED for the new regression modules before creation; focused test failures during construction were corrected without production code changes.
- Review fix evidence:
  - Added delivery channel snapshot freeze regression through `DeliverySnapshotService`.
  - Reworked compression process record assertions to key by `process_key`.
  - Added explicit soft-hide preservation assertions for stage artifact, approval request, approval decision, and tool confirmation rows.
  - Scoped existing InspectorPanel delivery detail assertion after full frontend suite exposed duplicate stable refs in both identity and stable-ref records.
- Code review:
  - Final spec/plan reviewer Important finding for delivery snapshot freeze coverage was fixed.
  - Final code-quality reviewer Minor findings were fixed.
  - Focused re-review subagent reported no Critical, Important, or Minor issues in the latest review fixes.

Fresh verification:

```powershell
uv run pytest backend/tests/regression/test_config_snapshot_regression.py::test_delivery_channel_updates_do_not_mutate_started_run_snapshot_or_run_ref -v
```

Exit code: `0`; key output: `1 passed`.

```powershell
uv run pytest backend/tests/regression/test_config_snapshot_regression.py backend/tests/regression/test_prompt_asset_boundary_regression.py backend/tests/regression/test_project_session_history_regression.py -v
```

Exit code: `0`; key output: `12 passed`.

```powershell
npm --prefix frontend run test -- SettingsBoundary ProjectSessionHistory
```

Exit code: `0`; key output: `2 passed (2)` test files, `7 passed (7)` tests.

```powershell
uv run pytest backend/tests/core/test_environment_settings.py backend/tests/services/test_runtime_limit_snapshot.py backend/tests/services/test_provider_model_binding_snapshots.py backend/tests/services/test_configuration_package_service.py backend/tests/context/test_context_size_guard.py backend/tests/context/test_context_compression.py backend/tests/api/test_session_history_api.py backend/tests/api/test_project_remove_api.py backend/tests/services/test_project_remove_history.py backend/tests/services/test_delivery_snapshot_gate.py -v
```

Exit code: `0`; key output: `115 passed`.

```powershell
npm --prefix frontend run test -- SettingsModal TemplateEditor WorkspaceShell
```

Exit code: `0`; key output: `3 passed (3)` test files, `55 passed (55)` tests.

```powershell
uv run pytest -q
```

Exit code: `0`; key output: `1295 passed, 3 warnings in 604.59s`.

```powershell
npm --prefix frontend run test -- --run
```

Initial exit code: `1`; root cause: `InspectorPanel.test.tsx` used a global/single text query for `delivery-record-1`, while the rendered detail intentionally included that stable ref in multiple Inspector records. The test was scoped and changed to `findAllByText`.

Rerun exit code: `0`; key output: `27 passed (27)` test files, `219 passed (219)` tests.

```powershell
npm --prefix frontend run build
```

Exit code: `0`; key output: `tsc --noEmit && vite build`, `136 modules transformed`, `built in 975ms`.
