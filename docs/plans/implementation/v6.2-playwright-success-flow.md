# V6.2 Playwright Success Flow Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Fallback to superpowers:executing-plans only when subagents are unavailable or cannot be safely constrained. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finalize the QA-E2E-V6.2 Playwright regression package on coordination base `e668943` by proving the feature-one success path starts from the real sidebar `New session` action before requirement submission, clarification, approvals, and delivery result review.

**Architecture:** Keep E2E tooling isolated under `e2e/`. The test runs the existing Vite frontend and intercepts `/api/*` calls with a stateful browser-side fixture that follows the current REST projection contracts, including `POST /api/projects/{projectId}/sessions`. The flow validates the real console UI while avoiding backend, OpenAPI, coordination-store writes, and final plan status writes.

**Tech Stack:** Playwright Test, Vite frontend dev server, TypeScript test fixtures, route-level REST projection mocks.

---

## Scope

This is the official QA-E2E-V6.2 worker plan. The original `QA-E2E-V6.2` claim on branch `test/qa-e2e-regression`, coordination base `2e682ec`, produced a `mock_ready` checkpoint because `New session` was not wired to the frontend create-session action. The finalization claim is `QA-E2E-V6.2-FINAL` on branch `test/qa-e2e-regression`, coordination base `e668943`, after AL06 integrated the new-session frontend prework.

**Create or modify:**
- `.gitignore`
- `docs/plans/implementation/v6.2-playwright-success-flow.md`
- `e2e/package.json`
- `e2e/package-lock.json`
- `e2e/playwright.config.ts`
- `e2e/tests/function-one-full-flow.spec.ts`
- `docs/plans/acceleration/reports/QA-E2E-V6.2.md`
- `docs/plans/acceleration/reports/QA-E2E-V6.2-FINAL.md`

**Do not modify:**
- QA-V6.4 files
- OpenAPI files or tests
- shared coordination store
- `docs/plans/function-one-platform-plan.md`
- split plan final task status
- acceleration plan final task status

## Requirements Mapping

- Finalization claim metadata: `claim_id=QA-E2E-V6.2-FINAL`, `lane_id=QA-E2E`, `task_id=V6.2`, `branch=test/qa-e2e-regression`, `coordination_base=e668943`.
- `e2e/package.json` defines `npm --prefix e2e run test`.
- The Playwright spec starts from no current project session, clicks the sidebar `New session` button, verifies that the test fixture received one `POST /api/projects/{projectId}/sessions` request, and then opens the returned draft session.
- The Playwright spec drives the console through initial requirement, clarification reply, solution design approval, code review approval, and final `delivery_result`.
- Mock API responses use current frontend API paths: `/api/projects`, `/api/projects/{projectId}/sessions` for both `GET` and `POST`, `/api/sessions/{sessionId}/workspace`, `/api/sessions/{sessionId}/messages`, `/api/approvals/{approvalId}/approve`, and inspector detail endpoints.
- Assertions cover Narrative Feed, Composer, Approval Block, Inspector, Delivery Result, focus restoration, global overflow, and a narrow viewport pass.
- The AL06 frontend prework integrated in `e668943` wires the `New session` sidebar button to `createSession()`, allowing this finalization checkpoint to declare expected ingest result `implemented` after commit.

## Frontend Design Gate

- Register: product UI. The inherited project baseline is an information-dense workspace UI centered on Narrative Feed, Run Switcher, Composer, Approval Block, Inspector, and DeliveryResultBlock.
- Context: repo-level `PRODUCT.md` and `DESIGN.md` are absent in this worktree, so this plan uses `docs/specs/frontend-workspace-global-design-v1.md` as the controlling frontend design source and keeps product semantics unchanged.
- Required checks: the Playwright spec asserts state stability across the success path, focus restoration after Inspector opening, no global horizontal overflow, and a narrow 390px viewport pass.
- Boundary: this task does not change visual styling or component semantics; it only verifies the existing UI against V6.2 success-path expectations.

## TDD Record

- [x] **Step 1: Run the target command before scaffolding**

Run:

```powershell
npm --prefix e2e run test -- function-one-full-flow.spec.ts
```

Expected before scaffolding: fails because `e2e/package.json` does not exist.

Observed:

```text
npm error enoent Could not read package.json: Error: ENOENT: no such file or directory, open '...\e2e\package.json'
```

- [x] **Step 2: Add isolated e2e package manifest**

Create `e2e/package.json` with:

```json
{
  "name": "ai-devflow-engine-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
```

- [x] **Step 3: Add Playwright config**

Create `e2e/playwright.config.ts` with a local frontend web server, using `E2E_BASE_URL` when an already-running app should be reused.

- [x] **Step 4: Add success path spec**

Create `e2e/tests/function-one-full-flow.spec.ts` with:
- stateful API route fixture
- full success-path UI test
- Run Switcher visibility and current-run assertions with historical and active run metadata
- global overflow helper
- narrow viewport assertions
- focus assertion after opening Inspector

- [x] **Step 5: Run the target command after scaffolding**

Run:

```powershell
npm --prefix e2e run test -- function-one-full-flow.spec.ts
```

Expected in an environment without installed e2e dependencies: fails because `@playwright/test` / `playwright` is not installed under `e2e/`.

Observed in the main worktree before migration:

```text
> ai-devflow-engine-e2e@0.1.0 test
> playwright test function-one-full-flow.spec.ts

error: unknown command 'test'
```

This indicates npm reached the new package script, but there is no local `e2e/node_modules/.bin/playwright` from `@playwright/test`; a non-local Playwright binary handled the command and did not support the test runner subcommand.

In the main worktree before migration, after user-approved dependency installation, `npm --prefix e2e install` generated `e2e/package-lock.json`. After user-approved Chromium setup, `npm --prefix e2e exec -- playwright install chromium` installed the local browser binary.

First browser-backed run failed because the route fixture matched `**/api/**`, which also intercepted Vite source modules such as `/src/api/hooks.ts`. The fixture returned a mocked API 404 for that module, React did not mount, and the first visible-region assertion failed. The route fixture was narrowed to requests whose pathname starts with `/api/`, preserving frontend module loading while still mocking backend API calls.

Observed in the main worktree before migration after the fixture correction:

```text
> ai-devflow-engine-e2e@0.1.0 test
> playwright test function-one-full-flow.spec.ts

Running 1 test using 1 worker

  ✓  1 [chromium] › tests\function-one-full-flow.spec.ts:83:3 › function one success path › completes requirement, clarification, approvals, and delivery result in the console (1.6s)

  1 passed (3.8s)
```

- [x] **Step 6: Optional focused frontend sanity command**

Run only if needed for integration confidence:

```powershell
npm --prefix frontend run test -- ConsolePage
```

This is not a substitute for the Playwright command.

Observed in the main worktree before migration:

```text
> ai-devflow-engine-frontend@0.1.0 test
> vitest --run WorkspaceShell

Test Files  1 passed (1)
Tests  25 passed (25)
```

## Coordinator Review Notes

- Dependency installation and browser setup were performed only after user approval for V6.2 installation.
- `e2e/package-lock.json` is part of the official QA-E2E-V6.2 worker diff because local e2e dependencies were installed for verification.
- The current UI exposes `New session` as a button without a handler; a later frontend slice must either implement that behavior or adjust V6.2 acceptance setup language.
- Worker evidence is recorded in `docs/plans/acceleration/reports/QA-E2E-V6.2.md`; the worker branch must not update the coordination store, platform plan, split plan, or acceleration execution plan final status.

## Current Worker Verification

Run in `C:\Users\lkw\Desktop\github\agent-project\ai-devflow-engine\.worktrees\test-qa-e2e-regression` after migrating the official V6.2 files:

```powershell
npm --prefix e2e install
```

Observed:

```text
added 3 packages, and audited 4 packages in 1s
found 0 vulnerabilities
```

```powershell
npm --prefix e2e run test -- function-one-full-flow.spec.ts
```

Observed:

```text
> ai-devflow-engine-e2e@0.1.0 test
> playwright test function-one-full-flow.spec.ts

Running 1 test using 1 worker

  ok 1 [chromium] tests\function-one-full-flow.spec.ts:83:3 function one success path completes requirement, clarification, approvals, and delivery result in the console (1.8s)

  1 passed (4.1s)
```

```powershell
npm --prefix frontend run test -- --run WorkspaceShell
```

Observed:

```text
> ai-devflow-engine-frontend@0.1.0 test
> vitest --run WorkspaceShell

Test Files  1 passed (1)
Tests  29 passed (29)
```

The QA-E2E worktree required `npm --prefix frontend ci` before browser verification because `frontend/node_modules` is intentionally worktree-local and ignored. The install used the existing `frontend/package-lock.json` and did not modify frontend manifests or lock files.

## Finalization Verification On `e668943`

The finalization claim `QA-E2E-V6.2-FINAL` updates the browser regression from an API-provided draft session to a UI-created draft session.

RED command after adding the `New session` click and fixture counter:

```powershell
npm --prefix e2e run test -- function-one-full-flow.spec.ts
```

Expected failure: the fixture has no `POST /api/projects/{projectId}/sessions` handling, so the counter remains `0`.

Observed:

```text
Expected: 1
Received: 0
Timeout 5000ms exceeded while waiting on the predicate
```

GREEN implementation:

- The fixture starts with an empty session list.
- The fixture handles `POST /api/projects/{projectId}/sessions`, increments `createdSessionCount`, stores the returned draft session, and responds with `201`.
- The existing success path continues from the created draft session.

GREEN command:

```powershell
npm --prefix e2e run test -- function-one-full-flow.spec.ts
```

Observed:

```text
Running 1 test using 1 worker
  ok 1 [chromium] tests\function-one-full-flow.spec.ts:83:3 function one success path completes requirement, clarification, approvals, and delivery result in the console (1.6s)
  1 passed (3.9s)
```

Impacted frontend checks:

```powershell
npm --prefix frontend run test -- --run WorkspaceShell
npm --prefix frontend run test -- --run
npm --prefix frontend run build
```

Observed:

```text
WorkspaceShell: Test Files 1 passed, Tests 31 passed
frontend test --run: Test Files 29 passed, Tests 229 passed
frontend build: tsc --noEmit && vite build completed, 137 modules transformed
```

## Remaining Owner Blocker

No V6.2 owner blocker remains after AL06 new-session prework. Final platform-plan and split-plan status updates still belong to the main coordination session after worker checkpoint commit, ingest, and integration checkpoint verification.
